# 欧陆风云 4 数据位置参考

> 本机实测路径与文件清单。项目里凡是要读游戏本体的地方（离线渲染、浏览器端选目录、旗帜导出、颜色比对）都以这份为准。
> 最后核对：2026-09-17。

---

## 一、三个根目录

| 用途 | 路径 |
|---|---|
| **游戏本体** | `D:\Software\Steam\Steam\steamapps\common\Europa Universalis IV` |
| **创意工坊模组** | `D:\Software\Steam\Steam\steamapps\workshop\content\236850`（236850 是 EU4 的 appid） |
| **本项目** | `F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser` |

代码里读它们的地方：`scripts/lib/map-assets.ts`（`EU4`、`WORKSHOP_ROOT`、`MOD_LOCALISATION` 三个常量）。

---

## 二、查看页真正需要的文件（只有这些）

离线渲染与浏览器端"选游戏目录"读的是同一批：

| 文件 | 大小 | 作用 |
|---|---|---|
| `map/provinces.bmp` | 34.6 MB | 每像素一个省份颜色，解码成省份 id 图（5632×2048） |
| `map/definition.csv` | 130 KB | 颜色 → 省份 id/名字 |
| `map/default.map` | 10 KB | `sea_starts` / `lakes` 海域省份表 |
| `common/religions/*.txt` | 53 KB（`00_religion.txt`） | 宗教颜色表 |
| `localisation/*_l_english.yml` | — | 国名、宗教名等文本（**中文靠模组覆盖**，见下） |
| `common/countries/*.txt` | 979 个文件 | 国家颜色（`color = { r g b }`） |
| `common/country_tags/00_countries.txt` | — | tag → 国家文件的映射（如 `HAB = "countries/Austria.txt"`） |
| `gfx/flags/` | — | 国旗美术源（导出成 `assets/flags/*`） |

**浏览器端只留了 3 个必需文件**（`provinces.bmp`、`definition.csv`、`default.map`），其余都是"有则更好"。

---

## 三、中文国名从哪来（关键）

- 汉化在**工坊模组**里，不在游戏本体：`…\workshop\content\236850\2976470733\localisation\`
  - 该模组是 **Chinese Language Mod for 1.37**，共 145 个 `*_l_english.yml`（合计 14.3 MB）
  - 查看页**只读其中两个**：`text_l_english.yml`（699 KB）、`countries_l_english.yml`（32 KB）
- 本机共 **63 个模组**带 `localisation/` 目录，但只有上面这个（及少量同类汉化）含中文；判定中文的办法是**解码后找 CJK 字符**，不能用 UTF-8 直接读——EU4 的本地化文件是 **CP1252 + 字母流编码**。
- 离线渲染写死了这个模组（`MOD_LOCALISATION`）；浏览器端让你**选一次目录**（mod 根目录或它里面的 `localisation` 都行），结果合并后存进 IndexedDB，只选一次。
- 合并顺序：**游戏目录在前、汉化 mod 在后**（后者覆盖前者）。顺序就是功能本身——反过来所有国名退回英文。

---

## 四、存档与存储

| 东西 | 位置 |
|---|---|
| 仓库里的示例存档 | `存档示例/mp_俄罗斯1574_11_12.eu4`（8.7 MB，1574.11.12，玩家 RUS）——测试用它，别删 |
| 本地站点存储（与 R2 同构） | `.dev-storage/`（**用户真实数据，不要拿去跑破坏性测试**） |
| 存储布局 | `saves/<id>/original.eu4`、`saves/<id>/meta.json`、`saves/<id>/viewer/data.json`、`saves/<id>/viewer/raster.png`、`saves/<id>/viewer/index.html`（离线自包含页，可选） |
| 索引 | `.dev-storage/index.json` |

**当前 `.dev-storage` 里的四份存档**：

| id | 存档 | 玩家 | 日期 | 备注 |
|---|---|---|---|---|
| `b6263f5baf54` | mp_俄罗斯1574_11_12 | RUS | 1574.11.12 | 有离线自包含页 |
| `19638ff27da9` | 奥地利帝国1621_02_02 | HAB | 1621.2.2 | **配色三模式的验证样本**（匈牙利 1612 起变红） |
| `266f9640d1c6` | mp_法兰西骑士团1575_04_02 | TEU | 1575.4.2 | |
| `c5464cb5f049` | 诺夫哥罗德1525_12_21 | NOV | 1525.12.21 | |

**浏览器端缓存**（都在用户自己的浏览器里，不上传服务器）：

- IndexedDB `eu4-save-archive`（v3）：`game-map`（解码后的省份 id 图，约 23 MB）+ `game-tables`（合并后的国名约 1 万条 + 宗教颜色，约 1 MB）
- `localStorage['eu4analyser.settings']`：主题、背景模式/间隔/蒙版/启用列表、旗帜来源、配色模式
- `localStorage['catalogue.token']`：上传口令（生产环境写操作需要）

---

## 五、站点资源与运行地址

| 东西 | 位置 |
|---|---|
| **部署产物 = 这一个目录** | `apps/site/public/`（Cloudflare Pages 直接发布它，无构建步骤） |
| 查看页三件套 | `viewer.html`（骨架）、`viewer.js`（客户端）、`paint.js`（画师，由 `scripts/lib/paint.js` 生成） |
| 数据面构建 | `viewer-build.js`（浏览器）、`viewer-data.js`、`viewer-store.js`（生成并入库）、`viewer-page.js`（托管页引导） |
| 背景/主题共享组件 | `page-theme.js` |
| 国旗 | `public/assets/flags/{base,modded,colonial,colonial-modded}/`（共 2125 个文件；`mods/` 14550 个仅本地、已 gitignore） |
| 背景图 | `public/背景图/`（16 张）+ `public/wallpapers.json` |
| 解析器 bundle | `public/eu4-parser.js`（由 `packages/eu4-parser/src/*.ts` 用类型剥离生成） |

**地址**：

- 本地目录页 http://127.0.0.1:8788
- 托管查看页 `/viewer.html?id=<存档id>`（先找已生成数据，没有才现场生成）
- 离线自包含查看页 `/saves/<id>/viewer/index.html`
- 生成数据的读取路径 `/saves/<id>/viewer/data.json` 与 `/saves/<id>/viewer/raster.png`

---

## 六、部署要点（Cloudflare）

- Pages：Root directory `apps/site`、**Build command 留空**、Output directory `public`
- R2 绑定变量名 **`SAVES_BUCKET`**；环境变量 **`UPLOAD_TOKEN`**（不设则 API 只读）
- 免费版限制：**单站点 20,000 个文件、单文件 25 MiB**（[官方文档](https://developers.cloudflare.com/pages/platform/limits/)）。实测发布 2159 个文件 / 46.7 MB，占上限 10.8%
- ⚠️ **必须走 git 部署，不要 `wrangler pages deploy apps/site/public`**：本地那 14550 个 `assets/flags/mods/` 会被一起上传，直接撞两万上限
- 发布前跑 `pnpm verify:deploy`（或双击 `检查部署.bat`）

---

## 七、本机容易踩的环境坑

- EU4 本地化是 **CP1252 + 字母流编码**，用 UTF-8 直接读会全是乱码（连"有没有中文"都判断错）。
- 创意工坊 63 个模组带 `localisation/`，**不要全合并**——只读汉化 mod 的那两个文件才和离线渲染结果一致。
- `.bat` 必须 **CRLF**；控制台绝不能出现 UTF-8 中文，也不要 `chcp`（cmd 会提前终止批处理，历史上踩过两次）。
- PowerShell 里用 `[IO.File]::ReadAllText/WriteAllText` 配 `UTF8Encoding($false)` 处理含中文的路径，别用 `Get-Content/Set-Content`（会破坏中文）。
- `git` 不在本机 PATH 上（`git status` 会报 CommandNotFound）。
