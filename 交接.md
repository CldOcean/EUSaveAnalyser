# EUSaveAnalyser 交接档案

最后更新：2026-09-19（**卡片客制化第一～六轮全部落地**：目录页卡片可改名 / 旗帜原版↔国家娘 / 结档缩略图 / 主角旗帜分页 / 排序含结档·封档日期；查看页标题行最右「📝 修改存档信息」可改标题·模组·结档日期·封档日期·主角国家（TAG 弹列强榜前 15），H1 可改名，副标题＝版本/结档日期/封档日期/模组，标题行有主角旗帜排；**殖民地旗改为「宗主旗 + 右半色块」运行时叠加**，不再构建期生成图片；两页导航亚克力。`pnpm test` **193 全过**、`pnpm verify:player`、`pnpm verify:deploy` 全绿）
最后更新（**面板阶段**）：2026-09-19 **省份/国家详情面板全部落地**——查看页点陆地省份 → 地图左缘「省份详情」面板（12 行 KV ＋ 地形/奇观/思潮/归属地区 ＋ 4 张分组表，含拥有者/宗教/文化变更史与「把地图切到那天」）；「转到国家」→ 国家详情面板（**13 个 Tab**：总览/君主/将领/文化/宗教/历史/顾问/建筑/州/阶级/财政/点数/思潮，按 pdx-tools 的构成复刻、全中文）；面板透明度由 🎨 里新增的「面板」拉条控制；地图新增 **⛶ 全屏**（保留下方工具栏）；光标统一普通箭头；面板旗帜＝目录页卡片主角旗尺寸（**48×48**）；新增 `apps/site/public/assets/ui/**`（**1,944 文件 / 20.7 MB**，9 个图标族 ＋ 6 张 JSON 表）与构建期中文名字表（`DATA.uiNames`）。`pnpm test` **203 全过**、`pnpm verify:player`（面板 **50/50** ＋ 运行期全过）、`pnpm verify:deploy` 全绿。
最后复核：2026-09-19 用户**人工验收通过**（四个验收清单在 `tmp/验收-阶段1.md`／`-阶段1.5.md`／`-阶段2.md`／`-阶段3.md`）；2026-09-18 实测三项通过（🎨、国名汉化、背景图）；**实际发布暂缓，先继续完善功能**。

---

## 一、这个项目是什么

把历年《欧陆风云 4》存档做成个人纪念网站：上传存档 → 在浏览器里生成月度版图时间线 → 九个视图（政治/宗教/文化/发展度/科技/思潮/王朝/神罗/战斗）逐帧回放 + 国家兴衰曲线 + 三张表格。

**部署目标**：GitHub + Cloudflare 免费版（Pages 静态 + Functions 后端 + R2 存存档），无常驻 Node 服务。
**决定性约束**：Cloudflare Worker 无文件系统、免费版 10ms CPU / 128MB 内存，而 `gamestate` 解压 57 MB → **解析与生成只能在浏览器或本地做**。

---

## 二、现在能用的功能（已实现并验证）

| 功能 | 状态 |
|---|---|
| 目录页：上传/拖拽、卡片信息、排序、删除、上传口令 | ✅ |
| 浏览器内解析（只读 3 KB 的 `meta`，约 7 ms，不碰 57 MB gamestate） | ✅ |
| 浏览器内生成完整时间线数据（数据面 38 个键与离线实现逐键相同） | ✅ |
| 上传后自动生成并入库（`viewer/data.json` + `viewer/raster.png`，约 2.7 MB/份）→ 查看秒开 | ✅ |
| 查看页九视图、播放/调速/缩放/分辨率、三张表、兴衰曲线 | ✅ |
| 国旗（原版 1027 / 国家娘 1076 / 殖民地合成旗 22） | ✅ |
| 背景图轮播（16 张，列表/随机/单图、间隔、蒙版、缩略图）+ 四套主题 | ✅ |
| 配色三模式（模组色 / 原始色 / 属国染色），属国从成为属国那天起变色，改后与宗主 ΔE 8~14、殖民地不再全白 | ✅ 2026-09-18 |
| 国名汉化（选一次汉化 mod 的 localisation 目录，合并结果入 IndexedDB） | ✅ **用户已确认可用** |
| 背景/主题：目录页与查看页**共用一份实现**（右下角 🎨，含离线自包含页） | ✅ 2026-09-18 |
| **卡片客制化**：标题点击改名、旗帜原版↔国家娘、结档缩略图、主角旗帜分页（每页 3 个）、排序含结档·封档日期 | ✅ 2026-09-19 |
| 查看页「📝 修改存档信息」（标题行最右）：标题/模组/结档日期/封档日期/主角国家（TAG 弹列强榜前 15）；H1 可改名 | ✅ 2026-09-19 |
| 殖民地旗＝**宗主旗 + 右半色块**（运行时叠加，跟随「旗帜」按钮；颜色哈希修好后不再全是黄） | ✅ 2026-09-19 |
| 两页导航栏亚克力（半透明毛玻璃，四套主题适配） | ✅ 2026-09-19 |
| 封档日期（现实时间，默认上传日期，卡片/面板/副标题/排序四处口径一致） | ✅ 2026-09-19 |
| 阶段 D：`public/` 即部署产物、旗帜瘦身、`pnpm verify:deploy` 闸门 | ✅ |
| **省份详情面板**（点任一陆地省；贴地图左缘、竖版 0.618、半透明、四主题）：发展度三数 / 拥有者 / 占领者 / 核心 / 宣称 / 宗教 / 文化 / 荒废度 / 贸易品 / 潜在贸易品 / 贸易公司 / 建筑网格（一行 4 个＋中文图注）/ 地形（名＋图）/ 奇观 / 思潮 / 归属地区 ＋ 4 张分组表（扩建基础设施 / 州 / 贸易公司投资 / 省份历史，**每条带「把地图切到那天」**）；**海洋不高亮、点海洋直接关闭** | ✅ 2026-09-19 |
| **国家详情面板**（省份面板「转到国家」、点国旗或列强榜）：**13 个 Tab** —— 总览（3×8 网格＋使者行＋军事卡＋君主卡＋理念卡＋战争区＋外交区，顶部 48×48 旗＋国名）/ 君主 / 将领 / 文化 / 宗教 / 历史 / 顾问 / 建筑 / 州 / 阶级 / 财政（区间切换＋瀑布图＋明细＋支出树）/ 点数（三条量条＋明细）/ 思潮（「已接纳 N/8」徽章＋各省进度条）；**省↔国互相跳转、返回栈一层** | ✅ 2026-09-19 |
| 面板透明度「**面板**」拉条（🎨 内、蒙版下方，仅查看页渲染）；**⛶ 全屏**（保留下方工具栏）；地图光标统一 `default`（无抓手） | ✅ 2026-09-19 |
| 名字本地化：文化/特质/政体改革/建筑/奇观/地形/思潮/理念/顾问/地区/兵种的中文名**构建期烤进 `DATA.uiNames`**（汉化 mod ＋ 277 条自造；自造清单 `tmp/自造译名清单.md`） | ✅ 2026-09-19 |
| `assets/ui/**` 素材与表：建筑 47 / 奇观 143 / 地形 17 / 宗教 39 / 思潮 8 / 政体改革 670 / 理念 30 / 修正 923 / 图标 67 ＋ `uiNames`·`provinceTerrain`·`area`·`ledgerSlots`·`manaSlots`·`advisorIds` 六张 JSON（全部小写游戏 key，缺图退化成文字块） | ✅ 2026-09-19 |

---

## 三、查看页统一成 🎨 形式（2026-09-18 已完成）

**做了什么**：查看页顶部的 ⚙ 设置按钮整块删掉（它里面只有主题和背景图两项，都搬到了 🎨）。工具栏只留 **旗帜 / 配色 / 分辨率**，右下角是 🎨。目录页、托管查看页、离线自包含页挂的都是同一个 `apps/site/public/page-theme.js`，三处读写同一个 `localStorage['eu4analyser.settings']`。

**关键约束**：离线自包含页面是把 `viewer.js`、`paint.js`、`page-theme.js` 的源码**内联**进 HTML 的，所以下面六步当时必须一起做完（任何一步单独做都会让离线页报错或丢掉 🎨）。

1. **`page-theme.js` 双形态**：保持 ES 模块给托管页；离线内联时按 `paint.js` 同款剥掉 `export `（`replace(/^export /gm, '')`）。全部 6 个导出都是 `export const` / `export (async) function`，没有其它 ESM 语法，由 `build-guards.test.ts` 静态把关。
2. **`scripts/render-timeline.ts` 加 `THEME_SOURCE`**：剥好的 `page-theme.js` 作为 `<script>` 排在 `VIEWER_SOURCE` **之前**，离线页因此有全局 `mountPageTheme`（+5 KB，`index.html` 2.70 MB）。
3. **`viewer-page.js` 暴露同样的全局**：挂载播放器前 `await import('./page-theme.js')` 并把导出挂到 `globalThis`（失败只丢 🎨，不挡地图）。
4. **删掉查看页自己的那一套**：`viewer.html` 删掉 ⚙ 按钮、`#panel1..3`、`#bgA/#bgB/#bgDim/#bgList/#dimRange/#dimVal/#bgInterval` 标记（旧 CSS 留着，无害）；`viewer.js` 删掉设置面板 / 主题 / 背景轮播 / 缩略图整段 **205 行**，只留一个 6 行的本地 `loadSettings()`（`flagSet` 与 `colourMode` 仍读同一个键），改为调用 `globalThis.mountPageTheme({ wallpapers: BG })`。
5. **测试同步改**：`scripts/verify-player-run.ts` 第 8 节改为断言"共享组件真的挂上了"（2 个轮播层 + 蒙版 + 注入的样式表 + 16 张缩略图 + 主题已落盘），第 9 节改为"← 回目录页 + 🎨 开合 + 旧面板 id 一个不剩"；`apps/site/test/frontend.test.ts` 改为断言旧控件**不在**标记里、而由 `page-theme.js` 拥有；`build-guards.test.ts` 增加 `THEME_SOURCE` 位置与"可剥离"守卫；`verify-deploy.ts` 把 `page-theme.js` 列为必需入口。
6. **重新生成 + 全量验证**：`pnpm timeline` → `pnpm verify:player` → `pnpm verify:deploy` → `pnpm test`（162 条全过）→ 重建 `mp_俄罗斯1574` 预生成页（`b6263f5baf54`，10 个产物已入库）。

**验证状态**：真实浏览器目视确认**已由用户完成 ✅**（目录页/查看页各点一次 🎨、切主题、切背景模式，背景图正常显示）。

---

## 四、查看页配色的两处修复（2026-09-18）

**问题一：原始色模式下殖民地政权全白。** 根因不在取色逻辑，而在存档本身：EU4 把 `C##`/`D##` 这类**动态国家**的 `colors.color` 写成占位值 `255 255 255`，真色只写在 `map_color` 里（`country_color` 与它永远相同，且会被「让属国染上你的颜色」类 mod 改写，**不能当原色用**）。11 个样例存档共 404 个纯白 `color`，其中只有 `C##` 殖民地真拥有省份。

**修法**：把 `255 255 255` 当作"没有颜色"，此时**用宗主（`colonial_parent`）自己的 `color` 推导**一个亲属色（引擎对殖民地正是这么做的：向白提亮 8%~23%），而不是回退到 `map_color`——否则会把染色 mod 的结果混进"原始色"。实测推导值与引擎自己写下的 `map_color` ΔE 中位 14、色相不变；修完后"拥有省份却画纯白"的国家 = **0**。

**问题二：属国染色模式下属国与宗主不够像。** 实测（mp_俄罗斯1574，33 组关系）改动前 ΔE(属国, 宗主) 中位 **34.8**、最大 **72.6**（ΔE≈2.3 才是"刚能看出差别"）；而同一宗主的兄弟属国之间中位只有 **0.8**（GBR 的 C04/C05 完全相同 `#d49393`）——两头都不对。根因是 `variantRgb` 的 `amount = 0.28~0.60` 是拍脑袋的百分比。

**修法**：不再猜百分比，改为**按目标 ΔE 反解 amount**（二分 24 步，构建期一次算完），同一宗主的属国按 tag 排序在 **ΔE 8~14** 区间均匀取档（用户选的"更相似"档）。改动后：属国↔宗主 **7.9~14.4**（中位 11.0），兄弟之间 **1.2~6.4**（中位 3.2）。近白宗主（奥地利 `#dcdcdc`）自动走压暗，ΔE 照样落在区间内。

**代码落点**：ΔE/Lab/反解/分层这些纯数学放 `packages/eu4-parser/src/colours.ts`，由 `build-browser-parser.ts` 一并打进 `public/eu4-parser.js`，**两个数据面共用同一份**（`SUBJECT_SHADE` 是唯一的旋钮，想改相似度只动这里）；"谁跟随谁"仍按纪律在两个数据面各写一份（`viewer-build.js` / `render-timeline.ts`），由逐键比对守着。**改了 `colours.ts` 必须重跑 `pnpm browser`（或至少 `node scripts/build-browser-parser.ts`）**。

**谁跟随谁的规则**：`diplomacy/dependency` 账本是准绳——**已独立的属国不在账本里，因此保持自己的色**（不编造独立日期）；`colonial_parent` 只补一个缺口：某殖民地没有 dependency 记录（游戏对"母国自己也没有领土"的殖民记录不写），且**两端都真的拥有省份**时才按"存档开局"起染色。

---

## 五、新会话可直接粘贴的提示词

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读项目根目录的 交接.md 和 欧陆风云4-数据位置.md，那里有当前进度（卡片客制化六轮 ＋ **省份/国家详情面板全部落地并已人工验收**、
殖民地旗是运行时叠加）、还没做的事和本机游戏文件位置。
测试条数以 `pnpm test` 实跑输出为准，不要引用文档里的数字。

当前阶段：**实际发布暂缓，继续完善功能**（不要催促发布）。

要求：
- 汇报时先给结论（做完了什么、验证结果），不要叙述调试过程；
- 改完必须跑 pnpm test、pnpm verify:player、pnpm verify:deploy，并重新生成离线页；
- 动过 assets/ui/*.json 或数据面之后，先 pnpm timeline 再 pnpm test；刷新存量存档用 修复存档数据.bat（已 --all，每份都重建）；
- 遇事直接给方案和推荐，关键选择让我拍板；
- 需要执行脚本时给我双击即用的 .bat，不要让我开终端敲命令。
```

---

## 六、本机常用命令与文件

| 用途 | 命令 / 文件 |
|---|---|
| 启动本地站点 | 双击 `启动网站.bat`（→ http://127.0.0.1:8788） |
| 本地生成一份自包含查看页 | 双击 `生成查看页.bat`（拖入 .eu4） |
| 只解析看报告 | 双击 `解析存档.bat` |
| 发布前闸门 | 双击 `检查部署.bat`（报告写进 `tmp\deploy-check.txt`） |
| 全量测试 | `pnpm test`（**203 条**，以实跑输出为准） |
| 重建/刷新已存存档的数据面 | 双击 `修复存档数据.bat`（**已改 `--all`：每份都重建**，跑前自动备份 index/meta，中文报告写 `tmp\修复存档数据.txt`） |
| 改过 `assets/ui/*.json` 或数据面之后 | 必须先 `pnpm timeline` 再 `pnpm test`（离线参照与浏览器面要同步，否则 `viewer-build.test.ts` 报假红） |
| 运行期播放器验证 | `pnpm verify:player` |
| 离线重建时间线 | `pnpm timeline` |
| 重建浏览器端资源 | `pnpm browser`（`eu4-parser.js` + `paint.js` + `wallpapers.json`） |

**四个 `检查/启动` 的 .bat 约定**：必须 **CRLF** 换行；控制台只输出 ASCII 且绝不 `chcp`（UTF-8 中文会让 cmd 提前终止批处理）；中文详情写文件用记事本打开。已有测试静态把关。

**跑 `pnpm test` 的环境提醒**（2026-09-18 踩到）：如果 DSH 会话的文件沙箱是 `workspace-write`，Node 的测试运行器（每个测试文件一个子进程）和 `frontend.test.ts` 里的 `execFileSync(node --check)` 都要开带管道的 stdio，会被挡成 `spawn EPERM`，表现为"全红但没人改错东西"。把该会话放宽到 `danger-full-access` 再跑即可；`pnpm timeline`、`pnpm verify:player`、`pnpm verify:deploy` 不 spawn，不受影响。

---

## 七、还没做 / 已知局限

- **卡片客制化已全部落地（2026-09-19）**：任务书现行版在项目根 `卡片客制化-任务书.md`（**只写还没做的事**）；第 1–6 轮的历史（每轮根因、被否掉的方案）归档在 `docs/卡片客制化-任务书-历史（第1-6轮）.md`。
- **`viewer/data.json` 两种格式打架 → 已修（2026-09-19，任务 D）**：`apps/site/src/build-viewer.ts` 现在是**唯一生产者**，只发布 `{version:2,data,panels}`；原始离线面不再进存档目录；10/10 都是 payload。
- **面板阶段已完成（2026-09-19）**：任务书＝项目根 `省份国家界面阶段任务书.md`（含四个阶段的验收清单、§11 后续波次提示词、§12 对话顺序；**已归档**）；调研材料＝`tmp/pdx-province-panel-report.md`、`tmp/details-*-probe.*`、`tmp/pdx-province-panel-report.md`；冻结契约字典＝`第三对话任务书.md`（§1.3 数据面 / §1.6 DOM / §1.7 交互 / §1.9 13 Tab / §3.2 素材映射）。
- **数据面变大（待决定是否瘦身）**：单份 payload **3.2–6.1 MB**（离线参照 `tmp/timeline/data.json` 6.35 MB）；gzip 约 22–25%，Cloudflare 走 brotli 后传输约 1–1.5 MB，但浏览器要多解析几 MB JSON。**若在意"秒开"，可让数据面瘦身**（历史事件只留最近 N 条 / 国家只留国力前 N 名）。
- **277 条自造译名待用户核对**：`tmp/自造译名清单.md`（游戏与全机 46 个 mod 的 localisation 里都没有中文的 key）。
- **待定小事**：国家面板「总览」顶部国名现用 **11px**（＝目录页卡片主角说明那一档），用户若想更大（如卡片标题的 15px）改一行 CSS 即可。
- **面板里明确"不做"的**：思潮 Tab 只做「已接纳 N/8」徽章＋各省进度条（**不做 pdx-tools 那种"最省点数接纳思潮"的规划器**）；州表的治理成本/集权列标"暂不提供"；`absolutism` 等存档没记录的字段显示 `—` 并注「存档未记录」；国家缺 `ledger`/`mana` 槽位时财政/点数显示「**数据待补**」（不显示错位科目名）。
- **实际发布暂缓（用户 2026-09-18 决定）**：GitHub + Cloudflare 先不发，**现在的阶段是继续完善功能**；`pnpm verify:deploy` 闸门已就绪，随时可发。
- **三项验证已全部通过（用户实测）**：真实浏览器目视（🎨 开合、四主题、三种背景模式与蒙版）✅、国名汉化 ✅、背景图正常 ✅。
- **`file://` 直开的代码事实（不再是待办）**：`BG` 是站点根绝对路径（`/背景图/…`），双击 HTML 直开时解析成 `file:///背景图/…`；经 `启动网站.bat`（http）打开一切正常——用户实测背景图正常走的就是 http 这条路。
- 主题让**查看页与目录页**都跟随同一个 `page-theme.js`：查看页工具栏/图表/导航的额外变量（`--shadow`、`--map-bg`、`--chart-*`、`--nav-*`）仍由 `viewer.html` 自己定义，只有 `--bg/--panel/--ink/--accent/--border` 这批通用变量以共享组件为准。
- 国名汉化：**已确认可用**；若哪天托管页国名又变英文，用 `viewer.html?id=<id>&reset=1` 重选汉化目录（`…\workshop\content\236850\2976470733`）。
- 属国颜色：存档只保留**现存**属国关系（带 `start_date`），已结束的关系不在档内；查看页只按账本给"现存属国"上色，**不编造独立日期**；同一国两次成为属国时只保留最后一次起始日期。殖民地另有 `colonial_parent`（真正的母子关系），只在账本缺记录、且母子两端都拥有省份时兜底。
- **「独立后 mod 会把原色还回」这个前提是错的（2026-09-18 实测推翻）**：西1567 档（`8fc9283423d0`）有 **31 个国家不是属国却画着别人的本色**——瓦剌 `OIR` 的 `map_color` 还是明 `MNG` 的 `#b38068`，而它自己的 `color` 是 `#ccb8b1`（＝游戏文件 `common/countries/OiratHorde.txt` 的 `204 184 177`，即真原色）。机制来自 mod（工坊 3278610540「让属国染上你的颜色」，存档里记的旧名是「让附庸国染上你的颜色」）：`change_country_color = { country = overlord }` 把全世界属国的 `map_color`/`country_color` 改成宗主色（`color` 保持原色），**独立还原只是一条决议**（`Ai_Subject_liberty_change_color`，人操国家得自己点），不点就永久残留；`Zifa_change_color_turn_off` 才会 `every_country` 一次性 `restore_country_color`。
- **朝贡国也被这个 mod 一起染了**（EU4 的 `is_subject` 把朝贡国算作 subject）：那 31 个里 20 个是朝贡国、其中 17 个的画面色恰是宗主本色，另外 3 个画着它们宗主自己的残留色。所以"模组实际行为与『朝贡国不算属国』一致"这个说法是错的——mod 并不区分。
- **已按 C 实施（2026-09-18，用户拍板）**：`packages/eu4-parser/src/colours.ts` 新增 `foreignTintFlags`（判据＝不是属国 + 拥有省份 + `map_color != color` + `map_color` 恰等于某国 `color`），**属国染色模式**把这类国家显示成它自己的 `color`；`模组色` 仍照存档画（游戏画面确实如此）、`原始色` 本来就看得到本色。实档结果：西1567 标记 **31** 个，其余 9 档 **0** 个（那 2 例 `FRS` 手动改色不匹配判据，未误伤）；离线面与浏览器面逐位一致。数据键＝`data.colours.foreignTint`（0/1 数组，`viewer.js` 的 `colourOf` 读它）。
- 殖民地的"原色"是从宗主原色推导的（存档里它们的 `color` 是占位白），所以**同一宗主下的殖民地原色是引擎近似值而非游戏文件原值**——色相与原值族一致，ΔE 落在 8~14。
- 朝贡国（`tributary_state`、`nahuatl_tributary` 等）不算属国——这是用户指定规则，查看页不给它们套宗主亲属色。**但注意染色 mod 不遵守这条**（它用 EU4 的 `is_subject`，把朝贡国也一起染成宗主色），所以属国染色模式要靠 `foreignTint` 把这类朝贡国的本色还回来。
