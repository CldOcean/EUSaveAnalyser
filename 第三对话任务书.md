# EUSaveAnalyser · 第三对话任务书（省份详情 / 国家详情面板）

> 2026-09-19 由**第三个主对话**定稿。执行者开工前先读 `交接.md` + `欧陆风云4-数据位置.md` + 本文件对应章节。
> **本文件的定位已收窄为"冻结契约与调研结论"**：具体派活、分阶段并行计划、每阶段给用户的验收清单，都在 **`省份国家界面阶段任务书.md`**（用户 2026-09-19 要求新建）——**要开工看那一份**，本文件当字典查（§1.3 数据面契约、§1.6 DOM、§1.7 交互、§1.8 全屏、§1.9 国家面板 13 Tab、§3.2 素材映射）。
> 本文件**只写还没做的事**；已完成的历史见 `交接.md` 与 `docs/卡片客制化-任务书-历史（第1-6轮）.md`。
> 用户的工作方式：主对话只规划与收尾，**具体实现交给新对话**；每个任务下面有一段**可直接粘贴的提示词**。
> 项目根：`F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser`。

---

## 0. 用户的原话与最高优先级的决策

用户原话（2026-09-19）：

> 「给视图添加省份详情页面板和国家详情页面板……**或者说，以上任务如果你能学会 pdxtools 的写法也行，复刻一个出来，这个最优先考虑！**」

**决策（主对话，见 §0.1 依据）：不移植 pdx-tools 的代码，而是"学它的信息架构与交互、用本项目现有技术栈实现"。**
理由：pdx-tools 是 Rust/WASM + Svelte 的独立应用，与本项目的静态站 + 原生 JS + canvas + 单 JSON 数据面**根本不是一条技术路线**；把它的省份/国家面板搬进来等于重写解析层与前端框架。而本项目已经有：省份历史回放、月度帧、旗帜运行时叠加、殖民地旗、离线自包含页、四套主题、`viewer-build.js` 与 `render-timeline.ts` 两份逐键一致的数据面。**复用这些比复刻 pdx-tools 便宜一个数量级。**

**要"学"的具体三点**（写进 §3.4 的验收）：
1. 面板是**选中态的投影**：hover 高亮、点击选中、选中态驱动面板内容（不是面板自己找数据）；
2. **省份 ↔ 国家互相跳转**是同一套选中态的两种形态，返回栈只有一层（回到上一个省份）；
3. 信息**分组**（概览 / 国力 / 科技思潮 / 政务 / 军事 / 外交 / 宗教文化 / 建筑奇观 / 历史），每组可折叠，空数据显式写"无"而不是留白。

### 0.1 pdx-tools 调研结论（2026-09-19，已核实完毕）

调研方式：本机 `web_fetch` 被沙箱 DNS 挡住（见 §0.2），改走 GitHub API / jsdelivr / codeload 拉全量源码（19.8 MB）本地精读。

**核实到的事实**

| 项 | 结果 |
|---|---|
| 许可证 | 仓库根 `LICENSE` = **AGPL-3.0**（`src/app/package.json` 里那个 `ISC` 是脚手架遗留错值）。AGPL 含"网络交互"条款，**个人非商业不是豁免理由** |
| 前端 | React 19 + react-router 8 + Vite + Tailwind 4 + zustand + TanStack Table / Virtual |
| 解析 | 浏览器 **WASM（Rust `wasm-eu4`）+ Web Worker（comlink）**；后端不参与分析 |
| 地图 | **WebGL2 自定义 shader**（5632×2048 拆成两张纹理），不是 canvas2d |
| 省份面板 | 右侧 `Sheet` 480px：一张 key-value 表（Development / Owner / Controller / Cores / Claims / Religion / Culture / Devastation / Trade Goods / Latent Trade Goods / In Trade Company / Buildings）＋ 4 张分组小表（Developments / States / Trade Company Investments / **Province History** 三列：日期｜事件｜图形） |
| 国家面板 | 右侧抽屉 970px（可折叠全宽），**13 个 Tab**：General / History / Institution / Advisors / Rulers / Leaders / Budget / Mana / Buildings / Religion / Culture / States / Estates |
| 省↔国跳转 | **没有"转到国家"按钮**：点任何国旗头像 = 一次 `setSelectedTag(tag)` 同时"选中该国 + 打开国家抽屉"；省份选中态只存在组件内、国家的存在 zustand store；**不写 URL、没有历史栈** |
| 海洋处理 | 后端对不可居住省（Ocean / Wasteland）返回 `null` → 前端关抽屉 + 清高亮（与我们的规则一致） |
| **它没有的** | 省份面板里**没有 terrain**（全仓 terrain 只用于地图模式着色与思潮成本）；省份历史只有 `Owner / Constructed / Demolished` 三种事件，**没有宗教/文化变化**——正好是我们比它多做的部分，不撞车 |
| 唯一有移植价值的独立单元 | `src/map/src/ProvinceFinder.ts`（63 行：ID 位图 → `getImageData(x,y,1,1)` → 二分查色表 → provinceId），**但仍是 AGPL 代码** |

**决策（有硬依据）**：**不移植任何代码**（AGPL 会传染整个站点），**只学信息架构与交互**；用本项目现有技术栈实现——与 §0 原始结论一致。

> 用户 2026-09-19 明确表态：**「我并不打算用这个来盈利，如果有必要我会开源的」**。所以 **AGPL 只是次要理由**；真正的理由是成本——它的面板层是 React + TanStack、地图是 WebGL2，搬进来等于重写，而它唯一有移植价值的 `ProvinceFinder`（63 行色表二分）对**我们毫无用处**：我们的数据面本身就是"每像素直接存省 id"，我们只需要"像素 → 省 id"的数组查表。

**要"学"的（已并入 §1.7 / §3.3 / §3.4）**

1. 面板是**选中态的投影**；点国旗＝"选中 + 打开"一个动作完成；
2. **点击 vs 拖拽的阈值**：它用 `|dx| + |dy| > 15` 判定平移（我们的 §1.7 采用同一个值）；
3. **层级化空值**：分组级空 → 整组不渲染；单元格级空 → 字面量 `—`（我们现有页面也是这么做的）；
4. 发展度显示**三个数**（税 / 产 / 兵）而不是一个和，并配一行说明；
5. **历史条目自带"把地图切到那一天"的按钮**（它用 `setSelectedDateText(date)`）——我们已有时间轴滑条与 `goTo()`，**这条要抄**（§3.4 第 10 条）；
6. 面板是覆盖层且 `modal=false`，面板打开时地图仍可 hover / 点击 / 平移（与我们的设计相同）；它还会用面板宽度做 `offsetX` 补偿把目标国平移到不被遮住的位置——**我们的"转到国家"只切面板、不平移镜头，所以不需要质心坐标**（将来若要平移，才需要在数据面里预处理每国质心）；
7. 坦诚说明数据局限（它有一个"存档里没记录的东西"浮层）——我们的省份/国家面板也照做：拿不到的数据写清"存档没记录"，不假装有。

**不要做的**：为了"像 pdx-tools"引入前端框架 / WASM / 运行期新依赖；抄它的任何源码或游戏派生素材（`provinces-*.webp`、`flags_*.webp`、建筑图标等，既是 AGPL 又是 Paradox 素材）；离线自包含页必须继续能 `file://` 直开。

### 0.2 本机取 GitHub 源码的可行路径（web_fetch 被挡时）

`web_fetch` 对 `github.com` / `raw.githubusercontent.com` 会报 `resolves to a non-public IP address`（沙箱 DNS），但下面几条 pwsh 命令可用：

```powershell
Invoke-RestMethod https://api.github.com/repos/<owner>/<repo> -Headers @{'User-Agent'='x'}          # 元数据 / 许可证
Invoke-WebRequest https://cdn.jsdelivr.net/gh/<owner>/<repo>@master/<path> -OutFile x               # 单文件
Invoke-WebRequest https://codeload.github.com/<owner>/<repo>/tar.gz/refs/heads/master -OutFile r.tgz # 全量（推荐，之后本地 grep）
```

（pdx-tools 的默认分支是 `master` 不是 `main`；解压副本曾被调研员放在 `$env:TEMP\pdxcache\repo\pdx-tools-master`。）

---

## 1. 冻结的契约（所有任务共同遵守）

### 1.1 现状（2026-09-19 复核，均已实测）

| 项 | 现状 |
|---|---|
| `viewer/data.json` | 10 份**全部**是浏览器 payload `{version:2,data,panels}`（任务 D 已收尾，`build-viewer.ts` 是唯一生产者） |
| 数据面 | `tmp/timeline/data.json`（离线面）与 `apps/site/public/viewer-build.js`（浏览器面）**逐键一致**，由 `apps/site/test/viewer-build.test.ts` 把关 |
| 素材 | `tmp/eu4-ui-assets/` 已有游戏 `gfx/interface` 的 7412 张 PNG（194 MB）；`apps/site/public/assets/` 目前**只有** `flags/` |
| 地图交互 | 目前只有滚轮缩放 + 左键拖动平移；**没有** hover / 点击选省 / tooltip |
| 设置面板 | 🎨 由 `apps/site/public/page-theme.js` 拥有（主题 / 背景图 / 切换间隔 / **蒙版**）；`localStorage['eu4analyser.settings']` |

### 1.2 数据面里**已经有**的（UI 直接用，不要重复造）

| 键 | 内容 | 面板用途 |
|---|---|---|
| `provinceIds` / `provinceNames` | 省 id 与名字（同下标） | 面板标题 |
| `owner` `controller` `religion` `culture` `tax/production/manpower`（客户端 `Int16Array`/`Float32Array`） | 当前帧回放值 | 归属、占领、宗教、文化、发展度 |
| `provinceInit` / `provinceEvents` | `[ord, provId, fieldIdx, valIdx]`（共 147,337 条） | **拥有者/宗教/文化变更史**（客户端按 provId 过滤＋缓存即可） |
| `battles` | `[monthKey, provId, contribution, attackerTagIdx, defenderTagIdx, losses]` | 本省战斗列表 |
| `provinceInstitutions` | `[provId, 已接纳数, 正在接纳的思潮序号]` | 思潮图标行 |
| `religions`/`religionColors`、`cultures`/`cultureColors`、`dynasties`/`dynastyColors`、`tags`/`tagColors`/`tagAlias`/`countryNames` | 字典与颜色 | 宗教/文化/王朝/国名 |
| `tagTech` | 每 tag 的 adm+dip+mil 合计 | 科技（国家面板） |
| `hre`、`colonialParent`、`curves`、`peak` | 神罗、殖民地母国、兴衰曲线 | 国家面板的"势力"小节 |
| `constants` | 海/湖/无主等颜色、`stripePeriod` 等 | 高亮配色 |

### 1.3 数据面**必须新增**的键（冻结 schema，两份实现都要产出）

> 两份实现：`scripts/render-timeline.ts`（离线）与 `apps/site/public/viewer-build.js`（浏览器）。
> **新增的每个键都必须逐键一致**，`viewer-build.test.ts` 会比对——这是本项目最容易翻车的地方。
> 数值一律用**数字下标 + 字典**（与现有风格一致）；中文字名**在构建期烤进数据面**（离线页读不到游戏目录，运行期不许再查游戏文件）。

```js
// 1) 建筑（存档当日快照；游戏把建筑写在省份块的 `buildings` 子块，现在被解析器丢掉了）
provinceBuildings: {
  dict:  string[],        // 建筑 key，例如 "marketplace"、"workshop"（排序后，保证两份面下标一致）
  names: string[],        // 与 dict 同下标的中文名（构建期从 localisation 烤入）
  rows:  number[][],      // [[provId, keyIdx, keyIdx, ...], ...]，只列有建筑的省
  builders?: string[][],  // 可选：[[provId, 建造者 tag, ...]] 与 buildings 一一对应（tooltip 用）
},

// 2) 地形（存档里**根本没有**；两份面都读 B 交付的同一份静态表，见 §3.2）
provinceTerrain: {
  dict:  string[],        // 地形 key，例如 "farmlands"、"forest"、"hills"、"mountain"
  names: string[],        // 中文名（构建期烤入）
  byId:  number[],        // 按省 id 下标，值 = dict 下标；-1 = 无（海洋/无地形）
},
// 来源必须是**提交进仓库的** apps/site/public/assets/ui/provinceTerrain.json
// （B 用游戏 map/terrain.bmp + map/terrain.txt 离线算一次）。
// 不要在渲染时现算地形：离线面与浏览器面各算一次就可能不同，viewer-build.test.ts 会挂。

// 3) 奇观（存档 `great_projects` 子块，一个省可以有多个）
provinceGreatProjects: {
  dict:  string[],        // 奇观 key，例如 "kronborg"、"falun_copper_mine"
  names: string[],
  rows:  number[][],      // [[provId, keyIdx, ...], ...]
},

// 4) 国家面板要的国家标量/列表（现成的省份数组推不出这些）
countryDetail: {
  [tag: string]: {
    government: string,      // government_name（如 russian_monarchy）→ 中文名
    governmentRank: number,  // 1..3
    governmentReforms: string[],   // 当前政体改革（government.reform_stack.reforms）
    techGroup: string,
    tech: [number, number, number],      // adm/dip/mil
    primaryCulture: string, dominantCulture: string, acceptedCultures: string[],
    religion: string,
    // 国力
    development: number, rawDevelopment: number, realmDevelopment: number,
    cities: number, provinces: number, ports: number, governCapacity: number,
    prestige: number, stability: number, legitimacy: number, corruption: number,
    mercantilism: number, armyTradition: number, navyTradition: number,
    inflation: number, professionalism: number, manpower: number, sailors: number,
    // 军事（团/艘）
    army: [number, number, number],      // 步/骑/炮
    navy: [number, number, number, number], // 重型/轻型/桨帆/运输
    // 外交
    rivals: string[], allies: string[], subjects: string[], atWar: string[],
    overlord: string, colonialParent: string,
    // 君主（来自国家 history 的 monarch 块，按 monarch.id 取当前那一个）
    monarch: { name: string, dynasty: string, adm: number, dip: number, mil: number, birthDate: string } | null,
    // 宗教/文化构成（dev 加权；也可由客户端从省份数组推，二者取一，实现方在注释里写明）
    religionDev: Record<string, number>,
    cultureDev: Record<string, number>,
  },
}
```

**约定**：
- `countryDetail` 的**键必须排序**（两份实现都按 `tags` 的顺序输出，或统一 `sort()`），否则 `JSON.stringify` 比对会假失败。
- 缺数据一律给 `0` / `''` / `[]` / `null`，**不许缺键**（缺键会让两份面不等、页面报错）。
- 客户端要用 `state` 标记区分"没有"和"是 0"，字符串统一空串。
- `countryDetail` **只烤"有省份的国家"**（约 1000–1400 个 tag），且**只放标量与短字符串**（不放逐省/逐月明细）；预计新增 < 0.6 MB。**不要把逐省明细塞进 `data.json`**。
- **先看现成入口再写解析**：`packages/eu4-parser/src/document.ts` 已经有 **`countryDetail(tag)`**（逐字 BlockView，实测 RUS 有 217 个成员）与 **`provinceDetail(id)`**；国家面板绝大多数字段（点数、国库、威望、稳定、正统、政府名与改革、科技、理念、贷款、使者、军队/海军、阶级、州…）可以直接从它们取。真正缺的是：数据面没有这些键、`province.buildings` 被丢、少数游戏文件表（area / advisor / building 名）。

### 1.4 素材路径与命名约定（冻结）

**一条铁律：客户端不做 key→文件名的映射**（离线页读不到任何索引文件）。素材由对话 B 发布到：

```
apps/site/public/assets/ui/buildings/<buildingKey>.png      ← 例 marketplace.png
apps/site/public/assets/ui/great_projects/<key>.png         ← 例 kronborg.png
apps/site/public/assets/ui/terrain/<terrainKey>.png         ← 例 farmlands.png
apps/site/public/assets/ui/religions/<religionKey>.png
apps/site/public/assets/ui/institutions/<0..7>.png          ← 八个思潮按序号
```

- 文件名 = **游戏数据里的 key**，全小写 ASCII（游戏 key 本来就是），扩展名 `.png`。
- 客户端 URL 一律**从站点根绝对**：`'/assets/ui/buildings/' + key + '.png'`（与 `assets/flags/` 同规矩；相对路径在这个项目里翻过车）。
- **缺图必须优雅退化**：`onerror` → 把 `<img>` 换成带文字的小方块（现有 `flagBox` / `heroFallback` 就是这套写法），并在元素上留 `data-key` 便于测试断言。
- 需要的子集（对话 B 负责精确清单与拷入）：建筑、奇观、地形、宗教、思潮。**其它素材不要进站点**（7412 张全量会无谓地撑大仓库）。

### 1.5 离线自包含页与部署的约束（踩过的坑）

1. **离线自包含页**（`saves/<id>/viewer/index.html`，由 `scripts/render-timeline.ts` 生成）把 `paint.js`、`page-theme.js`、`viewer.js` **内联**进 HTML：所以这三份文件里
   - **绝对不能出现反引号**（`viewer.js` 已被 `packages/eu4-parser/test/build-guards.test.ts` 静态把关）；
   - 不能出现字面量 `</script>`；
   - `page-theme.js` 必须保持"扁平脚本"：只允许 `export const/function/let/class`，不许 `import`、不许顶层 `await`（同一个 test 把关）。
2. `apps/site/public/viewer.html` 是**两个宿主共用的成品页**：不许出现 `${...}` 插值占位（测试会挂），只有 `<!-- HOST_SCRIPTS: -->` 那一块会被离线生成器替换。因此**面板的骨骼与 CSS 写在 `viewer.html` 里，两个宿主自动同时拥有**。
3. 素材**不许**出现在 `viewer/data.json` 里（base64 会把 2 MB 撑到几十 MB），只发路径；离线页靠站点根绝对路径 `/assets/...` 取图（与旗帜、背景图一致，`file://` 直开时按既有行为处理）。
4. 每份存档路径是 `/saves/<id>/viewer/*`，**三层深**：所有资源引用必须是站点根绝对，不要写 `../`。
5. 部署上限：`pnpm verify:deploy` 卡 **20000 文件**（当前 2139）。素材子集要在这个预算内（对话 B 报数字）。

### 1.6 面板的 DOM 契约（冻结 id/class，两个对话并行开发就靠这张表）

写在 `apps/site/public/viewer.html` 的 `.player` 里、`#view` **内部**（`#view` 是 `position:relative; overflow:hidden`）：

```html
<div id="view">
  <canvas id="map" width="5632" height="2048"></canvas>
  <canvas id="hover" width="5632" height="2048"></canvas>  <!-- 新增：hover 高亮层 -->
  <div id="detail" class="detail" hidden>                   <!-- 新增：详情面板 -->
    <div class="dHead">
      <button id="detailBack" type="button"></button>       <!-- 左上角：返回 / 关闭 -->
      <span id="detailTitle"></span>                        <!-- 面板标题 -->
      <button id="detailClose" type="button">✕</button>      <!-- 右上角：直接关闭 -->
    </div>
    <div id="detailBody" class="dBody"></div>               <!-- 内容由 viewer.js 填 -->
  </div>
</div>
```

- `.detail`：`position:absolute; left:0; top:0; height:100%; aspect-ratio:0.618; width:auto; overflow:hidden`（**横纵比 0.618 的竖版**，见 §5 待拍板 ①）。半透明背景由 `#detail::before` 承担（内容浮在上面，文字不受透明度影响）。
- `#hover`：与 `#map` 同尺寸、`position:absolute; left:0; top:0; width:100%; pointer-events:none`；**`applyView()` 必须把同一个 transform 同时写给两个 canvas**（这是唯一的变换出口）。
- 面板上的按钮 id 汇总：`detailBack`、`detailClose`、`detailGotoCountry`（省份面板里的「转到国家」）、`mapFull`（工具栏里的全屏按钮，放在 `.bar` 的 `.zoom` 组里）。
- 设置拉条 id：`pageThemePanelOpacity`（在 `page-theme.js` 里，**紧接 `#pageThemeDim` 那一行下面**），标签「面板」。
- `build-guards.test.ts` 第 285 行那段"markup mentions the controls"的 id 清单要**追加**新 id（由对话 U 负责）。

### 1.7 交互契约（冻结；按用户原话逐条落实）

**选中态（只有三态）**：`none` / `province(p)` / `country(t, back=q)`——`back` 是国家面板返回时要回到的省份。

| 事件 | none | province(q) | country(t, back=q) |
|---|---|---|---|
| hover 陆地省 p | 高亮该省 | 高亮该省 | 高亮该省 |
| hover 海洋 | 不高亮（**海洋不做任何处理**） | 同左 | 同左 |
| 点击陆地省 p | → province(p) | → province(p) | → country(owner(p), back=p) |
| 点击海洋 | 保持在 none | → none（直接关闭，**不经过省份面板**） | → none |
| 面板返回 | — | → none（按钮文字「✕ 关闭省份界面」） | → province(back)（按钮文字「← 返回到 - <省名>」） |
| 「转到国家」 | — | → country(owner(p), back=p) | — |
| `detailClose`（右上角 ✕） | — | → none | → none |

- **拖拽与点击消歧（必须做，否则一平移就弹面板）**：`mousedown` 记坐标；`mouseup` 时 **`|dx| + |dy| <= 15`**（借用 pdx-tools 的实测阈值）且间隔 < 500ms 才算点击；否则视为平移，不改选中态。拖动过程中不更新 hover。
- 海洋判据：该像素的 province id ∈ `DATA.waterSea` ∪ `DATA.waterLakes`，或 id ≤ 0。
- **谁拥有国**：`owner[id]`（回放值，`-1` = 无主）；占领判据与地图一致（`controller[id]` ≠ `owner[id]` 时显示占领国）。
- 面板打开时**地图照旧可拖拽缩放**；面板自身要 `stopPropagation`，不许把面板内的点击当地图点击。
- 「转到国家」取的是**当前帧**的拥有者（时间轴一动，面板内容跟着变；这是本项目的特性，不要缓存死）。

### 1.8 全屏（附加任务，冻结）

- 按钮 `#mapFull` 放在 `.bar` 的 `.zoom` 组末尾，文字「⛶ 全屏」/「⛶ 退出全屏」。
- 切换 `.player` 上的 `screen` 类：`.player.screen{position:fixed; inset:0; z-index:60; margin:0; border-radius:0; display:flex; flex-direction:column}`，`#view` 撑满剩余高度，**`.bar`（下方工具栏）必须留在屏幕底部可见**；进入时隐藏 `#nav`、`.headrow`、`.sub`、两张表、曲线图。
- ESC 与再次点击按钮都能退出；进出都要 `clampView(); applyView();`（否则地图会跑偏）。

### 1.9 国家面板：13 个 Tab 的构成与分档（2026-09-19 已逐 Tab 核实）

来源：pdx-tools 的 `CountrySideBarButton.tsx` 等 20 个组件 ＋ `models.rs`，以及对着本机存档的只读探针（`tmp/details-*-probe.*`）。**结论：一档 5 个 Tab 我们现成就能 100% 复刻，二档 7 个需新解析，三档 1 个建议只做徽章。**

> 组成顺序（照抄）：右侧抽屉 970px、可折叠全宽；Header＝国家选择器 ＋ 展开/折叠 ＋ `?`（"存档里没记录的东西"浮层）；下面是 13 个 Tab。

| # | Tab | 中文 | 档 | 里面有什么 | 我们缺什么 |
|---|---|---|---|---|---|
| 1 | General | 总览 | ① | 3 列×8 行网格（三系点数、国库、负债、通胀、威望、稳定、力量投射、创新度、腐败、政府强度、原始发展度、发展度、平均自治度、城市数、过度扩张、宗教统一度、专制主义、重商主义、辉煌点数、三系科技）＋ 使者图标行（商人/殖民者/外交官/传教士）＋ 宗教/主文化/国家 ID ＋ 卡2 军事（人力·补员·最大人力、陆海军士气、职业化、陆海传统、步骑炮佣、四类船、BEST LEADERS）＋ 卡3 君主（姓名·年龄·三维·性格·登基·文化·宗教·继承价值）＋ 卡4 理念（组名＋7 个 pip）＋ `Ongoing Wars` 区 ＋ `Diplomacy` 区（19 类关系行，空行自动消失） | 只差 `absolutism`（本存档 0 次出现 → 显示 `—` 并注"存档未记录"）、继承价值明细（公式在外部 crate，砍掉或只给粗值） |
| 2 | Rulers | 君主 | ① | 历代君主表：名/起/止/在位月数/旗/性格/ADM·DIP·MIL·合计/滑动平均/加权滑动平均；可展开"废继承人"子表 | 无 |
| 3 | Leaders | 将领 | ① | 将领表：名（兼君主时带三维）/标签（在职·海军上将·探险家·将军·征服者·君主）/激活日期/火力·冲击·机动·围城·合计 | 无 |
| 4 | Culture | 文化 | ① | 文化表：文化名（★主文化/★已接受）、组、省份数(值/%)、省份发展度(值/%)、已建州省份数/发展度、进行中的转化 | 无 |
| 5 | Religion | 宗教 | ① | 宗教表（色块＋名、省份数/%、发展度/%）、环形图、可转化目标、叛军 plurality 段 | 无 |
| 6 | History | 历史 | ② | 18 种事件的时间流（出现/吞并/改国号/迁都/文化/宗教/决议/政令/名臣/将领/君主/开战/停战/国家焦点…），每条带"把地图切到那天"＋同类过滤 | 事件映射（小）；**`readChange` 只扁平一层**，君主性格/兼将要走 `countryDetail` |
| 7 | Advisors | 顾问 | ② | "改革完成日期"一行 ＋ 名臣顾问头像网格（带获得日期） | 游戏文件 `common/advisors/*.txt` 的 id 全量表（小） |
| 8 | Budget | 财政 | ② | 三指标卡（营业利润/总支出/资本支出%）＋ 瀑布图 ＋ 明细表（上月·年初至今·去年·合计＋占比）＋ 支出树图 | **19/38 槽位枚举**（在外部 `eu4save` crate 里，本机副本查不到）＝二档最大卡点 |
| 9 | Mana | 点数 | ② | 发展花费·招将领花费卡 ＋ 三条点数量条 ＋ 三张明细饼表（点数去哪了） | **46 槽位枚举**（同上） |
| 10 | Buildings | 建筑 | ② | 全国建筑条形图（建筑名 ↔ 省份数） | `province.buildings` 被 `extractProvince` 丢掉（小修；也可走 `provinceDetail(id)`） |
| 11 | States | 州 | ② | 州表：州名（首都州★）/发展度/治理成本/集权节省/集权中/已集权/州议会/繁荣度 | 省→州映射要游戏 `map/area.txt`（48 KB，**我们没收集**）＋州名本地化；治理成本公式建议后置 |
| 12 | Estates | 阶级 | ② | 王室领地条 ＋ 每个阶级一张卡（忠诚度/领地占比/已完成议程/特权表/影响力修正表） | 深块遍历（小-中，`countryDetail` 可逐字读） |
| 13 | Institution | 思潮 | ③ | **它是"最省点数接纳思潮"的规划器**（开发成本模型＋逐步指令＋可编辑修正），不是显示器 | 复刻等于重写游戏规则；**只做"已接纳 N/8"徽章＋各省进度条**（`provinceInstitutions` 已有） |

**第一版推荐**：`General`（含战争/外交两区）＋ `Rulers` ＋ `Leaders` ＋ `Culture` ＋ `Religion` ＋ `History`——五个一档＋最便宜的一个二档，**基本不动解析器**。第二波再做 Budget/Mana/Buildings/States/Estates/Advisors。

**本地化要点（要重写的是英文硬编码，不是缺翻译）**：13 个 Tab 名、`?` 浮层全文、General 的 `Ongoing Wars`/`Diplomacy`/`BEST LEADERS:`/`Religion:`/`Primary culture:`/`Country ID:`/`Age:`/`Inaugurated:`/`Inheritance value:`/`Ideas`、所有表头（Name/Start/End/Reign (months)/Stats/Running Average…、Core Income…Net Profit、Mon. Avg.…、No estates…）、History 的 18 种事件文案（"a new ruler has ascended the throne!"、"declared war"、"Separate peace after N days"、Gained/Lost provinces…）、以及 tooltip 原文（Country ID 那句、`Morale obscured by maintenance`、`Not accurate for election governments`、`Negative values are not a bug…`）。
术语译名（与在用汉化 mod 对齐）：总览/历史/思潮/顾问/君主/将领/财政/点数/建筑/宗教/文化/州/阶级；政府强度、虔诚、贤能、部落统一、行省、核心行省、分封、朝贡国、世袭普洛尼亚、普洛尼亚、联姻属国、联合统治、过度扩张、职业化、陆军·海军传统、重商主义、辉煌点数、专制主义、创新度、正统性、共和传统、力量投射、宗教统一度、平均自治度、战争疲劳、集权中、繁荣度、王室领地、忠诚度、领地占比、特权、影响力修正、已完成议程、名臣顾问。数字一律显式 `zh-CN`。

**架构提醒**：面板按"国家"聚合即可，**不要把逐月/逐省明细塞进 `data.json`**；`countryDetail` 只烤有省份的国家（约 1000–1400 个）的标量与短字符串。

---

## 2. 阶段与并行计划

| 阶段 | 任务 | 对话 | 依赖 | 可并行 |
|---|---|---|---|---|
| **一** | A 数据面扩展（省份＋国家） | **新对话 A** | 无 | ✅ 与 B、C、U(阶段一) |
| **一** | B 素材管线（建筑/奇观/地形/宗教/思潮） | **新对话 B** | 无 | ✅ 与 A、C、U |
| **一** | C 设计预览与信息架构（给用户拍板） | **新对话 C** | 无 | ✅ 与 A、B、U |
| **一** | U 面板外壳 · 交互 · 全屏（不含内容栏目） | **新对话 U** | 只依赖 §1.6/1.7/1.8 契约 | ✅ 与 A、B、C |
| **二** | U 续：面板内容栏目（省份＋国家） | **对话 U 继续** | ← C 的用户拍板；A 的新键（没到就先用占位数据） | 与 A、B 尾段 |
| **三** | R 重建 10 份存档与离线页 | **新对话 R** | ← A、B、U 全部完成 | ❌ 必须最后 |
| **四** | 收尾（联调＋三闸门＋更新交接） | **主对话** | ← R | ❌ |

**为什么 U 拆成两段**：面板的**骨骼与交互**（§1.6/1.7/1.8）是用户已经定死的，可以立刻开工；面板的**内容栏目**取决于用户在 C 的预览稿上勾选的清单，所以第二段等拍板。两段在同一个对话里做，避免 `viewer.js` / `viewer.html` 被两个对话同时改。

**文件所有权（严格互斥，越界即冲突）**

| 对话 | 只能改 | 绝对不许改 |
|---|---|---|
| A | `packages/eu4-parser/src/**`、`packages/eu4-parser/test/**`、`apps/site/public/viewer-build.js`、`apps/site/public/viewer-data.js`、`apps/site/public/viewer-store.js`（只许加可选入参）、`apps/site/public/viewer-page.js`（只许加载入参）、`scripts/render-timeline.ts`、`apps/site/test/viewer-*.test.ts`；重建 `tmp/timeline/*` | `viewer.js`、`viewer.html`、`style.css`、`page-theme.js`、`verify-player-run.ts`、`assets/**`、`交接.md`；`loadStoredSave()`/`DATA_PATH` 的判据 |
| B | 新增 `scripts/export-ui-assets.ts`、`apps/site/public/assets/ui/**`、`scripts/verify-deploy.ts` | 其余一切（`tmp/eu4-ui-assets/**` 只读） |
| C | `tmp/panel-preview/**` | 其余一切（不改任何站点文件） |
| U | `apps/site/public/viewer.html`、`viewer.js`、`style.css`、`page-theme.js`、`scripts/verify-player-run.ts`、`apps/site/test/frontend.test.ts`、`packages/eu4-parser/test/build-guards.test.ts` | A/B 名下的文件 |
| R | `.dev-storage/**`（只重建、绝不删除） | 一切源代码 |

---

## 3. 任务

### 3.1 任务 A（新对话 A）：数据面扩展——省份＋国家

**目标**：把 §1.3 的四个新键做进**两份**数据面，并让它们逐键一致。

**要求**

1. **先探针，后动手**：`tmp/province-block-probe.ts`、`tmp/panel-shape-probe.ts`（主对话留下的只读探针）已经证实：
   - 省份块的 `buildings = { marketplace=yes … }`、`building_builders = { marketplace=SWE … }`、`great_projects = { null="kronborg" }` 都是**子块**，而 `packages/eu4-parser/src/document.ts` 的 `extractProvince()` 目前**只认 `cores` 和 `institutions`，其它子块一律 `continue`** —— 这就是建筑/奇观拿不到的根因；
   - 存档里**根本没有 `terrain`**（全文 0 次匹配）→ 地形**不在解析层做**：A 只负责把 B 交付的静态表 `apps/site/public/assets/ui/provinceTerrain.json`（省 id → 地形 key）**读取并并入数据面**（见下面第 4 条）；
   - `great_projects` 的值是 **`null=` 裸列表**（`{ null="kronborg" }`），不要在解析器里按 key 取值。
2. 解析层：给 `ProvinceRecord` 增加 `buildings` / `buildingBuilders` / `greatProjects` / `claims` 四个字段（`cores` 已有；**不改**已有字段语义），在 `extractProvince()` 里补上这四个子块——`claims` 与建筑同一次修复、几乎免费，**但要不要进数据面等预览稿拍板**（用户勾了「核心/宣称」再加 `provinceCores` / `provinceClaims` 两个键）。加单测（`packages/eu4-parser/test/`）。
3. 国家标量：按 §1.3 的 `countryDetail` 清单，从 `countries/{TAG}` 块里取（`technology{}`、`government{}`、`prestige`、`stability`、`legitimacy`、`corruption`、`mercantilism`、`army_tradition`、`navy_tradition`、`inflation`、`army_professionalism`、`manpower`、`sailors`、`num_of_*`、`rival`、`enemy`、`subjects`、`power_projection`、`is_at_war`/`at_war`…），君主取 `countries/{TAG}/monarch={id,type}` 指向的 **`history` 里的 monarch 块**（含 name/dynasty/ADM/DIP/MIL/birth_date）。缺的字段给默认值，**不许缺键**。
4. 名字与静态表：建筑名、奇观名、地形名、政体名**在构建期烤进数据面**（离线页读不到游戏目录）。两份实现都必须读仓库里**同一份**文件（不许一份读游戏 localisation、一份读 JSON）：
   - `apps/site/public/assets/ui/uiNames.json`（key→中文名，B 交付，格式见 §3.2 第 3 条）——离线面 `readFileSync`，浏览器面 `fetch` 后作为入参传进 `buildViewerData()`；
   - `apps/site/public/assets/ui/provinceTerrain.json`（省 id→地形 key，B 交付）。
   为了把这两份表喂给浏览器面，A **只允许对 `viewer-store.js`（`buildForSave` 的入参）与 `viewer-page.js`（加载入参）做"加一个可选入参"的最小改动**：`loadStoredSave` / `DATA_PATH` 的判据**一个字都不许动**（那是任务 D 刚立下的规矩）。
5. 两份实现 + 参照：改完跑 `pnpm timeline` 重新生成 `tmp/timeline/data.json`（`viewer-build.test.ts` 的参照），再跑 `pnpm test`，确认 `viewer-build.test.ts` 里"两份数据面逐键一致"的新键也过了（**要给新键补断言**，不能只靠旧的 38 键）。
6. 数据量预算：`provinceEvents` 已经 147k 条；新增的四个键**不要**把 `tmp/timeline/data.json`（现约 1.9 MB）撑爆——`provinceBuildings` 用省→建筑下标，不要存字符串数组。

**验收**：`pnpm test` 全绿（条数以实跑为准）；`tmp/timeline/data.json` 里四个新键存在、`countryDetail` 的 `RUS` 有合理值；贴出"两份数据面一致"的那条测试输出；贴出 `provinceBuildings` 只数/`provinceTerrain` 覆盖省数/`countryDetail` 覆盖国家数。

**可直接粘贴的提示词 → 新对话 A**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第三对话任务书.md 的「1.2、1.3、1.5、3.1 节」——
你是「任务 A：省份/国家详情的数据面扩展」的执行者。

要点：
- 只改 packages/eu4-parser/src/**、packages/eu4-parser/test/**、apps/site/public/viewer-build.js、
  apps/site/public/viewer-data.js、scripts/render-timeline.ts、apps/site/test/viewer-*.test.ts；
  别动 viewer.js / viewer.html / style.css / page-theme.js / verify-player-run.ts / assets/** / 交接.md；
- 根因已知：document.ts 的 extractProvince() 只认 cores 与 institutions，其它子块（buildings /
  building_builders / great_projects）被丢掉了；存档里没有 terrain（走 assets/ui/uiNames.json 里的地形表）；
  great_projects 是 null= 裸列表；
- 按任务书 1.3 冻结的四个键（provinceBuildings / provinceTerrain / provinceGreatProjects / countryDetail）做，
  两份数据面（render-timeline.ts 与 viewer-build.js）都要产出、逐键一致、不许缺键、countryDetail 的 tag 键要排序；
- 中文字名构建期烤入，统一读 apps/site/public/assets/ui/uiNames.json（另一个对话 B 在产出，A 先写读取逻辑）；
- 改完跑 pnpm timeline 重新生成 tmp/timeline/data.json，再跑 pnpm test；给新键补 viewer-build.test.ts 的断言；
- 先跑 tmp/province-block-probe.ts 与 tmp/panel-shape-probe.ts 看现有数据形状，别自己发明字段名；
- 汇报先给结论（四个键的实现情况、覆盖数、测试结果），不要叙述调试过程。
```

### 3.2 任务 B（新对话 B）：素材管线（图标进站点 + 中文名索引）

**目标**：把省份/国家面板要用的图标按 §1.4 的命名约定发布进站点，并交付一份 `uiNames.json`。

**现状（已实测）**：`tmp/eu4-ui-assets/`（另一个对话的成果，**只读**）：`all/buildings` 364 张、`all/great_projects` 149 张、`religion_icons` 28 张、`institutions` 8 张 64×64、`all/government_reform_icons` 346 张、`all/ideas*` 953 张；`_sprite定义/` 是游戏 `interface/*.gfx`；`manifest.json` 是文件清单。`apps/site/public/assets/` 目前只有 `flags/`。

**要求**

1. **建筑 / 奇观 / 宗教 / 思潮**：从 `tmp/eu4-ui-assets/` 里挑出正确的那一张，按 `assets/ui/<kind>/<游戏key>.png` 落位。**文件名必须等于游戏 key**（不一致就在脚本里改名/复制，别让客户端做映射）。素材调研已把映射规则查清（下表是结论，照做即可）：

   **① 建筑（47 个 key，47 张图 / 0.21 MB）**：key 来自 `common/buildings/00_buildings.txt` + `01_nativebuildings.txt`；**文件名 ≠ key**，必须查 `interface/building_icons.gfx` 的 `spriteType name = "GFX_<key>"` → `texturefile`。47/47 全部可解析（示例：`marketplace`→`latin_marketplace.png`、`temple`→`latin_temple.png`、`university`→`manufactory_university.png`、`state_house`→`latin_royal_palace.png`、`fort_15th`→`latin_fort01.png`、`ramparts`→`latin_march.png`、`tradecompany`→`manufactory_trade_company.png`）。**注意**：`costal_defence.png`（1058×647，游戏原名拼错）与 48×48 的 `coastal_defence.png` 是两张不同文件，别拿错。

   **② 奇观（139 个 key → 134 张图 / 9.93 MB，是体积大头）**：key 来自 `common/great_projects/*.txt`，**必须查 `interface/great_project.gfx` 的 `GFX_great_project_<key>`**——139 个里有 **17 个文件名 ≠ key**：`stonehenge`→`great_project_stone_henge.png`、`moai`→`great_project_easter_island_statues.png`、`kremlin`→`great_project_the_kremlin.png`、`kiel_canal` **与** `panama_canal`→同一张 `great_project_suez_canal.png`、`notre_dame_cathedral`→`great_project_notre_dame.png`、`edinburgh_castle`→`great_project_Edinburgh.png`、`stpeters_cathedral`→`great_project_saint_peters_cathedral.png`、`st_basil_cathedral`→`great_project_StBasil_cathedral.png`、`imperial_city_kyoto`→`great_project_Kyoto.png`、`palace_of_the_popes`→`great_project_PopePalace.png`、`ulm_minster_great_project`→`great_project_ulm_minster.png`、`divrigi_hospital_mosque`→`great_project_divrigi.png`、`grand_canal_1..4`→同一张 `great_project_grand_canal.png`。大写那几张在落位时**必须改成小写 key 文件名**（部署到 Linux 会大小写敏感）。若体积要削，只有这里可削（转 WebP 或降到 150×75 约 2.5 MB），**动之前先问主对话**。

   **③ 宗教（28 张 64×64 / 0.13 MB，100% 覆盖）**：`religion_icons/` 的 28 张在 1.37 里其实是**死文件**（全游戏 `*.gfx` 与 `eu4.exe` 里都搜不到），但**恰好是最好用的一套**——28 个宗教一个不缺、名字可核对。不要用真正在用的 29 帧条带（`icon_religion.png` 1856×64）：**帧序没有任何随包文件声明**，本机无法证实。**必须做 11 条别名**（图标文件名 → 当前 religion key）：`theravada`→`buddhism`、`fetishist`→`shamanism`、`alcheringa`→`dreamtime`、`mayan`→`mesoamerican_religion`、`norse`→`norse_pagan_reformed`、`tengri`→`tengri_pagan_reformed`、`shia`→`shiite`、`sikh`→`sikhism`、`hindu`→`hinduism`、`confucian`→`confucianism`、`animist`→`animism`（其余 17 条同名）。别名在**构建期脚本里**做复制改名，别落到运行期。mod 宗教没有图 → 客户端退化成只显示名字。

   **④ 思潮（8 张 48×48 / 40 KB）**：`institutions/` 里按 `countrytechnologyview.gfx` 的声明顺序 = 序号 0–7：`feudalism`、`renaissance`、`new_world_i`（＝colonialism）、`printing_press`、`global_trade`、`manufactories`、`enlightenment`、`institutions_industrialization`。落位成 `assets/ui/institutions/0.png … 7.png`（顺序以 `apps/site/public/viewer-build.js` 的 `INSTITUTIONS` 常量为准）。**别用** `tech_institution_*.png`（200×40 带英文文字）。

   **⑤ 顺手一起落的（国家面板要用）**：`government_reform_icons/`（346 张，key 来源是 `common/government_reforms/*.txt` 里显式的 `icon = "crown"` 字段，323/324 可用，只有 `reform_soldiers` 本机缺图）、`ideas/` 理念组图标（30 张，key＝`idea_<group>_ideas`）、`ideas_EU4/` 修正小图标（923 张，文件名即修正 key）、科技三系 `icon_powers_{administrative,diplomatic,military}_tech.png`、`icon_stability/prestige/legitimacy.png`、属国与盟友 `icon_vassal` / `subject_{colony,tributary,tradecompany}_icon` 等。

   **⑥ 不要**：`mapmode_*.png`（82×31 按钮整条、带英文文字）、`_sheets/`、`_preview.png`。
2. **地形**（另一条链路；**本机已验证可行**，见 §3.2 末尾的探针结论）。存档里**没有** terrain；地形由游戏 `map/terrain.bmp`（8bpp 调色板索引图）+ `map/terrain.txt` 决定。**一份素材调研曾报"本机做不出来"，那是误判**（它把 `provinces.bmp` 解析错了；我们用项目自己的解码器实测 `unmatched pixels 0 (0.00%)`）。正确的算法（**不需要 `positions.txt`，也不需要 Steam 校验文件**）：
   1. 用项目已有的 `scripts/lib/map-assets.ts` 的 `loadProvincePixels(W,H,colorToIdMap(loadDefinitions()))` 拿到**每像素的省 id**（地图管线本来就在用，实测 0% 未匹配）；
   2. 解析 `map/terrain.bmp`：8bpp、`dataOffset` 在 BMP 头偏移 10、调色板在 `14 + biSize`、行 stride 按 4 字节对齐、自下而上——拿到**每像素的调色板索引**；
   3. 把 `map/terrain.txt` 的 `terrain = {}` 块解析成 **索引 → 类别**（语法 `名字 = { type = <类别> color = { <索引> } }`，实测 27 条）；
   4. 逐省对像素做**类别多数票**（实测 3925 个省有票）；
   5. **再套 `categories.<类别>.terrain_override = { 省 id… }`**（实测 17 个类别、2336 个省，优先级最高）。
   实测结果：最终分布覆盖 18 个类别（grasslands 718 / ocean 451 / mountain 361 / hills 318 / woods 307 / farmlands 228 / savannah 206 / highlands 194 / desert 185 / drylands 182 / steppe 177 / coastline 156 / jungle 142 / inland_ocean 116 / coastal_desert 73 / marsh 66 / forest 38 / glacier 7），海洋省 0 个被误判成陆地；woods/jungle/savannah/drylands/highlands/glacier 这些在 `terrain.bmp` 里像素数为 0 的类别**正是靠 override 出来的**。探针：`tmp/terrain-derive-probe.ts`（只读，可复核）。
   请交付 **`apps/site/public/assets/ui/provinceTerrain.json`**（格式冻结，对话 A 直接读它）：
   ```json
   { "byId": { "1": "farmlands", "2": "forest" }, "dict": ["farmlands", "forest"] }
   ```
   `byId` 的键是省 id（字符串），值是**类别名**（`terrain = {}` 里的 `type`，即 18 个类别之一）；海洋/无地形的省**不要写键**。**不要在站点运行期现算地形**：这份 JSON 是唯一来源，离线面与浏览器面都读它。
   - 地形**配图**：省份窗口用的就是 `gfx/interface/combat_terrain_*.dds`（21 张，`interface/provinceview.gui` 里 `province_terrain` 指向 `GFX_combat_terrain_*`），**别用** `colony_terrain_*`（殖民面板那套）。转 PNG 落 `assets/ui/terrain/<类别名>.png`，**17 个类别有图 / ≈1.0 MB**。三条要按 `.gfx` 解析的别名：`glacier`→`combat_terrain_arctic.png`、`coastline`→`combat_terrain_inland_ocean.png`、`mountain` 与 `desert_mountain` 共用 `combat_terrain_mountains.png`；`inland_ocean` / `pti` / `impassable_mountains` **没有 sprite** → 客户端退化成只显示名字。
3. **`apps/site/public/assets/ui/uiNames.json`**（A 的输入，格式冻结）：
   ```json
   {
     "buildings":      { "marketplace": "市场", "workshop": "工场", "...": "..." },
     "greatProjects":  { "kronborg": "克伦堡", "...": "..." },
     "terrain":        { "farmlands": "农田", "forest": "森林", "...": "..." },
     "religions":      { "catholic": "天主教", "...": "..." },
     "governmentReforms": { "tsardom": "沙皇制", "...": "..." },
     "technologies":   { "...": "..." }
   }
   ```
   key 一律小写 ASCII、与游戏数据一致；**没有中文名的不要编**（缺的省略，客户端回退到 key）。
4. **不许进站点的东西**：测试/联系表（`_sheets/`、`_preview.png`）、`mapmode_*.png`（带英文文字的按钮整条）、`all/` 里的其它分类。站点体积与文件数预算见 §1.5。
5. `scripts/verify-deploy.ts`：把 `assets/ui` 加进 `REQUIRED_DIRS`，并加一条"每个 UI 子类目录非空"的检查；确保文件数仍在 20000 以内（报数字）。
6. 交付一份**覆盖报告**：每个子类有多少 key、有多少拿到了图标、哪些 key 没有图标（列出）、总文件数与总 MB。

**验收**：`pnpm verify:deploy` 全绿并报出文件数/体积；`assets/ui/**` 就位；`uiNames.json` 就位；覆盖报告给出清单；**每个 key 抽查 3 个**：直接打开 PNG 能看到图、文件名与游戏 key 一致。

**可直接粘贴的提示词 → 新对话 B**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第三对话任务书.md 的「1.4、1.5、3.2 节」——
你是「任务 B：省份/国家面板的素材管线」的执行者。素材原始件在 tmp/eu4-ui-assets/（只读，别改）。

要点：
- 只新增/修改 scripts/export-ui-assets.ts、apps/site/public/assets/ui/**、scripts/verify-deploy.ts；
  别动其它任何文件（尤其别改 apps/site/public/viewer*.js / viewer.html / page-theme.js）；
- 按任务书 1.4 的命名约定落位：assets/ui/buildings/<游戏key>.png、great_projects/、terrain/、religions/、
  institutions/；文件名必须等于游戏 key，客户端不做任何映射；
- 地形单独一条链路：用 map/terrain.bmp + map/terrain.txt 产出 province id → 地形 key 的表，并挑选/转换
  地形配图（gfx/interface 里的 colony_terrain_*/combat_terrain_*）；地形表的位置与格式先跟主对话确认；
- 交付 apps/site/public/assets/ui/uiNames.json（格式见任务书 3.2 第 3 条），key 小写 ASCII，缺中文名就省略；
- 别把测试图/联系表/mapmode 按钮条放进站点；verify-deploy.ts 加 assets/ui 的必需目录与检查，文件数必须 <20000；
- 汇报先给结论（各子类覆盖数、缺失清单、文件数与体积、verify:deploy 结果），不要叙述调试过程。
```

### 3.3 任务 C（新对话 C）：设计预览与信息架构（先给用户拍板）

**目标**：在做真面板之前，让用户**看着假数据**把"面板长什么样、国家面板有哪些栏目"定下来（用户明确说"下面去做一些栏目，但是我没有想好，你来提提意见吧"）。

**要求**

1. 产出 `tmp/panel-preview/index.html`（**单文件、双击即开**，不依赖服务器、不依赖站点其它文件；图片可以引用 `/assets/ui/...` 也可以先用占位方块）。
2. 用**假数据**渲染两种面板的完整布局，位置与尺寸照 §1.6（贴地图最左、竖版 0.618、半透明、四套主题切换）。
3. **横纵比必须给两种读法**（见 §5 待拍板 ①）：A 版＝`aspect-ratio:0.618`（竖版，宽=高×0.618）；B 版＝面板占地图宽度的 61.8%。做成一个开关。
4. **省份面板**内容按用户点名 + 主对话补充：省名 / 发展度（税·产·兵）/ 归属国（旗）/ 占领国 / 地形（名＋图）/ 宗教（名＋图标）/ 文化（"文化：具体文化"）/ 建筑网格（**一行 4 个、满了换行、每张配图注**）/ 奇观（同建筑）/ 思潮图标行 / 战斗记录 / 拥有者变更史 / 宗教·文化变更史 / 「转到国家」按钮。
   **另设一行「可选补充」供勾选**（信息架构借鉴 pdx-tools）：省份 id / 核心（哪些国家有核心）/ 宣称 / 荒废度 / 贸易品 / 是否贸易公司。
5. **国家面板**栏目清单＝**§1.9 的 13 个 Tab**（已逐一核实，含分档与第一版推荐）；做成**可勾选**（每个 Tab 一个复选框，默认只勾第一版推荐的六个，并把"数据来自哪里／有没有数据"写清）。下面 ①~⑩ 是主对话最初的提案，**已被 §1.9 取代，仅留作对照**：
   - ① 概览：旗/国家娘（1:1 方框）、国名、政体＋政府等级、国教＋宗教图标、技术组、总发展度
   - ② 国力：发展度（自有/原始/实际）、省份数·城市数·港口数、威望、正统、稳定、腐败、重商主义、陆军/海军传统、通胀、职业度、人力、水手
   - ③ 科技与思潮：adm/dip/mil 三系（相对着色）、已接纳思潮图标
   - ④ 君主与宫廷：当前君主（名/王朝/ADM-DIP-MIL/出生日期）、政体改革列表
   - ⑤ 宗教与文化构成：国教＋各省宗教按发展度占比、主文化/主导文化/接纳文化＋各省文化占比
   - ⑥ 军事：陆军团数（步/骑/炮）、海军舰船（重/轻/桨/运）、人力/水手
   - ⑦ 外交：盟友、宿敌、附属国（含类型）、宗主/母国、正在进行的战争
   - ⑧ 建筑与奇观：全国建筑统计（图标＋数量）、拥有的奇观列表（点击可跳到该省）
   - ⑨ 历史：本国发展度曲线（复用兴衰曲线数据）、改名史（MOS→RUS）、结档/封档日期
   - ⑩ 省份 Top 10：发展度最高的十省（点击进入该省面板）
6. 交互也做成**可点**的（不用真数据）：hover 高亮假省块、点击切面板、返回、转到国家、海洋点击关闭——让用户提前体验 §1.7 的状态机。另外把「**历史条目点一下 → 时间轴跳到那一天**」也做成可点演示（pdx-tools 有这个交互，我们已有滑条与 `goTo()`，值得抄）。
7. 交付一份 `tmp/panel-preview/CHECKLIST.md`：把上面的栏目逐条列出，留"要 / 不要 / 改名 / 换位置"的空格，用户可以直接在上面批注。
8. **不要**改任何站点源文件；这个任务的产物只有 `tmp/panel-preview/**`。

**验收**：用户能双击打开、切主题、点交互、勾栏目，并在 CHECKLIST 上给出结论。

**可直接粘贴的提示词 → 新对话 C**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md 与 第三对话任务书.md 的「1.6、1.7、3.3 节」以及用户原话（第 0 节）——
你是「任务 C：省份/国家详情面板的设计预览稿」的执行者，目标是让用户在不写真实功能的前提下拍板布局与栏目。

要点：
- 只产出 tmp/panel-preview/**（单文件 HTML + CHECKLIST.md），不许改任何站点源文件；
- 照任务书 1.6 的 DOM/尺寸（贴地图最左、竖版 0.618、半透明、四套主题）与 1.7 的交互状态机（hover 高亮／点击选省／
  转到国家／返回／点海洋关闭）做可点的假数据预览；横纵比给两种读法（任务书 5.①）；
- 省份面板栏目见任务书 3.3 第 4 条；国家面板栏目用第 5 条的 ①~⑩ 做成可勾选，并标注每个栏目的数据来源与有无数据；
- 交付 CHECKLIST.md，把栏目列成可批注的清单（要/不要/改名/换位置）；
- 汇报先给结论（预览页路径、两种横纵比怎么切、栏目清单有几项、哪些栏目你判断缺数据），不要叙述调试过程。
```

### 3.4 任务 U（新对话 U）：面板外壳 · 交互 · 内容 · 全屏

**分两段做，同一个对话**：阶段一（外壳与交互）现在就能开工；阶段二（内容栏目）等用户在 C 的预览稿上拍板。

**阶段一（不等任何人）**

1. `viewer.html`：按 §1.6 加入 `#hover`、`#detail` 骨骼与全部 CSS（四套主题都要好看：面板背景走 `--panel`，边框走 `--border`，标题走 `--ink`/`--accent`）；给 `.detail` 的半透明背景单独一层（`#detail::before` + `--panel-opacity`），保证**文字不被透明度影响**。
2. `page-theme.js`：在「蒙版」拉条**下面**加一行「面板」（`#pageThemePanelOpacity`），写 `settings.panelOpacity`（默认 88，范围 20–100），并把它落到 `document.documentElement.style.setProperty('--panel-opacity', …)`；**只有查看页传 `mountPageTheme({ wallpapers: BG, detailPanel: true })` 时才渲染这一行**（目录页不显示）。保持文件"扁平脚本"（见 §1.5）。
3. `viewer.js`（**不许出现反引号**）：
   - `#hover` 的画布与 `applyView()` 联动；hover 命中的省份用缓存的方式画高亮（Map 缓存，上限 8），海洋不清不画；
   - 坐标换算：客户端坐标 → `#view` 内坐标 → 逆变换（translate/scale）→ raster 像素 → `ids` 取 province id；
   - 点击/拖拽消歧（§1.7）；选中态状态机（§1.7 的表，逐行实现）；
   - 面板开关与内容区滚动、按钮文案（「✕ 关闭省份界面」/「← 返回到 - <省名>」）、`stopPropagation`；
   - 全屏（§1.8）。
4. `verify-player-run.ts`：DOM 桩要支持新元素（`hover`/`detail`/`detailBody`/`detailBack`/`detailClose`/`detailGotoCountry`/`mapFull`/`pageThemePanelOpacity`）、`getBoundingClientRect`、坐标事件派发。**新增第 14 节**断言：
   - hover 陆地省 → `#hover` 画了高亮，海洋 → 没画；
   - 点击陆地省 → 面板打开且标题＝该省名；点击海洋 → 面板关闭；
   - 省份面板「转到国家」→ 国家面板打开、返回按钮文案含该省名；点另一个省 → 国家面板换国、返回目标换成新省；
   - 拖拽（位移 40px）不改变选中态；
   - 四套主题下 `--panel-opacity` 生效；全屏切换后 `.bar` 仍在、`#view` 变高（DOM 桩里断言 class 与属性即可）。
5. `frontend.test.ts` 与 `build-guards.test.ts`：把新 id 加进"markup mentions the controls"清单；补一条"`#detail` 在 `#view` 内、`#hover` 与 `#map` 同尺寸"的静态断言。

**阶段二（C 拍板后）**

6. 按用户勾选后的清单填 `#detailBody`：省份面板照 §3.2 的构成（12 行 KV ＋ 4 张分组表，建筑网格一行 4 个、缺图退化成文字块）；国家面板**照 §1.9 的 13 个 Tab**（第一版＝总览／君主／将领／文化／宗教／历史）。数据全部来自 A 的新键＋§1.2 的现成键；**A 还没落地时先用占位数据**，不要自己造数据面。
7. 历史小节（拥有者/宗教/文化变更）在客户端过滤 `DATA.provinceEvents`（按 provId 过滤＋缓存）；战斗列表过滤 `DATA.battles`。
8. 图注/名字来自 `DATA.provinceBuildings.names` 等；图标走 `/assets/ui/...`；**缺图退化**（`onerror` → 文字块 + `data-key`）。
9. **空值分两层**（借用 pdx-tools）：分组级空 → 整组不渲染；单元格级空 → 字面量 `—`。拿不到的数据写清"存档没记录"，不假装有（面板里放一行小字说明数据局限）。
10. **历史条目带"把地图切到那天"**：每条拥有者/宗教/文化变更后面放一个小图标，点它把时间轴跳到该日期（`DATA.months.indexOf(ordinal)` + 既有的 `goTo()`），再点一次回到当前帧。这是"时间线驱动地图"，本项目地图本来就是时间轴驱动的，天然契合。
11. 若用户在预览稿上勾了「核心 / 宣称」，向对话 A 要 `provinceCores` / `provinceClaims` 两个键（A 在同一个子块修复里已经把它们解析出来），**不要自己从别处凑**。

**验收**：`pnpm test`、`pnpm verify:player` 全绿（真跑输出为准）；本地 `http://127.0.0.1:8788/viewer.html?id=990e9f3381e8` 目视：hover 高亮（海洋无反应）、点击开面板、转到国家/返回/海洋关闭、全屏保留工具栏、面板拉条在 🎨 里生效且四主题正常；**离线自包含页同样可用**（生成一份 `tmp/preview/` 页面目视）。

**可直接粘贴的提示词 → 新对话 U**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md 与 第三对话任务书.md 的「0、1.5、1.6、1.7、1.8、3.4 节」——
你是「任务 U：省份/国家详情面板」的执行者（外壳·交互·内容·全屏）。

要点：
- 只改 apps/site/public/viewer.html、viewer.js、style.css、page-theme.js、scripts/verify-player-run.ts、
  apps/site/test/frontend.test.ts、packages/eu4-parser/test/build-guards.test.ts；别动 viewer-build.js /
  viewer-data.js / render-timeline.ts / assets/** / 交接.md（数据面是另一个对话 A 的）；
- 第一段先做「外壳＋交互＋全屏」：DOM 与 CSS 照任务书 1.6（#hover、#detail、#detailBack/#detailTitle/
  #detailClose/#detailBody/#detailGotoCountry/#mapFull/#pageThemePanelOpacity），交互照 1.7 的状态机逐行实现
  （hover 高亮只对陆地、点击海洋直接关闭、拖拽位移>4px 不算点击、返回按钮文案、转到国家、当前帧的拥有者）；
  全屏照 1.8（.player.screen，保留 .bar）；page-theme.js 的「面板」拉条加在蒙版下面，只在 detailPanel:true 时渲染；
- viewer.js 里**绝对不能出现反引号**（build-guards 静态把关），page-theme.js 必须保持扁平脚本，viewer.html 不许有 ${} 插值；
- 第二段等用户在设计预览稿上拍板后再做内容栏目；数据键见任务书 1.3（A 在产出），没到就先用占位数据；
- 验收：pnpm test 与 pnpm verify:player 全绿（条数以实跑为准），并给出一份离线自包含页的目视结果；
- 汇报先给结论（做了哪些交互、断言在哪一节、目视结果、还差什么），不要叙述调试过程。
```

### 3.5 任务 R（新对话 R）：重建 10 份存档与离线页

**目标**：A（数据面）与 U（前端）改完后，把新代码**烤进**每份存档的产物：`viewer/data.json`（payload）、`viewer/raster.png`、`viewer/thumb.png`、离线自包含页 `viewer/index.html`。

**要求**：用任务 D 留下的现成入口（`scripts/rebuild-stored-data.ts` + 双击 `修复存档数据.bat`），或逐份
`node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage`。
**只重建、绝不删除**；跑之前备份 `index.json` 与 10 份 `meta.json`。抽查每份：离线页里能搜到新面板的类名、`data.json` 里四个新键存在、缩略图仍在。

**验收**：10/10 重建成功；抽查通过；`node tmp/data-format-audit.js` 仍 10/10 PAYLOAD；`pnpm verify:deploy` 全绿。

### 3.6 收尾（主对话，不派活）

R 报完后由**第三个主对话**做：目视（两种面板、四主题、全屏、hover、互相跳转、离线页）→ `pnpm test` + `pnpm verify:player` + `pnpm verify:deploy` → 更新 `交接.md`（功能表加两行、清掉本条）→ 把 `第三对话任务书.md` 标成已完成/归档 → 更新记忆。

---

## 4. 全局纪律

1. **不许碰 `.dev-storage/`**（任务 R 除外，且只重建、绝不删除）；测试用 `tmp/` 或 dev-server 的 `--root` 指向临时目录。
2. **不许改 `交接.md`、本任务书、以及别的对话名下的文件**；需要改就写进回报，由主对话协调。
3. **中文路径/文本**用 `[IO.File]::ReadAllText/WriteAllText` + `UTF8Encoding($false)`，不要 `Get-Content/Set-Content`。
4. **`.bat` 约定**（需要才做）：CRLF、控制台只输出 ASCII、绝不 `chcp`、中文写文件；新 .bat 要加进 `apps/site/test/frontend.test.ts` 的 `launchers` 列表。
5. **测试条数以 `pnpm test` 实跑输出为准**（文档里的数字都可能过时）。跑测试需要 `danger-full-access`；遇到 `spawn EPERM` 让用户放宽沙箱，别改代码绕过。
6. **汇报口径**：先给结论（做完什么 + 验证结果），再列改了哪些文件、怎么验证的、还差什么；不要叙述调试过程。
7. **先看再改**：动任何数据面前先跑 `tmp/province-block-probe.ts` / `tmp/panel-shape-probe.ts` / `tmp/data-format-audit.js`，别凭猜测写字段名。

---

## 5. 需要用户拍板的点（主对话汇总，不阻塞阶段一）

| # | 问题 | 主对话的建议 | 谁在等 |
|---|---|---|---|
| ① | 「面板横纵比 0.618」是哪种读法？ | 默认按**竖版**（宽 = 高 × 0.618）实现，预览稿里给两种读法供切换 | 对话 C 的预览、对话 U 的 CSS |
| ② | 国家面板第一版做哪几个 Tab？ | 按 §1.9 的推荐：**总览／君主／将领／文化／宗教／历史**（5 个一档 ＋ 最便宜的 1 个二档），其余 6 个二档 Tab 放第二波；**思潮只做"已接纳 N/8"徽章**，不做它的开发规划器 | 对话 U 第二段 |
| ③ | 面板在地图上时，地图要不要让位 | **覆盖**（面板浮在地图左缘，地图不被挤压），因为地图已有缩放平移 | 对话 U 第一段 |
| ④ | 省份面板的返回按钮旁边那句 | 按用户原话：「✕ 关闭省份界面」；右上角另给一个 ✕ | 对话 U 第一段 |
| ⑤ | 面板默认透明度 | 88%（与现有 `--panel` 的 .88 一致），拉条范围 20–100 | 对话 U 第一段 |
