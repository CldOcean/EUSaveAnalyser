# EUSaveAnalyser · 第四对话任务书（人物特质汉化 · 全列表点表头排序 · 两张排行榜）

> 更新：2026-09-19（**主对话写，只写本轮要做的事**）。项目根：`F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser`。
> 执行者开工前先读 `交接.md` + `欧陆风云4-数据位置.md` + 本文件对应章节；做完把结论告诉**主对话**（负责联调、全量验证、更新 `交接.md`），执行者**不要自己改 `交接.md`、也不要改本任务书**。
> 上一轮的 `省份国家界面阶段任务书.md` / `第三对话任务书.md` / `卡片客制化-任务书.md` 都已完成归档，只作参考。
> **本轮的「任务 A/B/C/D」与上一轮的 A/B/C/U/R/P 是两回事**，按本文件为准。

---

## 0. 你现在要做什么（操作清单）

| 批次 | 交给谁 | 任务 | 依赖 | 完成标志 |
|---|---|---|---|---|
| **1** | **新对话 A** | **任务 A：数据面**——①`ruler_flags` 与 `personalities` 拆开 ②新增 `rankings` 键（将军榜/君主榜，含评分与分量）③**历史表里的值接线**（宗教/文化/政体/decision，见 §2.5） | 无 | `pnpm timeline` + `pnpm test` + `pnpm verify:player` 全绿；两数据面逐键一致；贴出覆盖率与 `rankings` 样例；历史表四类 **`NAME-MISSING` = 0** |
| **1** | **新对话 B** | **任务 B：名字表**——把英文 key、缺失名字族与未解析占位符全部清掉：①`personalities` 补 28 个 `ancestor_*` ②新增 `tradeGoods` 族 ③**新增 `decisions`（272 个）与 `government`（7 个）族** ④**清洗 `[Root.Get…]` 命令（21 个特权名）** ⑤**补 32 个影响力修正缺名**（见 §2.5） | 无 | 覆盖率探针「未命中 = 0、含 `[` 的名字 = 0」；`pnpm timeline` + `pnpm test` 全绿；自造清单已追加 |
| **1** | **新对话 C** | **任务 C1：界面**——通用「点表头排序」（降序→升序循环，作用到**所有**列表） | 无 | `pnpm verify:player` + `pnpm test` 全绿；§5 的 C1 测试目标你亲手点过 |
| **2** | **对话 C 继续** | **任务 C2：界面**——查看页下方新增「历史十五个最优秀将军」「十五个最优秀君主」两张榜 | ← **任务 A 的键**（A 没到就先用桩，形状以 §3.4 为准） | `pnpm verify:player` + `pnpm test` 全绿；§5 的 C2 目标你亲手点过 |
| **2** | **对话 C 继续** | **任务 C3：界面**——**手机端只读适配**：viewport ＋ Pointer Events（单指拖动/双指捏合）＋ 手机布局 ＋ 手机默认半分辨率·1×（用户 2026-09-19 指定范围，见 §4.3 C3） | 无（与 C2 互不冲突，先后随意） | `pnpm verify:player` + `pnpm test` 全绿；§5.6 的手机目标你亲手点过 |
| **3** | **新对话 D** | **任务 D：重建**——把 A/B/C 的结果烤进 15 份存档与离线页（`修复存档数据.bat`，**只跑一次**） | ← **必须等 A、B、C（含 C2、C3）全部报完成** | 15/15 重建成功；抽查四项通过 |
| **4** | **主对话（不派活）** | 收尾：端到端目视 + 全量验证 + 更新 `交接.md` + 把验收清单给你 | ← D | 三闸门全绿 + 你验收 |

### 严格顺序（一步一步，不要含糊）

1. **第一批（三个对话同时开工，互不重叠文件）**：A（数据面）、B（名字表）、C1（排序界面）。
   - 三者**没有先后依赖**，唯一的公共资源是「跑测试闸门」：各自跑自己的那几项即可，不要同时 `pnpm timeline`（它会写 `tmp/timeline/**`；谁需要就串行，各跑各的会互相覆盖）。
   - **A 必须先跑 `pnpm timeline` 再跑 `pnpm test`**（数据面改了，离线参照要同步，否则 `viewer-build.test.ts` 报假红）。
   - **B 也必须先 `pnpm timeline` 再 `pnpm test`**（名字表是构建期烤进数据面的）。
   - **C1 不碰数据面**，只跑 `pnpm verify:player` 与 `pnpm test`。
   - 结论：**A 与 B 的 `pnpm timeline` 要错开**（A 跑完 → B 再跑；或 B 先跑完 → A 再跑），其余互不干扰。
2. **第二批**：C2（两张榜）与 **C3（手机端只读适配）** 都由**同一个对话 C** 接着做（C1 报完成后）；两者**互不冲突、先后随意**，但 D 必须等它们**都**报完成。C2 **只依赖任务 A 冻结的 `rankings` 形状**（§3.4），不依赖 A 是否已合并——A 还没合并时用 §3.4 的桩数据先把界面与排序做完，A 落地后再换成真数据跑一遍。**C3 不依赖 A/B 任何东西**，可以立刻做。
   - ⚠️ 用户 2026-09-19 拍板：手机端**只做只读适配**（能看、能切视图、单指拖动、双指捏合缩放），**不做**上传/编辑/解析；所以 C3 **不碰** `app.js` 与目录页。
3. **第三批（必须等）**：D 必须等 **A、B、C（含 C2）全部报完成**，且**只跑一次**。原因：产物是构建期烤死的，重建一次 15 份要 5~10 分钟；早跑 = 白烧 + 覆盖。
4. **禁止**：任何任务都不许先动 `.dev-storage/**`（只有 D 可以，且只重建、绝不删除；动手前先按 §4.4 备份）。D 之前若有人在浏览器里手点托管查看页，浏览器会**自愈回写**旧产物，不算破坏，但会掩盖进度——验收只看 D 之后的产物。
5. **只有主对话**改 `交接.md`、本任务书，并做最终联调。

### 启动卡（哪个对话贴哪一段）

- **新对话 A** → 贴 §4.1 末尾「提示词 → 新对话 A」
- **新对话 B** → 贴 §4.2 末尾「提示词 → 新对话 B」
- **新对话 C** → 先贴 §4.3 末尾「提示词 → 新对话 C（C1）」；它报完再贴「→ 对话 C 继续（C2）」与「→ 对话 C 继续（C3 手机端只读适配）」（C2/C3 先后随意，**两个都报完成**才轮得到 D）
- **新对话 D** → 等 A/B/C 都报完之后，贴 §4.4 末尾「提示词 → 新对话 D」

---

## 1. 用户的原话与要求映射

用户 2026-09-19 的原话（**保留措辞**）：

> 「安排好任务，严格说明好顺序 有些特质还是没有被本地化为汉语，将领等列表的排序标签应该支持点一下从大到小排序，再点一下从小到大排序，注意我说的是类似的所有列表 查看页下方栏目添加历史十五个最优秀将军排行榜，要有他的能力和战争实战数据做评分，添加十五个最优秀君主排行榜，要有能力在位时间和在位各项国家实力提升的综合评分，按以上任务建立"第四对话任务书"」

| # | 要求 | 落到哪 | 交付 |
|---|---|---|---|
| 1 | 「有些特质还是没有被本地化为汉语」 | A（拆根因）＋ B（补名） | 面板「性格」列不再出现英文 key；真特质有中文名 |
| 2 | 「排序标签点一下从大到小、再点一下从小到大」，且「**类似的所有列表**」 | C1 | 一个通用排序器，覆盖 §3.3 列出的**全部**表 |
| 3 | 「查看页下方栏目添加历史十五个最优秀将军排行榜，要有他的能力和战争实战数据做评分」 | A（数据）＋ C2（界面） | 新表「历史十五个最优秀将军」 |
| 4 | 「添加十五个最优秀君主排行榜，要有能力在位时间和在位各项国家实力提升的综合评分」 | A（数据）＋ C2（界面） | 新表「十五个最优秀君主」 |
| 5 | 「安排好任务，严格说明好顺序」 | 本文件 §0 | 上面的批次表 + 五步顺序 + 启动卡 |

**用户第二轮贴图追加的要求**（2026-09-19，原话里的例子保留）：

> 「这个也是，先分析问题，不改任务书」——指①国家历史表里 `estate_burghers_comission_merchant_ships_for_war`、`conventicle_act`、`Intrigue_enable`、`2nd 俄罗斯征服波洛茨克之战` 之类；②阶级表里 `[Root.GetNobilityOrFallbackName]整合政策`、`授予[Root.Get市民Name]经济自由权` 之类；③君主性格行里 `stroganovs_asked、well_advised_tutored_heir、widow_widower`（第③条＝要求 1，已在 A/B 里）。
> 主对话已分析完（根因、规模、位置见 **§2.5**），用户拍板：**并进 A/B 现有地盘，不加对话、不改顺序**。

| # | 要求 | 落到哪 | 交付 |
|---|---|---|---|
| 1b | 国家历史表与阶级表里还有英文 key 与 `[Root.Get…]` 占位符 | A（接线 4 行）＋ B（建 `decisions`/`government` 族、清洗命令、补 32 个缺名） | 历史表不再出现 decision key / 文化·宗教 key / 政体 key；阶级特权名不含 `[`；影响力修正不显示原始 key |

---

## 2. 现状复核（主对话 2026-09-19 实测，不是推测）

### 2.1 结构（读代码得到，执行者照这个走）

| 事实 | 位置 | 对任务的意义 |
|---|---|---|
| 数据面有**两份实现**：离线 `scripts/render-timeline.ts`、浏览器 `apps/site/public/viewer-build.js`；由 `apps/site/test/viewer-build.test.ts` **逐键比对** | 两份都调用**共享装配器** `packages/eu4-parser/src/details.ts#buildDetailTables()` | 新键要挂在这个共享装配器里，**不要在两份数据面各写一遍**（这是本项目最容易翻车的地方） |
| `buildDetailTables(input)` 目前收 `{doc, provinces, tagId, saveDate, startDate, wars, tables}` | `details.ts:354`（`DetailInput`）、调用点 `render-timeline.ts:654`、`viewer-build.js:376` | **两份面都已经有 `timeline` 变量**，A 只要把它加进入参（一行），"在位发展度"的月度回放就只需写**一份** |
| `countryDetail` **只给有省份的国家**（本存档 273 个），键排序 | `details.ts:2199`（`landed`）、`:2294`、`:2338` | 榜单候选池不能只看 `countryDetail`，否则死国的名将全丢（见 2.3） |
| 「性格」列的数据来自 `Person.personalities`，而解析器把 **`personalities` 与 `ruler_flags` 两个块合并**塞进同一个数组 | `details.ts:776-784` | 这是"性格列全是英文"的**根因**：把事件标记（flag）当成了性格 |
| 面板「性格」列的渲染在 `viewer.js` 的 `personalityNameOf()`（977 行），读 `DATA.personalityNames[key]`，缺名回退原 key | `viewer.js:976-981`、用于 `:3112`（君主卡）、`:3229`（历代君主表）、`:3244`（未继位继承人） | A 拆完字段后，面板**不用改**就会只剩真性格 |
| 底部三张表是**宿主产出的 HTML 字符串**（`panels.leaderHtml/cityHtml/institutionHtml`），客户端只 `innerHTML` 注进 `#leaderBody/#cityBody/#institutionBody`；表头是 `viewer.html` 里的静态 `<th>` | `viewer.js:43-53`、`viewer.html:528-540` | 排序**不要**去改生产者：客户端直接排 DOM 行，按 `th` 上声明的类型解析单元格文本（§3.3） |
| 面板里的表全部由 `viewer.js` 现场拼 HTML（`rulersPane`/`leadersPane`/`statesPane`/… 见 `viewer.js` 2669-3400） | `viewer.js` | 面板表的排序、`data-sort`、`data-table` 全在 C 一个文件里，不和别人抢 |
| 战斗数据：`DATA.battles` **不含指挥官**（只有 `[月键, 省, 贡献, 攻方tagIdx, 守方tagIdx, 伤亡, 海战]`） | `render-timeline.ts:537-582` | 将军榜**不能**用 `DATA.battles`，要从 `extractWars(doc)`（`wars.ts`）取 `battle.attacker/defender.commander`（**是名字字符串**） |
| 将领档案：`countryDetail[tag].leaders[]`＝`{name, kind, active, activation, fire, shock, maneuver, siege}`，含**已故**将领（不是只有现役） | `details.ts:177-186`、`:1846-1864` | 将军榜的能力值直接取这里；`kind` ∈ `general/admiral/conquistador/explorer` |
| 君主档案：`countryDetail[tag].rulers[]`＝`{name, start, end, months, personalities, adm, dip, mil}`，**全史** | `details.ts:157-166`、`:1792-1809` | 君主榜的能力/在位直接取这里；缺的是"在位发展度" |
| 已有"按身份合并"的纪律：颜色用按日期解析 `resolveTag`，**身份合并（曲线/占领）用 `resolveTagLatest`** | `timeline.ts:451/486` | 榜单也用 `resolveTagLatest`（一国一条线，改名不断线） |
| `uiNameOf(group, key)` 读 `DATA.uiNames[group][key]`；贸易品已经在查 `tradeGoods` 族 | `viewer.js:942`、`:1765` | **B 只要往 `uiNames.json` 加 `tradeGoods` 族，界面一个字都不用改** |
| `buildDetailTables` 的名字表装配**允许文件里多出来的键族原样透传**（固定清单之外也会带上） | `details.ts:2120-2126` | 新增键族不需要改数据面 |
| 离线自包含页把 `viewer.js` **内联**进 HTML | `build-guards.test.ts` 把关 | **`viewer.js` 里绝对不许出现反引号**（已有 3 次前科），也不许出现字面量 `</script>` |
| 面板 DOM 契约：`#detail` / `#detailBody` / `#detailBack` / `#detailClose` / `#detailTitle`；13 个 Tab 用 `id="dTab-*"` | `第三对话任务书.md §1.6`、`verify-player-run.ts` 第 14 节 | 新增榜表的 `tbody` id 要同步登记（§3.3、§4.3） |
| 运行期闸门：`scripts/verify-player-run.ts`（DOM 桩跑真实 `viewer.js`，第 14 节是面板 28 项检查）；静态闸门：`apps/site/test/frontend.test.ts`、`packages/eu4-parser/test/build-guards.test.ts` | 见文件 | C 要在这三处补断言；`build-guards.test.ts:296` 有 `leaderBody/cityBody/institutionBody` 的 id 清单要追加 |

### 2.2 实测：面板「性格」列到底漏了什么（主对话探针 `tmp/probe-trait-names.cjs`）

跑遍 `.dev-storage/saves/*/viewer/data.json`（现 15 份）后的结论：

- 面板会渲染、但 `uiNames.personalities`（182 条）里**没有中文名**的 key，并集 = **168 个**。
- 按 key 形态拆开：
  - **28 个以 `_personality` 结尾**（全部是 `ancestor_*_personality`：`ancestor_calm_personality`、`ancestor_just_personality`、`ancestor_scholar_personality`、`ancestor_tactical_genius_personality` …）→ **是真特质**，只是表里没有（来自模组/事件，游戏里玩家看到的是中文）→ **B 补名**。
  - **140 个不是特质**，是事件/决议标记（例：`has_lowborn_consort`、`me_flag_sonofaconcubine_11`、`just_suggested_new_adm_advisor`、`heiljsf_CK2_event_happen_flag`、`mary_is_heir`、`pali_law_collection` …）→ **A 把 `ruler_flags` 拆出去，这些不再进「性格」列**。
- 单份样例：`b6263f5baf54`（俄罗斯 1574）用到 126 个 key、缺名 74 个；`990e9f3381e8` 用到 46 个、缺名 5 个。

### 2.3 实测：将军榜的可行性（主对话探针 `tmp/probe-commander-match.ts`，跑真存档）

```
save 存档示例/mp_俄罗斯1574_11_12.eu4
wars=638 battles=3172 sides=6344 sidesWithCommander=4958
battle tags=585 resolved=574 resolvedTagsWithLeaders=203 (countryDetail tags=273)
joined=2322 (46.8% of commanded sides), ambiguousName=19, commanderTagUnknown=1754
```

读法（**这决定了榜单的口径，执行者不要改**）：

- 3,172 场战斗里有 **4,958 个"有指挥官的战斗方"**；按「**最终国号 + 指挥官姓名**」能对上将领档案的是 **2,322 个（46.8%）**——已经够排出前 15 名（两千多个战斗方、数百名将领）。
- **对不上的两个原因**：①`countryDetail` 只收"存档当日还有省份"的国家，而战场出现过 **574 个国号**，其中只有 203 个有将领档案（1,754 个战斗方属于这类"没有档案的国家"）；②同名多人（19 例）。
- **所以**：A 要把"战场出现过的国号"也算进候选池（去 `countries/{最终国号}/history` 读将领块），并把**最终覆盖率**写进回报；不管提到多少，**榜单说明里必须写明覆盖率**（不许装作全量）。
- 例子（可作验收锚点）：`1446.2.4 ENG->GBR "约翰 克林顿"` 能对上 1 名将领；`1445.2.2 SRH->DLH "巴鲁儿 洛迪"` 对不上（DLH 有档案但没这个人）。

### 2.4 实测：君主榜的数据陷阱（主对话探针 `tmp/probe-reign-sanity.cjs`，`b6263f5baf54`）

```
countries with rulers=272  rulers=1576  one-ruler countries=11
nameless=3  zero-stats=17  still-ruling=272
  2042m  阿尔布雷希特 V  [HAB] 1444.11.11 .. (now)  stats=5     ← 假任期：HAB 只有 1 条君主任职记录
  1834m  巴尔斯拜        [MAM] 1444.11.11 .. (now)  stats=6
  1560m  (no name)      [POL] 1444.11.11 .. (now)  stats=0     ← 无名占位，必须剔除
  669m   瓦西里 I        [RUS] 1444.11.11 .. 1445.2.9  1/3/2
  798m   尤里 II         [RUS] 1507.3.13 .. 1573.9.12   2/3/6
```

- **11 个国家只有 1 条任职记录**（HAB 是其中之一）→ 任期长度与"在位发展度增量"都不可信（跨了整个战役）。
- 3 条**无名**、17 条**三维全 0**的占位。
- 所以君主榜候选池必须剔除这些（判据见 §3.5），否则榜首会是"在位 170 年的假君主"。

### 2.5 实测：历史表与阶级表的未汉化面（2026-09-19 追加，探针 `tmp/probe-untranslated*.cjs`）

用户第二轮贴图后，主对话扫了 10 份有 `viewer/data.json` 的存档（**123,958 条历史行**）。结论：**问题全是"名字明明有中文却没接线"＋"四个缺失的名字族/清洗"，归属不变（接线归 A，名字与清洗归 B）**。

| # | 现象（用户截图里的例子） | 根因（代码位置） | 实测规模 | 修法 | 归属 |
|---|---|---|---|---|---|
| 1 | `decision` 行显示原始 key：`conventicle_act`、`blasphemy_act`、`Intrigue_enable`、`azuki_recover_color`、`estate_burghers_comission_merchant_ships_for_war` | `details.ts:1672-1673` 把 decision 的 key 当文本写进历史；`uiNames` **压根没有 `decisions` 族** | **19,409 行 / 272 个不同 key**，有名率 **0/272** | 新增 `uiNames.decisions` 族（游戏本体＋全部模组的 `decisions/*.txt` 及 localisation）＋ A 接线 1 行 | B 建族、A 接线 |
| 2 | `接受文化：novgorodian`、`主文化改为 platinean`、`国教改为 protestant` | `details.ts:1621/1624/1627/1634` 直接写原始 key，**从没查过名字表** | **1,852 行**（文化 1,372 ＋ 宗教 480）；而中文名 **150/150 全都在表里** | 纯接线：`uiNames.religions[v]` 与 `cultureNames[cultures.indexOf(v)]` | A（4 行） |
| 3 | `政体改为 monarchy / republic / tribal / theocracy` | 没有「政体名」族（`governmentReforms` 是政体**改革**，不是 `government_name`） | 7 个值（另有模组 `gov_celestial_empire`、`ancient_chinese_togofu`、`native`） | 新增 `uiNames.government` 族 ＋ A 接线 | B 建族、A 接线 |
| 4 | 阶级特权名里带 `[Root.Get…]`：`[Root.GetNobilityOrFallbackName]整合政策`、`[Root.GetStateForm]之上的权力`、`授予[Root.Get市民Name]经济自由权` | 名字表生成时把汉化 mod 的**动态本地化命令原样烤了进来**，从没解析 | **21 个特权名**（`estate_nobles_supremacy_over_crown` 一个就用在 181 处） | 生成期把命令映射成固定中文（`GetStateForm`→政体名、`GetNobilityOrFallbackName`→贵族、`Get市民Name`→市民、`Get奇兹尔巴什Name`→奇兹尔巴什…），未知命令去掉中括号 | B（**不用改数据面**） |
| 5 | 阶级「影响力修正」显示原始 key | 名字表缺条目 | **32 个缺名**：模组 `cestate_*` 25、`FLY_*` 4、`CE_*` 2；本体 3 个（`REGENCY_SLUR`、`KOR_purged_faction`、`REELCTION_DEPOWER_ESTATES`——最后一个连游戏文件都是错拼） | 本体查汉化 mod、模组去各自 localisation 找，找不到才自造并入清单 | B |
| 6 | `2nd 俄罗斯征服波洛茨克之战`、`国家焦点：ADM` | 战争名是存档原样存的；焦点用拉丁缩写 | 序数 2nd–9th；焦点 4 个（ADM/DIP/MIL/none） | **可选**：`^(\d+)(st\|nd\|rd\|th) ` → `第N次 `；`ADM/DIP/MIL/none` → `行政/外交/军事/无` | A（可选） |

> **不要动**（不是缺陷）：`initial`（开局：TAG）、`tagSwitch`（改国号：TAG），以及 `heir/monarch/leader/queen` 里的拉丁人名（`Maitei Calchagui`、`Nayeli`、`Huraa`、`Wassink` 这类是游戏/模组本来的当地语人名，游戏里也没中文化）。
> 🔗 **A 与 B 的耦合**：`uiNames.decisions` / `uiNames.government` 由 B 建、A 用；**A 的代码必须"缺表就退回原 key、不许抛错"**，这样 A 不必等 B、B 也不必等 A。

---

## 3. 冻结的契约（所有任务共同遵守，改动要主对话点头）

### 3.1 数据面机制（不要违反）

1. 任意新数据键都必须在**两份数据面**产出且**逐键一致**；共享逻辑放 `packages/eu4-parser/src/details.ts`（用 `buildDetailTables`），**不要在两份面各写一遍**。
2. 改过数据面或 `assets/ui/*.json` 之后，顺序固定：`pnpm timeline` → `pnpm test`。
3. 缺数据一律给 `0` / `''` / `[]` / `null`，**不许缺键**。
4. 数值一律用"字典 + 下标"风格；中文名**构建期烤进数据面**（离线页运行期读不到游戏文件）。
5. `pnpm test` 需要 `danger-full-access` 会话（当前就是）；遇到 `spawn EPERM` 不要改代码绕过。

### 3.2 铁律

1. **`viewer.js` 与 `render-timeline.ts` 的模板内绝对不许出现反引号**（会截断 HTML 模板，已有 3 次前科，`build-guards.test.ts` 静态把关）；`viewer.js` 内也不许出现字面量 `</script>`。
2. **不许碰** `.dev-storage/**`（只有任务 D 可以，且只重建不删除）；测试用 `tmp/` 或 dev-server 的 `--root` 指到临时目录。
3. 中文路径/文本用 `[IO.File]::ReadAllText/WriteAllText` + `UTF8Encoding($false)`。
4. `.bat`（若确实要做）：**CRLF**、控制台只输出 ASCII、绝不 `chcp`、中文只写文件。
5. 测试条数不要写死：以 `pnpm test` 实跑输出为准。
6. 汇报口径：**先给结论**（做了什么 + 验证结果 + 数字），再列改了哪些文件、怎么验证的、还剩什么；不要叙述调试过程。

### 3.3 排序契约（冻结：一个通用排序器，作用到所有列表）

**A. 作用范围（"类似的所有列表"＝这些，一个不能漏）**

| 位置 | 表 | 谁做的 HTML |
|---|---|---|
| 查看页下方 | 列强榜（`#leaderBody`）、城市发展度（`#cityBody`）、思潮发源地（`#institutionBody`） | 宿主（`panels.*Html`） |
| 查看页下方（**C2 新增**） | 将军榜（`#generalBody`）、君主榜（`#monarchBody`） | 客户端（`viewer.js`，读 `DATA.rankings`） |
| 省份面板 | 扩建基础设施、州、贸易公司投资、省份历史（4 张分组表） | `viewer.js` |
| 国家面板 | 历代君主、未继位继承人、将领、文化构成、宗教构成、建筑、州、阶级的特权表与影响力修正表、顾问、财政明细表、点数明细表、思潮各省进度、历史 | `viewer.js` |

**B. 实现（写在 `viewer.js` 里，**一处**）**

- 一个函数 `makeSortable(rootEl)`：遍历 `rootEl` 内所有 `table`，给每个 `thead th` 绑点击（用 `data-sort-bound` 幂等，重复调用不重复绑）。
- 挂载点：①`viewer.html` 页面级——给 **5 张底部表**的 `<table>` 加 `data-sortable`（客户端启动时对每个 `table[data-sortable]` 调一次）；②面板级——`#detailBody` 每次重渲染后对该元素调一次。
- **列类型**由 `th` 上的 `data-sort` 声明：`num` / `date` / `text`（缺省 `text`）。
  - `num`：取 `td.getAttribute('data-sort')`（有就用它，用于"名称＋旗帜""4 / 5 / 2"这类复合单元格），否则取 `td.textContent`，去掉千分位逗号、`%`、空白；解析不出数字 → 视为 `NaN`，**排序时永远排在最后**（升序降序都在最后）。
  - `date`：`YYYY.M.D` → `年*372 + 月*31 + 日`（与项目现有 ordinal 同式）；空/`—`/`(now)`/`在位` → `NaN`（同样排最后）。
  - `text`：`localeCompare(other, 'zh-CN')`。
- **点击行为（用户原话：点一下从大到小，再点一下从小到大）**：
  - 第一次点某列 → **降序**；再点同一列 → **升序**；再点 → 降序，如此循环。
  - 点了另一列 → 该列从**降序**重新开始。
  - 同一时刻只有一列带 `aria-sort="descending|ascending"`，其余列清除；表头加一个箭头字形（↓/↑）作为可视提示。
  - `th` **没有** `data-sort` 的列（例如 `#` 序号列）不参与排序。
- **`#` 序号列不重写**：它永远显示"原始榜名次"（1..15），排序只搬动行，便于用户对照。
- **稳定排序**：用 `Array.prototype.sort`（V8 稳定），比较值相等时保持原相对顺序——这样平价值下的首行是确定的，验收才可复现。
- **表格身份与状态保持**：每张表加 `data-table="<稳定名>"`（宿主表用 `leader`/`city`/`institution`，C2 两张用 `general`/`monarch`，面板表用 `rulers`/`leaders`/`states`/`history`/`buildings`/`estates`/`advisors`/`culture`/`religion`/`budget`/`mana`/`institution`/`improve`/`tradecompany`/`area`）。
  面板每次重建 HTML 后，排序状态要**保持**：`viewer.js` 里存 `{ table: <data-table>, col: <列下标>, dir: 1|-1 }`，渲染函数末尾调用同一个 `applyStoredSort(rootEl)` 重放。
- **光标**：表头 `cursor:pointer`（可点击控件的常规反馈）；**地图仍然是 `default`（不许出现 `grab`）**——用户要求的是地图不要抓手。
- **不许动**：宿主三张表的 HTML 生成逻辑（`render-timeline.ts` / `viewer-build.js` 的 `leaderHtml/cityHtml/institutionHtml`）与它们的字段顺序。

### 3.4 冻结的新数据键：`rankings`（任务 A 产出、任务 C2 消费）

顶层新增 **1 个键**（顶层键 56 → 57），形状如下（**名字、层级、单位都冻结**）：

```js
rankings: {
  meta: {
    generalPool: number,        // 参与归一化的将领数（有 ≥1 场可归属战斗）
    generalQualified: number,   // 过门槛（≥ MIN_GENERAL_BATTLES）的将领数
    monarchPool: number,        // 参与归一化的君主数（已过剔除规则）
    weights: {                  // 把权重也发出来，页面注释与回报都引用它
      general: { skill: 0.45, war: 0.40, win: 0.15 },
      monarch: { ability: 0.35, tenure: 0.15, growth: 0.35, pace: 0.15 },
    },
    floors: { minBattles: 3, minReignMonths: 60 },
    coverage: {                 // 覆盖率要写在榜的说明里，不许装作全量
      commandedSides: number,   // 有指挥官的战斗方（本存档 4,958）
      joinedSides: number,      // 能对上将领档案的（基线 2,322）
      resolvedTags: number,     // 战场出现过的最终国号数（基线 574）
      tagsWithLeaders: number,  // 其中有将领档案的（基线 203）
    },
  },
  generals: [                   // ≤15 条，按 score 降序
    { tag, name, kind,          // kind: general/conquistador/admiral/explorer
      fire, shock, maneuver, siege, skill,
      battles, wins, winRate,   // winRate ∈ [0,1]
      kills, taken, net,        // 造成伤亡 / 自身伤亡 / 净杀伤（人）
      score,                    // 0..100，一位小数
      parts: { skill, war, win } }   // 三个分量，0..1，已 clamp
  ],
  monarchs: [                   // ≤15 条，按 score 降序
    { tag, name, adm, dip, mil, stats,
      start, end, months,       // end 为 '' 表示仍在位
      devStart, devEnd, devGain, devPerYear, provincesGain,
      score,                    // 0..100，一位小数
      parts: { ability, tenure, growth, pace } }
  ],
}
```

- 两个数组可以少于 15 条，**不许缺键**；`generals`/`monarchs` 数组里的字段一个不许少。
- `meta` 是给页面注释、回报与验收用的，**不要省略**。

### 3.5 冻结的评分口径（常量集中在一处，便于以后调）

**共同规则**

- 归一化：`norm(x) = clamp((x - min) / (max - min), 0, 1)`；`max === min` 时该分量对所有候选人都给 `1`。`min/max` 取自**本存档的候选池**（所以分数是"本存档内相对分"，页面上要写明）。
- 名单内所有分数 `round(100 * Σ(权重 × norm), 1)`。
- 排序（保证两次跑完全一致）：将军 `score ↓ → battles ↓ → skill ↓ → name ↑`；君主 `score ↓ → devGain ↓ → months ↓ → name ↑`。

**将军（「能力和战争实战数据」）**

```js
const GP_SKILL = 0.45, GP_WAR = 0.40, GP_WIN = 0.15;
const MIN_GENERAL_BATTLES = 3;

skillRaw = fire + shock + maneuver + siege            // 0..24
warRaw   = Σ_over_battles (kills_i - taken_i)         // 净杀伤（人）
winRaw   = wins / battles                             // 0..1（不做 min-max，本身就是比例）
score    = 100 * (GP_SKILL*norm(skillRaw) + GP_WAR*norm(warRaw) + GP_WIN*winRaw)
```

- **一场战斗记到这个将领头上**＝该战斗方 `country`（先 `resolveTagLatest` 取最终国号）＋ `commander` 姓名，与候选池里的 `(tag, name)` 精确相等。
- `kills` = 对手那一方 `losses`，`taken` = 自己这一方 `losses`（`wars.ts` 的 `BattleSide.losses`，缺就给 0）。
- `wins` = 自己那一方获胜的场次（`battle.attackerWon` 对攻方直接取值，对守方取反）。
- **同名多人（同国号同名）**：把该名字的战斗记到**激活日期与该战斗日期最近**的那一位（`activation` 与 `battle.date` 都转 ordinal 后取最小距离），并在回报里给出这类例子的数量。
- 候选池 = ①能对上档案的将领（`countryDetail[tag].leaders`）＋ ②A 扩展出的"战场出现过、但 `countryDetail` 里没有的国家"的将领（去 `countries/{最终国号}/history` 读）。**两个来源合并后按 `(最终国号, 姓名)` 去重**。
- 入榜门槛：`battles >= MIN_GENERAL_BATTLES`。

**君主（「能力、在位时间和在位各项国家实力提升」）**

```js
const MP_ABILITY = 0.35, MP_TENURE = 0.15, MP_GROWTH = 0.35, MP_PACE = 0.15;
const MIN_REIGN_MONTHS = 60;      // 5 年

abilityRaw = adm + dip + mil                       // 0..18
tenureRaw  = months
growthRaw  = devEnd - devStart                     // 在位发展度增量（可为负）
paceRaw    = devGain / (months / 12)               // 年均发展度增量
score = 100 * (MP_ABILITY*norm(abilityRaw) + MP_TENURE*norm(tenureRaw)
             + MP_GROWTH*norm(growthRaw) + MP_PACE*norm(paceRaw))
```

- **候选池剔除规则（三条都要，实测依据见 §2.4）**：
  1. `name === ''` 的占位君主 → 剔除；
  2. 该国 `rulers.length === 1` **且** 这位君主 `end === ''`（＝存档里根本没有这个国家的君主任职链，任期跨了整个战役）→ 剔除；
  3. `months < MIN_REIGN_MONTHS` → 不入榜（短任期会把 `paceRaw` 顶到天上）。
- **发展度口径**：`devStart` = 该国**身份**（`resolveTagLatest`）在 `start` 那个月的"拥有省份发展度之和"；`devEnd` = `end` 那个月的值，仍在位就取**存档日期末月**。与"国家兴衰曲线"同口径（月度回放、按身份合并、改名不断线）。
- `provincesGain` = 同两个时点的拥有省份数之差，只做展示列，**不进分数**。
- 候选池的发展度回放要在 `details.ts` 里**只实现一份**（`DetailInput` 加 `timeline` 入参，见 2.1）；做法：像曲线那样一次月度扫描，同时维护一个 `Map<tag, dev>` 与一个 `Map<tag, provinceCount>`，只在"某国某君主的起/止月"做快照（不发布任何逐月序列）。

### 3.6 冻结的新 DOM（任务 C 负责实现、登记进测试）

```html
<h2>历史十五个最优秀将军 · 综合评分</h2>
<div class="note">评分＝能力 45% ＋ 净杀伤 40% ＋ 胜率 15%（本存档内归一化）；只收参战 ≥ 3 场的将领；
只统计能按「国家＋姓名」对上将领档案的战斗（本存档覆盖率 4,958 个指挥战斗方中 2,3xx 个）。</div>
<table data-sortable data-table="general"><thead><tr>
  <th>#</th><th data-sort="text">姓名</th><th data-sort="text">国家</th><th data-sort="text">类型</th>
  <th data-sort="num">能力合计</th><th data-sort="num">参战</th><th data-sort="num">胜</th>
  <th data-sort="num">胜率</th><th data-sort="num">造成伤亡</th><th data-sort="num">自身伤亡</th>
  <th data-sort="num">净杀伤</th><th data-sort="num">综合评分</th>
</tr></thead><tbody id="generalBody"></tbody></table>

<h2>十五个最优秀君主 · 综合评分</h2>
<div class="note">评分＝能力 35% ＋ 在位时长 15% ＋ 在位发展度增量 35% ＋ 年均发展度增速 15%（本存档内归一化）；
只收在位 ≥ 5 年、且存档里有完整任职链的君主。</div>
<table data-sortable data-table="monarch"><thead><tr>
  <th>#</th><th data-sort="text">姓名</th><th data-sort="text">国家</th>
  <th data-sort="num">ADM/DIP/MIL</th><th data-sort="num">三维合计</th>
  <th data-sort="date">起</th><th data-sort="date">止</th><th data-sort="num">在位月数</th>
  <th data-sort="num">发展度增量</th><th data-sort="num">年均增量</th><th data-sort="num">综合评分</th>
</tr></thead><tbody id="monarchBody"></tbody></table>
```

- 两张表的 `#` 列同样显示"评分榜原始名次"，不参与排序。
- 复合单元格（姓名/国家/三维）**必须**带 `data-sort` 覆盖值（例：三维单元格 `data-sort="18"`，显示 `6 / 6 / 6`；国家单元格 `data-sort="俄罗斯"`，内容含旗帜）。
- 榜的说明里必须带实测覆盖率与权重（沿用 `rankings.meta`，**不要写死字符串**，从 `DATA.rankings.meta` 里读）。
- 需登记的 id（`build-guards.test.ts:296` 那一段）：`generalBody`、`monarchBody`；C2 还要把这两张表的 `<h2>`/`data-table` 写进 `frontend.test.ts` 或 `verify-player-run.ts` 的断言。

---

## 4. 任务

### 4.1 任务 A（新对话 A）：数据面 —— 性格/标记拆分 ＋ `rankings` ＋ 历史表接线

**文件所有权（只有你能改）**
`packages/eu4-parser/src/details.ts`、`packages/eu4-parser/src/types.ts`（如需要）、`packages/eu4-parser/src/index.ts`、`scripts/render-timeline.ts`（**只改** `buildDetailTables` 调用处的入参、把 `rankings` 加进 data.json 的那一处）、`apps/site/public/viewer-build.js`（**只改**同样两处）、`apps/site/public/eu4-parser.js`（由 `pnpm browser` 重新生成）、`scripts/build-browser-parser.ts`（若导出清单要动）、`apps/site/test/viewer-build.test.ts`（新键断言）、`tmp/**`（探针）。

**不许改**：`apps/site/public/viewer.js`、`viewer.html`、`style.css`（任务 C 的地盘）、`scripts/export-ui-tables.ts` 与 `assets/ui/*.json`（任务 B 的地盘）、`交接.md`、本任务书、`.dev-storage/**`。

**交付 1：性格与事件标记拆开（根因修复）**

- `Person.personalities` 现在把 **`personalities` 块与 `ruler_flags` 块**混在一起（`details.ts:776-784`）。拆成两个：
  - `personalities`：只来自 `personalities` 块（真特质）；
  - `flags`：来自 `ruler_flags` 块。
- 三个出口的形状都要带上新字段（**不许缺键**）：`countryDetail[tag].monarch.flags`、`countryDetail[tag].rulers[*].flags`、`countryDetail[tag].failedHeirs[*].flags`。
- `viewer.js` **不用改**（它只读 `personalities`）——拆完 140 个 flag 就自动从「性格」列消失。
- 同步 `viewer-build.test.ts` 里与 `personalities` 有关的断言（第 545、564-577 行那几处）。

**交付 2：`rankings` 键（§3.4 的形状 + §3.5 的口径）**

- 计算放在 `details.ts` 里（共享，两份面共用）。
- `DetailInput` 增加 `timeline`（`packages/eu4-parser/src/timeline.ts` 的 `buildTimeline()` 结果）；两个调用点各加一行（两份面在调用前都已经有 `timeline` 变量）。
- 扩候选池：除了 `countryDetail` 里的将领，还要把"战场出现过、但不在 `countryDetail` 里"的最终国号也算进来（读 `countries/{tag}/history` 的将领块）。**若某个国号在存档 `countries` 区块里已经没有 `history` 块，就读不到——如实记进回报，不要编造。**
- 覆盖率、池大小、门槛、权重按 §3.4 的 `meta` 发出来。
- 报告数字（必须出现在回报里）：`commandedSides` / `joinedSides`（基线 4,958 / 2,322）/ 扩展后的 `tagsWithLeaders` / `generalPool` / `generalQualified` / `monarchPool` / `rankings.generals[0]` 与 `rankings.monarchs[0]` 的**完整对象**。

**交付 3：历史表里的"值"接上名字表（用户第二轮贴图追加，§2.5）**

- `details.ts:1621/1624/1627/1634` 现在把原始 key 直接拼进历史文本，改成查表：
  - `religion` → `uiNames.religions[value]`（实测 480 行，中文名 10/10 都在）；
  - `primaryCulture` / `addAcceptedCulture` / `removeAcceptedCulture` → `cultureNames[cultures.indexOf(value)]`，取不到再退回 `uiNames.cultures[value]`（实测 1,372 行，150/150 都有）；
  - `government`（`:1647`）→ `uiNames.government[value]`（**B 建的新族**）；
  - `decision`（`:1672-1673`）→ `uiNames.decisions[value]`（**B 建的新族**）。
- **缺表/缺名一律退回原 key，不许抛错、不许缺键**——这样 A 不用等 B（B 还没合时这两族就是 `{}`）。
- `focus`（`:1659`）：`ADM/DIP/MIL/none` → `行政/外交/军事/无`。
- 可选：`warStart/warEnd` 的 `2nd …` → `第N次 …`（`/^(\d+)(st|nd|rd|th) /`）；做了就在回报里说明。
- **不要动** `initial`（开局：TAG）、`tagSwitch`（改国号：TAG）与四条人物事件里的人名（§2.5 末尾）。

**验收（A）**

- 探针（可写 `tmp/a4-rankings-check.ts`，抄 `tmp/s1-wave3-check.ts` 的加载样板）打印上面那串数字，并**独立复核**：①随便挑一位入榜将领，手工数它在 `extractWars` 里同一 `(最终国号, 姓名)` 的战斗场次，必须与 `battles` 一致；②随便挑一位入榜君主，用独立的月度回放核对 `devGain`（与"国家兴衰曲线"同月同国的发展度差一致）。
- `pnpm timeline` → `pnpm test` → `pnpm verify:player` 全绿；`viewer-build.test.ts` 打印的"all N keys match"里 N 比原来 +1。
- 改动前后 `data.json` 体积对比（新键预计 **< 20 KB**；若超过 100 KB，说明把逐月明细塞进去了，必须改掉）。
- 历史表复核：用主对话的 `tmp/probe-history-values.cjs` 跑一遍，`religion` / `primaryCulture` / `addAcceptedCulture` / `removeAcceptedCulture` 四类的 `NAME-MISSING` 必须为 **0**；B 合并后 `government` 与 `decision` 也应为 0（B 未合并前它们退回原 key 是预期行为，要在回报里写明当时是哪一种）。
- 改完把结论报给主对话（不要自己改 `交接.md`）。

**提示词 → 新对话 A**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第四对话任务书.md 的「2.1 / 2.3 / 2.4 / 3.1 / 3.4 / 3.5 / 4.1」——
你是本轮「任务 A：数据面」的执行者，口径已经冻结在任务书里，不要自己另立公式。

要点：
- 只改 packages/eu4-parser/src/details.ts（＋ types.ts/index.ts 如需）、scripts/render-timeline.ts 与
  apps/site/public/viewer-build.js 里"调用 buildDetailTables、把新键加进 data.json"的那几处、
  apps/site/test/viewer-build.test.ts、tmp/ 下的探针；不许动 viewer.js/viewer.html/style.css、
  export-ui-tables.ts 与 assets/ui/*.json、交接.md、本任务书、.dev-storage；
- 交付 1：把 ruler_flags 从 personalities 里拆出来（details.ts:776-784），新增 flags: string[]，
  monarch / rulers[*] / failedHeirs[*] 三处都要有这个键且不许缺；面板不用改（它只读 personalities）；
- 交付 2：新增顶层键 rankings（形状＝任务书 §3.4，公式＝§3.5），计算放在 details.ts 里共享一份，
  DetailInput 增加 timeline 入参（两份面调用前都已有 timeline 变量，各加一行）；
  将军候选池要扩展到"战场出现过但不在 countryDetail 里"的国号（读 countries/{tag}/history 的将领块），
  君主候选池要按 §3.5 剔除"无名 / 只有 1 条任职记录且仍在位 / 在位不足 5 年"的假任期；
  覆盖率与池大小按 meta 发出来，不许装作全量；
- 交付 3（用户第二轮追加，§2.5）：历史表里的"值"接线——details.ts:1621/1624/1627/1634 的 religion 与
  culture（primaryCulture/addAcceptedCulture/removeAcceptedCulture）改成查 uiNames.religions 与
  cultureNames[cultures.indexOf(v)]；:1647 government 查 uiNames.government、:1672 decision 查
  uiNames.decisions（两族由任务 B 建，**缺表就退回原 key、不许抛错**）；focus 的 ADM/DIP/MIL/none → 行政/外交/军事/无；
  warStart/warEnd 的 "2nd " → "第N次 "（可选）；不要动 initial/tagSwitch 与四条人物事件里的人名；
- 发展度回放只实现一份（月度扫描 + Map<tag,dev>，只在君主起止月做快照），不要发布逐月序列；
  viewer.js 里不许出现反引号；
- 验收：pnpm timeline → pnpm test → pnpm verify:player 全绿；viewer-build.test.ts 的"all N keys match"里 N +1；
  探针独立复核至少一位将军的战斗场次与一位君主的发展度增量；历史表用 tmp/probe-history-values.cjs 复核
  四类 NAME-MISSING = 0；贴出 meta 全字段与 generals[0]/monarchs[0] 完整对象；
- 汇报先给结论（覆盖率、池大小、两位榜首对象、改了哪些文件、验证结果），不要叙述调试过程。
```

---

### 4.2 任务 B（新对话 B）：名字表 —— 面板上不再有英文 key

**文件所有权（只有你能改）**
`scripts/export-ui-tables.ts`、`apps/site/public/assets/ui/uiNames.json`、`tmp/自造译名清单.md`、`tmp/**`（探针/报告）。

**不许改**：`viewer.js`、`viewer.html`、`style.css`、`packages/eu4-parser/**`、`scripts/render-timeline.ts`、`apps/site/public/viewer-build.js`、`交接.md`、本任务书、`.dev-storage/**`。

**交付 1：`personalities` 族补全（28 个已知缺名）**

- 已知缺名清单（`tmp/probe-trait-names.cjs` 实测，28 个，全部以 `_personality` 结尾）：
  `ancestor_architectural_visionary_personality`、`ancestor_benevolent_personality`、`ancestor_bold_fighter_personality`、`ancestor_calm_personality`、`ancestor_careful_personality`、`ancestor_charismatic_negotiator_personality`、`ancestor_conqueror_personality`、`ancestor_entrepreneur_personality`、`ancestor_fierce_negotiator_personality`、`ancestor_free_thinker_personality`、`ancestor_incorruptible_personality`、`ancestor_industrious_personality`、`ancestor_inspiring_leader_personality`、`ancestor_intricate_web_weaver_personality`、`ancestor_just_personality`、`ancestor_kind_hearted_personality`、`ancestor_lawgiver_personality`、`ancestor_martial_educator_personality`、`ancestor_midas_touched_personality`、`ancestor_reformist_personality`、`ancestor_scholar_personality`、`ancestor_secretive_personality`、`ancestor_silver_tongue_personality`、`ancestor_strict_personality`、`ancestor_tactical_genius_personality`、`ancestor_tolerant_personality`、`ancestor_well_connected_personality`、`ancestor_zealot_personality`。
- 来源顺序：①游戏本体 `common/ruler_personalities`（现有做法）；②**全部已装模组**的同类目录（`D:\Software\Steam\Steam\steamapps\workshop\content\236850\<id>\common\ruler_personalities`，模组可能换目录名，扫一遍再定）；③各模组的 `localisation`（`l_simp_chinese.yml` / `l_chinese.yml`）里的中文名。
- **key 的写法**要与汉化 mod 对齐：现有生成器已有"变体查找"（`(k) => [k.replace(/_personality$/, '')]`），照抄它的做法。
- 万一真的找不到官方中文（真·自造）：写进 `tmp/自造译名清单.md`，**并在回报里把自造条目单独列全**（用户要逐条核对）。

**交付 2：新增 `tradeGoods` 族**

- 面板现在会显示 `grain` / `coal` 这类英文 key（`viewer.js:1765` 已经在查 `uiNameOf('tradeGoods', key)`，只是表里没有这个族）。
- 31 个 key（游戏本体 localisation 里就有中文，汉化 mod 也有）→ 加进 `uiNames.json`。
- **界面一个字都不用改**；`details.ts` 的名字表装配会把多出来的键族原样透传（已确认）。

**交付 3：覆盖率探针（新写，必须留下）**

- 目标：对 **15 份** `.dev-storage/saves/*/viewer/data.json`，把面板会显示的所有"名字类字段"与对应名字表逐一对齐，输出**未命中 = 0**（或给出无法命中的白名单，附原因）。
  至少覆盖：`personalities`（拆开后只剩真特质）、`cultures`、`governmentReforms`、`buildings`、`greatProjects`、`terrain`、`religions`、`institutions`、`ideaGroups`、`advisors`、`areas`、`estates`/`estatePrivileges`/`estateInfluenceModifiers`、**`tradeGoods`**。
- 建议直接扩写 `tmp/probe-trait-names.cjs` 的做法（那个脚本已经是"按存档实际用到的 key 统计未命中"）。
- **注意**：A 正在把 `ruler_flags` 拆出去，B 的探针要能容忍两种形状（`flags` 存在与否都行），或在 A 合并后再跑一次；两者**不冲突**（B 只读 data.json，不写）。

**交付 4：历史表与阶级表的缺失名字与占位符（用户第二轮贴图追加，§2.5）**

- **新增 `decisions` 族**：实测 **272 个不同 decision key 完全没有名字**（19,409 行历史都显示原始 key）。来源＝游戏本体 `decisions/*.txt` ＋ **全部已装模组**的同类目录与它们的 `localisation`；模组私有的（`azuki_recover_color`、`Intrigue_enable`、`FLY_*`、`CE_*`、`estate_*`）去对应模组里找中文，找不到才自造并入清单。
- **新增 `government` 族**：7 个值 `monarchy` / `republic` / `tribal` / `theocracy` / `native` / `gov_celestial_empire` / `ancient_chinese_togofu`。
- **清洗 `[Root.Get…]` 动态命令**：**21 个特权名**的中文里含未解析命令（清单见 §2.5 第 4 行的 `tmp/probe-untranslated.cjs` 输出）。做法＝**生成期**把命令换成固定中文（`GetStateForm`→政体名、`GetNobilityOrFallbackName`/`Get贵族Name`→贵族、`Get市民Name`→市民、`Get奇兹尔巴什Name`→奇兹尔巴什、`Get贵族Name`→贵族…），**未知命令去掉整个中括号**只留词干；最终表里**不许再出现 `[`**。
- **补 32 个影响力修正缺名**：模组 `cestate_*` 25、`FLY_*` 4、`CE_*` 2，本体 3（`REGENCY_SLUR`、`KOR_purged_faction`、`REELCTION_DEPOWER_ESTATES`——最后一个是游戏文件里的错拼，按错拼去查）。
- 复核脚本现成（主对话留的）：`tmp/probe-untranslated.cjs`、`tmp/probe-untranslated-union.cjs`，改完各跑一遍对照。

**验收（B）**

- 覆盖率探针输出：`personalities` 未命中 0；`tradeGoods` 31/31 有中文；**含 `[` 的名字 = 0**；`estateInfluenceModifiers` 缺名 = 0；`decisions` **272/272**、`government` **7/7** 有中文；其它族未命中 0。
- `pnpm timeline` → `pnpm test` 全绿（名字表是构建期烤进数据面的，**必须先 `pnpm timeline`**）。⚠️ 如果 A 也在改数据面，**两人不要同时跑 `pnpm timeline`**（会互相覆盖 `tmp/timeline/**`）——报一下就错开。
- 回报里给出：新增条数（各族）、自造条目全表、`uiNames.json` 体积变化。

**提示词 → 新对话 B**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第四对话任务书.md 的「2.2 / 3.1 / 4.2」——
你是本轮「任务 B：名字表」的执行者。

要点：
- 只改 scripts/export-ui-tables.ts、apps/site/public/assets/ui/uiNames.json、tmp/自造译名清单.md 与 tmp/ 下的探针；
  不许动 viewer.js/viewer.html/style.css、packages/eu4-parser/**、scripts/render-timeline.ts、
  apps/site/public/viewer-build.js、交接.md、本任务书、.dev-storage；
- 交付 1：uiNames.personalities 补全 28 个 ancestor_*_personality（清单在任务书 §4.2），来源＝游戏本体 +
  全部已装模组的 common/ruler_personalities + 各模组 localisation（l_simp_chinese/l_chinese）；
  key 变体查找照抄现有生成器的写法；找不到官中的才自造，自造条目要单独列全给用户核对；
- 交付 2：新增 tradeGoods 族（31 个 key，viewer.js:1765 已经在查这个族，界面不用改）；
- 交付 3：写一个覆盖率探针：对 .dev-storage/saves/*/viewer/data.json（现 15 份）统计所有名字类字段的未命中，
  目标 0；做法可扩写 tmp/probe-trait-names.cjs；
- 交付 4（用户第二轮追加，§2.5）：①新增 decisions 族（272 个 key，历史表 19409 行都在裸奔；本体＋全部模组的
  decisions 与 localisation 都要扫）；②新增 government 族（7 个值）；③清洗 21 个特权名里的 [Root.Get…] 动态命令
  （生成期换成固定中文，未知命令去掉中括号，最终表里不许再有 "["）；④补 32 个影响力修正缺名
  （cestate_* 25 / FLY_* 4 / CE_* 2 / 本体 REGENCY_SLUR、KOR_purged_faction、REELCTION_DEPOWER_ESTATES 错拼）；
  复核脚本用主对话留的 tmp/probe-untranslated.cjs 与 tmp/probe-untranslated-union.cjs；
- 验收：pnpm timeline → pnpm test 全绿（注意：任务 A 也在改数据面，两人不要同时跑 pnpm timeline，报一下错开）；
  贴出各族新增条数（含 decisions 272/272、government 7/7）、含 "[" 的名字 = 0、影响力修正缺名 = 0、
  自造条目全表、uiNames.json 体积变化；
- 汇报先给结论，不要叙述调试过程。
```

---

### 4.3 任务 C（新对话 C）：界面 —— 三波（C1 排序 / C2 两张榜 / C3 手机端只读适配）

**文件所有权（只有你能改）**
`apps/site/public/viewer.js`、`apps/site/public/viewer.html`、`apps/site/public/style.css`、`scripts/verify-player-run.ts`、`apps/site/test/frontend.test.ts`、`packages/eu4-parser/test/build-guards.test.ts`（**只加**新 id 登记）、`tmp/**`。

**不许改**：`packages/eu4-parser/src/**`、`scripts/render-timeline.ts`、`apps/site/public/viewer-build.js`、`apps/site/public/app.js`（目录页这一轮不动）、`assets/ui/*.json`、`交接.md`、本任务书、`.dev-storage/**`。

#### C1（第一波：排序，不依赖任何人）

1. 按 §3.3 实现通用排序器：`viewer.html` 给底部三张表加 `data-sortable` 与 `data-table`、给每个 `<th>` 标 `data-sort`（`#` 列不标）；`viewer.js` 里加 `makeSortable(rootEl)` + `applyStoredSort(rootEl)` + 排序状态表；面板每次重建 HTML 后重放。
2. 面板里的每张表：加 `data-table` 稳定名（§3.3 的清单），复合单元格加 `data-sort` 覆盖值（三维 `4 / 5 / 2` → `data-sort="11"`）。
3. **不改**宿主产出的三张表 HTML（`panels.*Html`）。
4. 测试：
   - `verify-player-run.ts` 新增一节（第 15 节）：断言①点一次表头是降序、再点一次是升序、第三次回到降序；②`aria-sort` 只有一个列有；③平价值下首行符合"稳定排序"；④`#` 列文本没被改写；⑤面板重渲染后排序状态保持（选一个带表的 Tab 验证）；⑥`th` 没有 `data-sort` 的列点击无效。
   - `frontend.test.ts`：断言 `viewer.html` 里底部三张表都有 `data-sortable`，且 `viewer.js` 里**没有**出现对 `panels.*Html` 的二次改写。
5. 验收：`pnpm verify:player` + `pnpm test` 全绿；把 §5.1 的 C1 测试目标交给用户。

#### C2（第二波：两张榜，依赖 A 的 `rankings`）

1. `viewer.html` 加 §3.6 的两张表（`#generalBody` / `#monarchBody`，`data-sortable`、`data-table="general"|"monarch"`），表头与说明照抄（说明文字从 `DATA.rankings.meta` 取值，不要写死数字）。
2. `viewer.js` 加两个渲染函数从 `DATA.rankings` 渲染：
   - 将军榜：`#`（原榜名次）/ 姓名 / 国家（旗帜＋国名，`data-sort` 覆盖） / 类型（`leaderKindLabel` 已有）/ 能力合计 / 参战 / 胜 / 胜率（一位小数百分比）/ 造成伤亡 / 自身伤亡 / 净杀伤（千分位）/ 综合评分（一位小数）。
   - 君主榜：`#` / 姓名 / 国家 / ADM/DIP/MIL（`data-sort` 覆盖三维合计）/ 三维合计 / 起 / 止（`''` 显示「在位」）/ 在位月数 / 发展度增量（带 `+`/`−`）/ 年均增量 / 综合评分。
   - 数字一律 `fmt()`（现有函数，`zh-CN` 千分位）；空值写 `DASH`。
   - `DATA.rankings` 不存在时（旧产物）：两张表显示一行「数据面待补」，**不要抛错**。
3. 排序：两张表自动继承 C1 的排序器（它们带 `data-sortable`）。
4. 登记：`build-guards.test.ts:296` 的 id 清单追加 `generalBody`、`monarchBody`；`verify-player-run.ts` 再补一节断言：两张表 15/15 行、评分列是数字、点「综合评分」一次降序二次升序、点「国家」按中文名排序、`rankings` 缺失时不报错。
5. 验收：`pnpm verify:player` + `pnpm test` 全绿；把 §5.2 的 C2 目标交给用户。

#### C3（第三波：手机端只读适配；用户 2026-09-19 指定范围）

**范围（用户原话）**：「手机端能看就行了，不需要上传、编辑，能够流畅使用视图，能够拖动视图，能够双指捏合调整视图大小就行」。
→ 只做"**手机浏览器查看已入库存档**"这一条路；上传/解析/编辑保持现状，**不动 `app.js` 与目录页**。

**冻结契约（照做；不许自创口径、不许往数据面加字段）**

1. **viewport**：`viewer.html` 的 `<head>` 加一行
   `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`
   （目录页 `index.html` 已有，不改它）。
2. **地图交互迁到 Pointer Events，且桌面鼠标行为逐条不变**：
   - `pointerdown`（只认 `isPrimary`）→ 记起点 ＋ `canvas.setPointerCapture(e.pointerId)`，并维护一张 active pointers 表；
   - **单指** `pointermove` → 平移，**沿用现有 `zx/zy` 累加 + `clampView()` + `applyView()`**，不要另写一套变换；
   - **双指**：出现第二个点时记 `d0`（两指距离）与当时的 `view.scale`；之后每次 `pointermove` 按
     `next = clamp(s0 * d / d0)` 求目标 scale，然后**只做增量** `zoomAt(next / view.scale, midX, midY)`
     （中点＝两指中点的容器坐标）——复用现成的 `zoomAt()`，**不要直接改 `view.scale`**（会和 clamp 打架）；
   - **`pointercancel` 必须与 `pointerup` 等效**（触摸被系统打断时不许卡在拖动/双指态）；最后一个指针抬起时清空表；
   - `wheel` 保留原样（桌面滚轮不变）；
   - **点击判定**：`|dx|+|dy| ≤ 15`（`pointerType !== 'touch'`）／`≤ 24`（`pointerType === 'touch'`）；500 ms 时间阈值不变；
   - hover 高亮：`pointermove` 也驱动（手指按住时高亮跟随）；桌面 `mouseleave` 行为不变。
3. **手机布局**（只在 `@media (pointer:coarse)` 或 `max-width:820px` 内生效，**桌面不许有任何像素变化**）：
   - `#detail` 面板＝**全屏抽屉**（贴满视口、自身可滚动）；13 个 Tab 横向滚动；
   - 底部三张表 ＋ 两张榜：外层横向滚动（`overflow-x:auto`），表头不换行；
   - 曲线 `<svg>` 高度 690 → 约 **380px**；
   - 控制条/工具栏的触摸目标 ≥ **40px**（播放、速度 +−、九个视图、⛶ 全屏）；
   - 「📝 修改存档信息」**保留**（它也是档案信息入口），但必须点得准（≥40px）。
4. **手机默认值**（只影响 `pointer:coarse`；桌面默认不变）：
   - 仍默认**一半分辨率**；手机上**禁用切到原生**（点击给提示「手机上不支持原生分辨率」）；
   - 默认速度 **1×**（桌面仍 3×）；> 8× 时给一行提示。
5. **不许改**：`panels.*Html` 的产出、任何数据面键与形状、`app.js`、桌面视觉与行为、`.dev-storage/**`。
6. **铁律**：`viewer.js` 不许出现反引号；不许出现字面量 `</script>`。

**测试**

- `verify-player-run.ts` 新增一节（手机节），断言：
  ①单指 `pointerdown/move/up` 序列 → 平移量与 `applyView()` 的 transform 一致；
  ②双指序列（两个 pointerId 交替 move）→ scale 按距离比变化，且锚点＝两指中点（`view.x/y` 随之变化）；
  ③`pointercancel` 后不残留拖动/双指态（再发单指 move 不平移）；
  ④触摸阈值：`|dx|+|dy| = 20` 的**触摸**序列不弹面板，**鼠标**同距离要弹面板；
  ⑤手机默认 1×、分辨率按钮禁用；桌面默认 3×、可切原生；
  ⑥**第 6 节（缩放/平移/filter）原样通过**——这是"桌面没被改坏"的证据。
- `frontend.test.ts`：断言 `viewer.html` 含 `name="viewport"`。
- 验收：`pnpm verify:player` + `pnpm test` 全绿；把 §5.6 的手机目标交给用户。

**注意**：托管查看页即时生效（它加载站点上的 `viewer.js`）；**手机上看离线自包含页要等一次重建**（任务 D 或 `修复存档数据.bat`）。

**提示词 → 新对话 C（C1）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第四对话任务书.md 的「2.1 / 3.2 / 3.3 / 4.3 的 C1」——
你是本轮「任务 C：界面」的执行者，**这一轮只做 C1（通用排序）**，两张榜（C2）等我说继续。

要点：
- 只改 apps/site/public/viewer.js、viewer.html、style.css、scripts/verify-player-run.ts、
  apps/site/test/frontend.test.ts、packages/eu4-parser/test/build-guards.test.ts（只加 id 登记）、tmp/；
  不许动 packages/eu4-parser/**、scripts/render-timeline.ts、viewer-build.js、app.js、assets/ui/*.json、
  交接.md、本任务书、.dev-storage；
- 排序契约照 §3.3 一条不改：作用到"所有类似列表"（底部三张宿主表 + 省份面板 4 张 + 国家面板各 Tab 的表）；
  一个 makeSortable(rootEl)，th 上 data-sort="num|date|text"，复合单元格用 td 的 data-sort 覆盖；
  第一次点击降序、再点升序、循环；同一时刻只有一个 aria-sort；"#" 列不排序也不重写；
  稳定排序（平价值次序可复现）；面板重渲染后排序状态要保留（data-table 稳定名 + applyStoredSort）；
  宿主产出的三张表 HTML 一个字都不改；表头 cursor:pointer，地图仍必须是 default（不许出现 grab）；
- viewer.js 里绝对不许出现反引号（会截断 HTML 模板）；不许出现字面量 </script>；
- 测试：verify-player-run.ts 新增一节覆盖①降/升/循环②aria-sort 唯一③稳定排序④# 列没被改写
  ⑤面板重渲染后保持⑥无 data-sort 的列点了无效；frontend.test.ts 断言三张表带 data-sortable；
- 验收：pnpm verify:player + pnpm test 全绿；给我一份"我自己能动手验"的测试目标（打开哪个页面、点哪里、应看到什么）；
- 汇报先给结论，不要叙述调试过程。
```

**提示词 → 对话 C 继续（C2，等 A 的键到位或先用桩）**

```
继续 C2：查看页下方新增「历史十五个最优秀将军」与「十五个最优秀君主」两张排行榜。

口径与 DOM 全在 第四对话任务书.md 的「3.4 / 3.5 / 3.6 / 4.3 的 C2」——
你只做界面：从 DATA.rankings 渲染两类榜（≤15 行）、表头 data-sort 齐全、说明文字从 rankings.meta 取，
排序沿用 C1 的排序器；DATA.rankings 缺失时显示"数据面待补"而不报错。
登记 generalBody/monarchBody 进 build-guards 的 id 清单，并在 verify-player-run.ts 补一节断言。
只改你在 C1 里拥有的那几个文件；不许动 packages/eu4-parser/**、render-timeline.ts、viewer-build.js、
assets/ui/*.json、交接.md、本任务书、.dev-storage。
验收：pnpm verify:player + pnpm test 全绿，并给我一份能自己动手验的测试目标。汇报先给结论。
```

**提示词 → 对话 C 继续（C3 手机端只读适配；不依赖 A/B，可立刻做）**

```
继续 C3：手机端只读适配（viewport ＋ 单指拖动 ＋ 双指捏合缩放 ＋ 手机布局 ＋ 手机默认半分辨率/1×）。

范围与冻结契约全在 第四对话任务书.md 的「4.3 的 C3」与「5.6」——
用户范围原话：手机端能看就行，不需要上传、编辑；要能流畅切视图、单指拖动视图、双指捏合调整视图大小。
只改你在 C1 里拥有的那几个文件（viewer.js / viewer.html / style.css / scripts/verify-player-run.ts /
apps/site/test/frontend.test.ts / build-guards.test.ts 只加 id 登记）；不许动 packages/eu4-parser/**、
render-timeline.ts、viewer-build.js、app.js、assets/ui/*.json、交接.md、本任务书、.dev-storage。

要点：
- 地图交互迁到 Pointer Events，但**桌面鼠标行为逐条不变**：单指＝平移（沿用 zx/zy + clampView + applyView），
  双指＝按两指距离比只做增量 zoomAt(next / view.scale, 中点的容器坐标)，**不要直接改 view.scale**；
  pointercancel 必须与 pointerup 等效（否则触摸被打断会卡在拖动/双指态）；wheel 保留；
  点击阈值：鼠标 |dx|+|dy| ≤ 15，触摸 ≤ 24，500ms 不变；
- 手机布局只在 @media (pointer:coarse) / max-width:820px 内生效（桌面零像素变化）：面板全屏抽屉 + 13 Tab 横滚 +
  底部表格横滚 + 曲线 690→380 + 触摸目标 ≥40px；"📝 修改存档信息" 保留但要点得准；
- 手机默认 1× 并禁用切原生分辨率（点击给提示）；桌面默认 3×、可切原生，都不变；
- viewer.js 不许反引号、不许字面量 </script>；不许往数据面/rankings 加任何字段；
- 测试：verify-player-run.ts 新增手机节（①单指平移 ②双指按距离比缩放且锚点=两指中点 ③pointercancel 不卡
  ④触摸 20px 不弹面板而鼠标同距离弹 ⑤手机 1× 且原生禁用 ⑥第 6 节原样通过），frontend.test.ts 断言 viewer.html 有 viewport meta；
- 验收：pnpm verify:player + pnpm test 全绿；给我一份能自己动手验的手机测试目标（§5.6）。汇报先给结论。
```

---

### 4.4 任务 D（新对话 D）：重建 15 份存档与离线页

**触发条件：必须等 A、B、C（含 C2）三方全部报完成之后才能开工。** 产物是构建期烤死的，改完代码不重建就看不到任何变化；重建一次约 5~10 分钟/15 份，所以**只跑一次**。

**文件所有权**：不改任何源代码；只写 `.dev-storage/saves/<id>/viewer/**`（**唯一**允许写用户数据的任务，且**只重建、绝不删除**）。

**步骤**

1. **先备份**（动手之前）：`.dev-storage/index.json` 与 15 份 `meta.json` → `tmp/task4-backup/`。
2. 双击根目录 `修复存档数据.bat`（已 `--all`：每份都重建；跑前自动备份 index/meta，中文报告写 `tmp\修复存档数据.txt`）。它内部就是逐份跑
   `node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage`。
3. 逐份抽查，写进回报：
   - 每份 `viewer/data.json` 是 `{version:2,data,panels}`，且 `data.rankings` 存在、`data.countryDetail[某tag].rulers[0].flags` 存在；
   - 每份的「性格」列数据里：**不再有** `has_lowborn_consort` / `me_flag_*` 这类 flag（抽查 1~2 份的 `rulers[*].personalities` 与 `flags`）；
   - 名字：抽查一份，`personalities` 里的 `ancestor_*_personality` 在 `uiNames.personalities` 里有中文；
   - 离线页 `viewer/index.html` 里能搜到新逻辑（例如 `generalBody` / `data-sortable`），说明内联的是新 `viewer.js`；
   - `viewer/thumb.png` 都在。
4. 重建后比对备份：`custom` 字段与 `viewerData: true` 一个不少，`info` 六个键不退化。

**验收（D）**：15/15 重建成功；上面五项抽查逐条通过；回报里给出每份的耗时与结论、以及"以后重建还会不会踩同样的坑"。

**提示词 → 新对话 D（等 A/B/C 都报完再贴）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 第四对话任务书.md 的「4.4」——
你是本轮「任务 D：重建 15 份存档与离线页」的执行者。A/B/C 的代码改动都已合并，现在把结果烤进产物。

要点：
- 动手前先把 .dev-storage/index.json 与 15 份 meta.json 备份到 tmp/task4-backup/；
- 双击根目录 修复存档数据.bat（已 --all，每份都重建；中文报告写 tmp\修复存档数据.txt）；
  全程只重建、绝不删除用户数据；
- 抽查并把结论写进回报：①每份 data.json 是 {version:2,data,panels} 且 data.rankings 存在、
  rulers[*].flags 存在；②"性格"列不再出现 has_lowborn_consort / me_flag_* 这类 flag；
  ③ancestor_*_personality 有中文名；④离线页 index.html 能搜到 generalBody / data-sortable；
  ⑤viewer/thumb.png 都在；⑥custom 与 viewerData 不丢；
- 不许改任何源代码、交接.md、本任务书；汇报先给结论（重建了几份、五项抽查结果、耗时），不要叙述调试过程。
```

---

## 5. 验收：你自己动手的测试目标（用户亲自点）

> 前置：先双击 `启动网站.bat`（→ http://127.0.0.1:8788）。下面用 `b6263f5baf54`（俄罗斯 1574，主对话实测过的锚点）。
> **D 之前**看到的是旧产物（没有新榜、性格列仍是旧的）；**D 之后**才是最终效果。

### 5.1 C1（排序）——打开 `http://127.0.0.1:8788/viewer.html?id=b6263f5baf54`

| # | 点哪里 | 应该看到 |
|---|---|---|
| 1 | 底部「列强榜 · 前 15 名」→ 点表头**时代评分**一次 | 首行是 **撒丁-皮埃蒙特 SPI、2284.0**，表头出现 ↓；末行是波希米亚 219.0 |
| 2 | 再点**时代评分**一次 | 首行变成 **波希米亚 BOH、219.0**，末行是撒丁-皮埃蒙特 2284.0，表头箭头变 ↑ |
| 3 | 再点一次 | 回到第 1 步的样子（降序） |
| 4 | 「城市发展度排名」→ 点**发展度**一次 | 首行 **威尼斯 49**；再一次 → 首行 **维也纳 34**（34 那批里的第一行） |
| 5 | 把**每一列**都点一遍（含国名、Tag、首都、国教、月收入、总兵力） | 每次都先降序再升序；「#」列点不动、数字也永远是原来的 1..15 |
| 6 | 点地图上任意陆地省 → 省份面板 → 点「省份历史」表的**日期**表头一次 | 最新的事件排到最上面；再点一次回到最早在上 |
| 7 | 「转到国家」→ 国家面板**将领** Tab → 点**火**一次 | 火=6 的那几位（曼施坦因 / 穆斯霍夫 / 德夫林格）排到最前；再点一次火=6 的掉到最后 |
| 8 | 在国家面板换到**财政** Tab，再换回**将领** Tab | 之前点过的排序列与方向**还在**（不是重置） |
| 9 | 鼠标放在表头上 / 放到地图上 | 表头是手形；**地图仍是普通箭头**（没有抓手） |

### 5.2 C2（两张榜）——同一个页面往下滚

| # | 点哪里 | 应该看到 |
|---|---|---|
| 1 | 滚到「思潮发源地」下面 | 出现 **「历史十五个最优秀将军 · 综合评分」** 与 **「十五个最优秀君主 · 综合评分」** 两张表，各 ≤15 行 |
| 2 | 读将军榜的说明行 | 写着权重（能力 45% / 净杀伤 40% / 胜率 15%）与**本存档的战斗覆盖率**（"4958 个指挥战斗方中 2,3xx 个对上档案"这种，具体数字由 A 回报） |
| 3 | 点将军榜**综合评分**一次 / 再点一次 | 首行分别是最高分 / 最低分那条 |
| 4 | 点将军榜**参战**一次 | 参战场次最多的那位排到最前（与他在「将领」Tab 里的场次能对上；A 的回报里会给一位可手工核对的例子） |
| 5 | 点君主榜**发展度增量**一次 | 在位期间把国家发展度做大最多的那位排最前；每一位的「止」列要么是具体日期、要么是「在位」 |
| 6 | 君主榜里**不许**出现"在位 2000 多个月"的奥地利阿尔布雷希特 V，也**不许**出现无名或三维 0/0/0 的行 | 这是 §2.4 那三条剔除规则生效的证据 |
| 7 | 点君主榜**国家**一次 | 按国名中文排序（不是按 TAG） |
| 8 | 将军/君主榜点任意列 | 同样先降序再升序；「#」列始终是评分榜原始名次 |

### 5.3 B（特质汉化）

| # | 点哪里 | 应该看到 |
|---|---|---|
| 1 | 点地图上任意国家 → 国家面板 → **君主** Tab | 「性格」列里**看不到** `has_lowborn_consort`、`me_flag_*`、`just_suggested_new_adm_advisor` 这类英文标记 |
| 2 | 同上，「性格」列里带 `ancestor_*` 的条目 | 显示中文（不是 `ancestor_calm_personality` 这种原文） |
| 3 | 省份面板 → 贸易品一行 | 显示中文（不是 `grain` / `coal`） |
| 4 | 主对话会把 B 的自造译名清单整理给你逐条核对 | 你确认或提出修改 |

### 5.4 A（数据面，主对话代你核对）

主对话在 D 之后跑三闸门（`pnpm test` / `pnpm verify:player` / `pnpm verify:deploy`）并核对：`rankings.meta` 覆盖率、`generals[0]` / `monarchs[0]` 与榜单首行一致、`flags` 与 `personalities` 无交叉污染。

### 5.5 历史表 / 阶级表（第二轮追加，D 之后）

| # | 点哪里 | 应该看到 |
|---|---|---|
| 1 | 国家面板 → **历史** Tab，往下翻 | 不再出现 `conventicle_act`、`blasphemy_act`、`Intrigue_enable`、`azuki_recover_color`、`estate_burghers_comission_merchant_ships_for_war` 这类原始 key，改成汉化 mod 的中文名 |
| 2 | 同上，找「接受文化：…」「主文化改为 …」「国教改为 …」「政体改为 …」 | 显示中文（如「接受文化：诺夫哥罗德」「国教改为 新教」「政体改为 君主制」），不再有 `novgorodian` / `protestant` / `monarchy` |
| 3 | 国家面板 → **阶级** Tab → 每个阶级的「特权」表 | 不再出现带方括号的占位符（`[Root.GetNobilityOrFallbackName]整合政策`、`[Root.GetStateForm]之上的权力`、`授予[Root.Get市民Name]经济自由权`） |
| 4 | 同上，「影响力修正」表 | 不再出现全大写下划线 key（`REGENCY_SLUR`、`KOR_purged_faction`、`cestate_*`…） |
| 5 | 历史 Tab 里的战争名 | 可能看到「第二次 …之战」而不是「2nd …之战」（这一条是**可选**项，做了才有；回报里会写明做没做） |

### 5.6 C3（手机端只读适配；托管查看页即时生效，不必等 D）

用手机浏览器打开 `viewer.html?id=b6263f5baf54`（线上地址，或电脑 `启动网站.bat` 后用手机访问同一台机器的 8788 端口）：

| # | 操作 | 应该看到 |
|---|---|---|
| 1 | 打开页面 | **不再整体缩小成桌面版**；字大小正常、按钮点得准 |
| 2 | 单指按住地图拖动 | 地图跟手平移；**松手不会**弹出省份面板 |
| 3 | 双指捏合（放大/缩小） | 地图以**两指中点**为中心缩放；**页面本身不缩放**（浏览器缩放条不动、页面不跟着跑） |
| 4 | 手指按住不动 / 轻点陆地省 | 高亮跟随手指；**轻点一下**弹出该省详情面板 |
| 5 | 打开的面板 | 全屏铺开、能上下滚；13 个 Tab 能横向滑到最后一个；表格能横向滑 |
| 6 | 九个视图按钮 / 播放按钮 | 都能点准；播放默认 **1×**、流畅 |
| 7 | 分辨率按钮 | 点了只给提示「手机上不支持原生分辨率」，**不会**切到原生 |
| 8 | 回到电脑上重复第 2–3 步 | 滚轮缩放、按住拖动、hover 高亮、点一下弹面板——**与以前完全一样**（这是"C3 没改坏桌面"的证据） |

⚠️ 手机上打开**离线自包含页**（`index.html`）要等一次重建（任务 D 或 `修复存档数据.bat`）；**托管查看页即时生效**。

---

## 6. 全局纪律（每个任务都适用）

1. **不许碰 `.dev-storage/**`**（任务 D 除外，且只重建、绝不删除；动手前先备份）。
2. **不许改 `交接.md`**，也不许改别的任务名下的文件；需要改就写进回报，由主对话协调。
3. **反引号铁律**：`viewer.js`、`render-timeline.ts` 的模板内不许出现反引号；不许出现字面量 `</script>`。
4. **测试条数不要写死**：以 `pnpm test` 实跑输出为准。
5. **跑测试的环境**：本会话是 `danger-full-access`，可以直接跑；若换到 `workspace-write` 会 `spawn EPERM`（全红但没人改错东西），让用户放宽沙箱，别改代码绕过。
6. **汇报口径**：先给结论（做完什么 + 验证结果 + 关键数字），再列改了哪些文件、怎么验证的、还剩什么没做；不要叙述调试过程。
7. **口径不许自创**：评分公式、剔除规则、覆盖率的写法都以 §3.5 为准；要改先报主对话。

---

## 7. 收尾（主对话负责，不用派活）

A、B、C、D 全部报完成后，主对话做：

1. 端到端目视（§5.1 / §5.2 / §5.5 我自己先跑一遍，再交给你）；
2. 三闸门：`pnpm test` → `pnpm verify:player` → `pnpm verify:deploy`，全绿；
3. 核对关键数字：`rankings.meta` 覆盖率与两张榜的说明行一致；`flags` 不污染 `personalities`；`tmp/probe-untranslated*.cjs` 的「含 `[` 的名字 = 0 / 四个族未命中 = 0」；
4. 更新 `交接.md`（把本轮结果写进去、清理已完成条目、把"待办"缩到真实剩余）；
5. 把验收清单（§5）按最终数字**填实**后交给你确认；
6. 本任务书若再无待办，加归档横幅并说明。
