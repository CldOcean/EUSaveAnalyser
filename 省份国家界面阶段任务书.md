# EUSaveAnalyser · 省份国家界面阶段任务书（复刻 pdx-tools 的两个面板）

> **状态：✅ 全部完成并归档（2026-09-19）**——阶段 1（省份面板完全复刻＋国家骨架/波 1）、1.5（全屏命中/光标/名字表/旗帜尺寸）、2（建筑·州·阶级·顾问）、3（财政·点数·思潮徽章）均已交付，用户**人工验收通过**；国家面板 **13 个 Tab 全部可点**；10 份存档已用 `--all` 重建为新数据面。验收清单见 `tmp/验收-阶段{1,1.5,2,3}.md`；当前进度与遗留以 **`交接.md`** 为准，本文件转为**历史参考**（§11 的波次提示词、§12 的对话顺序仍可复用）。

> 2026-09-19 主对话定稿。**用户要求**：省份界面**尽可能完全复刻**；国家面板**尽可能完全复刻，可分多波次**；**每个阶段都要给出一份用户能自己动手验证的测试目标**。
> 配套权威文件：`第三对话任务书.md`（冻结契约 §1.3/§1.4/§1.5/§1.6/§1.7/§1.8/§1.9/§3.2）、`tmp/pdx-province-panel-report.md`（省份面板逐字段还原）、`tmp/details-*-probe.*`（国家面板探针）。
> 项目根：`F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser`。执行者开工前读 `交接.md` + 本文件 + 对应权威文件章节。

---

## 0. 怎么用这份文档

1. **4 个新对话并行开工**（S1 数据面 / S2 游戏文件表 / S3 素材 / U 面板），任务与提示词在 §6。它们**文件所有权严格互斥**（§5），可以真并行。
2. 国家面板分 **3 波**：波 1 六个 Tab（§6）、波 2 四个 Tab（§7）、波 3 两个 Tab ＋ 思潮徽章（§8）。每波由同一个 U 对话接着做，避免 `viewer.js` 被两个对话同时改。
3. **每个阶段结束，执行者必须交付 `tmp/验收-阶段N.md`**：一份给用户看的清单——"双击哪个 .bat、打开哪个页面、点哪里、应该看到什么具体值、哪里可能不对"。清单里的期望值**必须用只读探针在本机存档上算出来**，不许编。
4. 用户只看三样：①双击 `启动网站.bat` 打开页面点一点；②读 `tmp/验收-阶段N.md`；③不满意就在清单上批注（要/不要/改名/换位置）。**不需要开终端。**
5. 执行者的自测闸门（写进回报）：`pnpm test`、`pnpm verify:player`、`pnpm verify:deploy` 全绿（条数以实跑为准）。

---

## 1. 契约速查（不重复抄，指向权威处）

| 项 | 权威位置 |
|---|---|
| 省份面板构成（12 行 KV ＋ 4 张分组表、空值规则、tooltip、抽屉交互） | `tmp/pdx-province-panel-report.md` 表 1–表 3；本文 §3.1 有"完全复刻版"的最终行表 |
| 国家面板 13 Tab 构成与分档、中文本地化要点 | `第三对话任务书.md` **§1.9** |
| 数据面已有键（直接用） | `第三对话任务书.md` §1.2 |
| 数据面新增键 schema | `第三对话任务书.md` §1.3 ＋ 本文 §2（本阶段扩充） |
| DOM 契约（`#hover` / `#detail` / `#detailBack` / `#detailClose` / `#detailBody` / `#detailGotoCountry` / `#mapFull` / `#pageThemePanelOpacity`） | `第三对话任务书.md` §1.6 |
| 交互状态机（none / province / country、拖拽阈值 `|dx|+|dy|>15`、点海洋关闭） | `第三对话任务书.md` §1.7 |
| 全屏（`.player.screen`，保留 `.bar`） | `第三对话任务书.md` §1.8 |
| 素材命名 `/assets/ui/<kind>/<key>.png`、缺图退化 | `第三对话任务书.md` §1.4 ＋ 本文 §4（映射规则） |
| 离线自包含页与代码铁律（无反引号 / 扁平脚本 / 无 `${}` / 站点根绝对路径） | `第三对话任务书.md` §1.5 |
| pdx-tools 决策（不移植代码，只学构成） | `第三对话任务书.md` §0.1 |

---

## 2. 本阶段的数据面契约（在 §1.3 之上扩充，**名字与形状冻结**）

> 两份实现都要产出（`scripts/render-timeline.ts` 离线 ＋ `apps/site/public/viewer-build.js` 浏览器），**逐键一致**，缺数据给 `0/''/[]/null`，**不许缺键**。

**省份面板（S1 负责）**

```js
provinceBuildings:     { dict: string[], names: string[], rows: number[][], builders: string[][] }
                       // rows = [[provId, 建筑下标…]]；builders 与之一一对应（出资国 tag，可空串）
provinceClaims:        { dict: number[], rows: number[][] }   // 值＝tagIdx（claims 是裸 tag 列表）
provinceGreatProjects: { dict: string[], names: string[], rows: number[][] }   // null= 裸列表
provinceLatentTradeGoods: { dict: string[], rows: number[][] }
provinceImprove:       { dict: number[], rows: number[][] }   // country_improve_count：[[provId, tagIdx, 次数]…]（实测可为 -1）
provinceTerrain:       { dict: string[], names: string[], byId: number[] }     // -1 = 无；读 S2 的 provinceTerrain.json
provinceArea:          { dict: string[], names: string[], byId: number[] }     // 省→地区；读 S2 的 area.json
areaDetail:            { [areaIdx: string]: { states: { tagIdx: number, prosperity: number }[],
                                              investments: { tagIdx: number, icons: string[] }[] } }
                       // 省份面板第 2/3 张分组表；读 S2 的表 + 存档 map_area_data
provinceDevastation:   number[]   // 按省 id 下标（存档里只有 >0 才写，缺=0）
provinceTradeCompany:  number[]   // 0/1：active_trade_company
```

**国家面板（S1 负责，按波次交付，键名一次冻结）**

```js
countryDetail: { [tag: string]: {           // 只烤"有省份的国家"，约 1000–1400 个；键按 tags 顺序排序
  // ---- 波 1：总览 / 君主 / 将领 / 文化 / 宗教 / 历史 ----
  powers: [number, number, number],          // ADM/DIP/MIL 点数（countries/{TAG}/powers 裸 3 元表）
  treasury: number, debt: number, inflation: number,
  prestige: number, stability: number, powerProjection: number,
  innovativeness: number, corruption: number,
  governmentStrengthKind: string, governmentStrength: number,   // legitimacy/republican_tradition/devotion/meritocracy/horde_unity 取首个 >0；全 0 = 'native'
  government: string, governmentRank: number, governmentReforms: string[],
  development: number, rawDevelopment: number, autonomyPercent: number,
  cities: number, overextension: number, religiousUnity: number,
  absolutism: number | null,                 // 本存档 0 次出现 → null（面板显示 —「存档未记录」）
  mercantilism: number, splendor: number, tech: [number, number, number],
  envoys: { merchants: number, colonists: number, diplomats: number, missionaries: number },
  religion: string, primaryCulture: string, countryId: number,
  army: [number, number, number, number],    // 步/骑/炮/雇佣
  navy: [number, number, number, number],    // 重/轻/桨/运输
  manpower: number, reinforce: number, maxManpower: number,
  landMorale: number | null, navalMorale: number | null, professionalism: number,
  armyTradition: number, navyTradition: number,
  ideas: { group: string, unlocked: number, total: number }[],
  monarch: { name: string, dynasty: string, adm: number, dip: number, mil: number,
             age: number, culture: string, religion: string, inaugurated: string,
             personalities: string[] } | null,
  rulers: { name: string, start: string, end: string, months: number, personalities: string[],
            adm: number, dip: number, mil: number }[],
  failedHeirs: { name: string, birth: string, personalities: string[], adm: number, dip: number, mil: number }[],
  leaders: { name: string, kind: string, active: boolean, activation: string,
             fire: number, shock: number, maneuver: number, siege: number }[],
  bestGeneral: { name: string, fire: number, shock: number, maneuver: number, siege: number } | null,
  bestAdmiral: { …同上… } | null,
  religionDev: { [religionKey: string]: number },       // 宗教→发展度（可用 num_of_religions_dev）
  cultureStats: { culture: string, group: string, provinces: number, dev: number,
                  statedProvinces: number, statedDev: number }[],
  history: { date: string, ordinal: number, kind: string, text: string, tag?: string,
             provId?: number, payload?: number }[],     // 18 种事件，kind 见 §7/§2 注释
  // ---- 波 2：建筑 / 州 / 阶级 / 顾问 ----
  buildingCount: { building: string, provinces: number }[],
  states: { area: string, name: string, dev: number, capitalState: boolean,
            prosperity: number, prosperityMode: string, stateHouse: boolean }[],
  estates: { kind: string, loyalty: number, territory: number, agendas: number,
             privileges: { name: string, since: string }[],
             influences: { name: string, value: string, expires: string }[] }[],
  crownland: number,
  advisors: { id: string, name: string, date: string }[],
  // ---- 波 3：财政 / 点数 ----
  budget: { period: string, income: number[], expense: number[], … },   // 19/38 槽，槽位名来自 S2 的枚举表
  manaSpent: { adm: number[], dip: number[], mil: number[] },           // 46 槽
} }
```

---

## 3. 阶段与并行计划（总表）

| 阶段 | S1 数据面 | S2 游戏文件表 | S3 素材 | U 面板 | R 重建 | 用户验收产物 |
|---|---|---|---|---|---|---|
| **1** | 省份全量键 ＋ 国家波 1 键 | 地形表 / 地区表 / 中文名表 / 图标映射表 | 建筑·奇观·地形·宗教·思潮·政体·理念图标落位 | 外壳＋交互＋全屏＋面板拉条＋**省份面板完全复刻**＋国家面板骨架（Tab 切换）＋波 1 六个 Tab | — | `tmp/验收-阶段1.md` |
| **2** | 国家波 2 键（建筑统计/州/阶级/顾问）＋ 顾问表接入 | 州名与 area 表、顾问 id 表 | 波 2 需要的图标（州议会/议程/顾问头像） | 波 2 四个 Tab 渲染 | — | `tmp/验收-阶段2.md` |
| **3** | 国家波 3 键（ledger 19/38、mana 46） | **财政/点数槽位枚举**（唯一硬风险） | 财政/点数图标（可选） | 波 3 两个 Tab ＋ 思潮"已接纳 N/8"徽章 | — | `tmp/验收-阶段3.md` |
| **4** | — | — | — | — | 重建 10 份存档＋离线页 | `tmp/验收-阶段4.md`（我出） |
| **1.5** | S1 把名字表烤进数据面 | S2 产出完整名字表（含自造兜底＋清单） | — | U 修全屏命中偏移 ＋ 光标 | R 重建 10 份（让用户马上看得见） | `tmp/验收-阶段1.5.md` |

> **阶段 1.5 的由来**：用户 2026-09-19 验收阶段 1 时报了三个问题（全屏命中偏移／地图光标是抓手／文化·特质没汉化）。**决定：先修再往下走**——坐标偏移会让后面每波目视验收都不可信；"名字从哪来"不定死，波 2/波 3 的州名·阶级·特权·顾问·建筑都要回头补。详见 §6.8。

**并行关系**：阶段 1 的 4 个对话同时开工；S1/S2/S3 是 U 的上游，但**契约已冻结**，U 用占位数据先做外壳与交互，上游落地后再接真数据（不阻塞）。阶段 2/3 由**同一批对话继续**（文件所有权不变，避免冲突）。阶段 4 必须等前面全部完成。

---

## 4. 素材与游戏文件表的映射规则（速查，**结论已核实**）

| 类别 | 数量 | 怎么对上 key（**必须查 `.gfx`**） | 落位 |
|---|---|---|---|
| 建筑 | 47 key | `interface/building_icons.gfx` 的 `GFX_<key>` → texturefile（`marketplace`→`latin_marketplace.png`） | `assets/ui/buildings/<key>.png` |
| 奇观 | 139 key → 134 张图 | `interface/great_project.gfx` 的 `GFX_great_project_<key>`；**17 个文件名 ≠ key**（含 4 个大写） | `assets/ui/great_projects/<key>.png` |
| 宗教 | 28 张 64×64 | `religion_icons/<图标名>.png` ＋ **11 条别名**（theravada→buddhism、fetishist→shamanism、alcheringa→dreamtime、mayan→mesoamerican_religion、norse→norse_pagan_reformed、tengri→tengri_pagan_reformed、shia→shiite、sikh→sikhism、hindu→hinduism、confucian→confucianism、animist→animism） | `assets/ui/religions/<religionKey>.png` |
| 思潮 | 8 张 | 按 `countrytechnologyview.gfx` 声明序 0–7（feudalism / renaissance / new_world_i / printing_press / global_trade / manufactories / enlightenment / industrialization） | `assets/ui/institutions/<0..7>.png` |
| 地形 | 17 张 | `combat_terrain_*.dds`（省份窗口那套）；`glacier`→`arctic.png`、`coastline`→`inland_ocean.png`、`mountain`≡`desert_mountain`→`mountains.png` | `assets/ui/terrain/<类别名>.png` |
| 政体改革 | 346 张 | `common/government_reforms/*.txt` 里显式 `icon = "crown"` 字段（323/324 可用） | `assets/ui/government_reforms/<key>.png` |
| 理念/修正/科技/其它 | 30 ＋ 923 ＋ … | 文件名即 key（科技三系用 `icon_powers_*_{administrative,diplomatic,military}_tech.png`） | `assets/ui/ideas/`、`assets/ui/modifiers/`、`assets/ui/icons/` |
| 表格类（不进运行期映射） | — | `uiNames.json`（中文名：建筑/奇观/地形/宗教/政体改革…）、`area.json`（省→地区＋地区中文名）、`provinceTerrain.json`（省→地形）、`advisorIds.json`、`ledgerSlots.json`（19/38）、`manaSlots.json`（46） | `assets/ui/*.json` |

**命名铁律**：文件名＝小写 ASCII 的游戏 key（`[a-z0-9_]`，`-`→`_`）；**不保留游戏原目录结构**（原素材 115 个含大写、3 个含空格、8 组大小写撞名，部署到 Linux 直接 404）；客户端**不做任何 name→path 映射**，缺图 `onerror` 退化成带文字的小方块。

---

## 5. 文件所有权（严格互斥）

| 对话 | 只能改 | 绝对不许改 |
|---|---|---|
| **S1 数据面** | `packages/eu4-parser/src/**`、`packages/eu4-parser/test/**`、`apps/site/public/viewer-build.js`、`viewer-data.js`、`viewer-store.js`（只许加入参）、`viewer-page.js`（只许加载入参）、`scripts/render-timeline.ts`、`apps/site/test/viewer-*.test.ts`；重建 `tmp/timeline/*` | `viewer.js`、`viewer.html`、`style.css`、`page-theme.js`、`verify-player-run.ts`、`assets/**`、`交接.md` |
| **S2 游戏文件表** | `scripts/export-ui-tables.ts`（新）、`scripts/lib/game-ui-tables.ts`（新，可选）、`apps/site/public/assets/ui/*.json` | `assets/ui/**/*.png`、数据面文件、面板文件 |
| **S3 素材** | `scripts/export-ui-assets.ts`（新）、`apps/site/public/assets/ui/**/*.png`、`scripts/verify-deploy.ts` | 任何 `.json` 表、数据面文件、面板文件 |
| **U 面板** | `apps/site/public/viewer.html`、`viewer.js`、`style.css`、`page-theme.js`、`scripts/verify-player-run.ts`、`apps/site/test/frontend.test.ts`、`packages/eu4-parser/test/build-guards.test.ts` | S1/S2/S3 名下的文件 |
| **R 重建** | `.dev-storage/**`（只重建、绝不删除） | 一切源代码 |

> `tmp/` 是草稿区，任何人都可以在里面写只读探针与验收清单；`.dev-storage/` 只有 R 能碰。

---

## 6. 阶段 1：外壳 ＋ 省份面板完全复刻 ＋ 国家面板骨架与波 1

### 6.1 对话 S1：数据面（省份全量 ＋ 国家波 1）

- **解析层**：`extractProvince()` 现在只认 `cores`/`institutions`，其它子块全丢（这是建筑/奇观/宣称拿不到的根因）。补上 `claims`、`latent_trade_goods`、`buildings`、`building_builders`、`great_projects`；把 `devastation`、`active_trade_company` 提成一等字段（现在散在 `extra` 里）。加单测。
- **数据面**：按 §2 产出省份全量键 ＋ 国家波 1 键；两份实现逐键一致；给 `viewer-build.test.ts` 补新键断言。
- **读 S2 的表**：`provinceTerrain.json`（省→地形）、`area.json`（省→地区＋中文名）、`uiNames.json`（中文名）。离线面 `readFileSync`，浏览器面 `fetch` 后作为入参（**只许给 `buildViewerData()` 加入参、给 `viewer-store.js#buildForSave` 与 `viewer-page.js` 加一行加载**；`loadStoredSave`/`DATA_PATH` 一个字都不许动）。
- **不许现算地形**：地形唯一来源是 S2 的表。
- **闸门**：`pnpm timeline`（重生成参照）→ `pnpm test` 全绿并贴出"两份数据面逐键一致"的输出；报出各新键的覆盖数（省数/国家数）。

### 6.2 对话 S2：游戏文件表（U 与 S1 的上游）

- 产出 `apps/site/public/assets/ui/` 下 6 份 JSON（格式见 §2/§4）：
  1. `provinceTerrain.json`：**算法已冻结且已跑通**——用 `scripts/lib/map-assets.ts#loadProvincePixels` 拿每像素省 id；解析 `map/terrain.bmp`（8bpp、调色板在 `14+biSize`、stride 4 字节对齐、自下而上）得每像素索引；`map/terrain.txt` 的 `terrain = {}`（`名字 = { type = <类别> color = { <索引> } }`）得索引→类别；逐省多数票；**再套 `categories.<类别>.terrain_override = { 省 id… }`（2336 省，优先级最高）**。只读探针 `tmp/terrain-derive-probe.ts` 可直接复用（实测 18 个类别全部有值）。
  2. `area.json`：省→地区映射来自游戏 `map/area.txt`（48 KB，约 728 个 area）；地区中文名来自汉化 mod `localisation/areas_regions_l_english.yml`（如 `brittany_area:0 "布列塔尼"`）。
  3. `uiNames.json`：建筑/奇观/地形/宗教/政体改革/理念的中文名。**地形中文名游戏里没有**（只有英法德西四语）→ 按本任务书 §6.5 的对照表自造，其余从 localisation 取。
  4. `advisorIds.json`：`common/advisors/*.txt` 的全量顾问 id → 中文名。
  5. `ledgerSlots.json`（income 19 / expense 38）与 6. `manaSlots.json`（46 槽）：**这是本阶段唯一硬风险**——槽位→科目的枚举在上游 Rust `eu4save` crate 里，本机 pdx-tools 副本查不到。两条路：①从 `codeload.github.com/rakaly/eu4save/tar.gz/refs/heads/master` 取上游源码对照（本机 `web_fetch` 被沙箱挡，但 `codeload`/`api.github.com`/`jsdelivr` 可用）；②用一个数值已知的存档反推校准。**若两条都走不通，如实报"波 3 暂缓"**，不要编造槽位名。
- **闸门**：写一份 `tmp/ui-tables-report.md`：每个表条数、抽查 5 条（key→中文名/省 id→地形）、与游戏文件对照的证据；并自测 `assets/ui/*.json` 能被 `node -e "JSON.parse(...)"` 正常读出。

### 6.3 对话 S3：素材落位

- 按 §4 把 PNG 落位到 `apps/site/public/assets/ui/**`，子集约 **234 文件 / ≈11.4 MB**（建筑 47、奇观 134、地形 17、宗教 28、思潮 8，外加政体改革 346 与理念/修正/图标若干）。
- 奇观是体积大头（9.93 MB）：**先按原图落位**，若 `verify:deploy` 体积报警再问主对话要不要转 WebP/缩图。
- `scripts/verify-deploy.ts`：把 `assets/ui` 加进 `REQUIRED_DIRS`，加"每个子类目录非空 + 文件数 < 20000"的检查，报出文件数与体积。
- **闸门**：`pnpm verify:deploy` 全绿；覆盖报告（每类有多少 key、多少拿到图、缺哪些）。

### 6.4 对话 U：外壳 ＋ 交互 ＋ 省份面板 ＋ 国家骨架与波 1

**第一段（不等任何人，契约已冻结）**
1. `viewer.html`：按 §1.6 加入 `#hover` 与 `#detail` 骨骼与全部 CSS（四主题适配）；半透明背景单独一层（`#detail::before` ＋ `--panel-opacity`），文字不受透明度影响。
2. `page-theme.js`：在「蒙版」下面加「面板」拉条（`#pageThemePanelOpacity`，默认 88、范围 20–100，写 `settings.panelOpacity`），**只在 `mountPageTheme({ detailPanel: true })` 时渲染**；保持"扁平脚本"（无 import、无顶层 await）。
3. `viewer.js`（**零反引号**）：`#hover` 随 `applyView()` 同变换；hover 命中省 → 高亮（缓存，海洋不清不画）；点击/拖拽消歧 `|dx|+|dy| > 15`；§1.7 的三态状态机逐行实现；面板开合、返回文案（「✕ 关闭省份界面」/「← 返回到 - <省名>」）、`stopPropagation`；全屏（§1.8，保留 `.bar`）。
4. `verify-player-run.ts`：DOM 桩支持新元素与坐标事件；**新增第 14 节**断言 hover/点击/海洋关闭/拖拽不改选中/转到国家/返回目标/全屏保留工具栏/拉条生效；`build-guards.test.ts` 与 `frontend.test.ts` 补新 id 的静态断言。

**第二段：省份面板完全复刻（行表冻结）**
| 位置 | 行 | 数据来源 |
|---|---|---|
| 标题 | `{省 id}: {省名}` | `provinceIds` / `provinceNames` |
| KV 1 | `发展度：税 / 产 / 兵` | 客户端回放数组（随当前帧） |
| KV 2–3 | `拥有者：` / `占领者：` | `owner[]` / `controller[]`（≠ 时才醒目） |
| KV 4–5 | `核心：` / `宣称：` | `provinceCores` / `provinceClaims`（国旗列表，空则不显示） |
| KV 6–7 | `宗教：` / `文化：` | 回放值 ＋ `religions`/`cultures` 字典 ＋ 图标 `assets/ui/religions/*` |
| KV 8 | `荒废度：` | `provinceDevastation` |
| KV 9–10 | `贸易品：` / `潜在贸易品：` | 字典 ＋ `provinceLatentTradeGoods` |
| KV 11 | `属于贸易公司：是/否` | `provinceTradeCompany` |
| KV 12 | `建筑：`（图标网格，一行 4 个、满行换行、每张配中文图注） | `provinceBuildings`（＋`builders` 做 tooltip） |
| 增行 | `地形：`（中文名 ＋ 配图）、`奇观：`（图标＋名称）、`思潮：`（已接纳图标行）、`归属地区：` | `provinceTerrain` / `provinceGreatProjects` / `provinceInstitutions` / `provinceArea` |
| 表 1 | `{地区名} 州`：国家 ｜ 繁荣度 | `areaDetail[area].states` |
| 表 2 | `{地区名} 贸易公司投资`：国家 ｜ 投资（空则整表不渲染） | `areaDetail[area].investments` |
| 表 3 | `扩建基础设施`：国家 ｜ 次数（值可为 -1） | `provinceImprove` |
| 表 4 | `省份历史`：日期 ｜ 事件 ｜ 图形（易主→国旗、建造/拆除→建筑图标；**再加一行"宗教/文化变更"**——这是我们比它多的）；每条带「把地图切到那天」按钮 | 客户端过滤 `provinceInit`＋`provinceEvents` ＋ `goTo()` |
| 按钮 | 「转到国家」 | `owner[id]` → 国家面板 |

**第三段：国家面板骨架 ＋ 波 1 六个 Tab**：Tab 切换（总览/君主/将领/文化/宗教/历史 ＋ 后续波次的占位禁用态）；总览按 §1.9 表 1b 的 3 列×8 行 ＋ 使者行 ＋ 宗教/主文化/国家 ID ＋ 军事卡 ＋ 君主卡 ＋ 理念卡 ＋ 战争区 ＋ 外交区；其余五个 Tab 按 §1.9 的构成渲染。空值按"分组级空→整组不渲染，单元格级空→`—`"。

### 6.5 【你能自己验的测试目标 → 阶段 1】

> 执行者必须把这份清单落成 `tmp/验收-阶段1.md`，并把**期望值用探针在本机存档上算出来**填进去。下面是主对话已实测的锚点，可直接用。

**A. 外壳与交互（任何存档）**
1. 双击 `启动网站.bat` → 打开 `http://127.0.0.1:8788/viewer.html?id=990e9f3381e8`。
2. 鼠标划过陆地省 → 该省高亮；**划过海洋 → 什么都不发生**（不做任何处理）。
3. 按住左键拖动地图 → 地图平移，**不弹面板**；松手后再单击某个省 → 面板打开。
4. 点海洋 → 如果面板是开的，**直接关闭**（不经过省份面板）。
5. 点 `⛶ 全屏` → 地图铺满整屏、**下方工具栏仍在**；再点一次或按 ESC 退出。
6. 点右下角 🎨 → 里面有「面板」拉条（在「蒙版」下面）；拖动它 → 两个面板的**半透明背景**跟着变，文字始终清晰；四套主题都要看一遍。

**B. 省份面板（在 10 份存档任意一份上点一个省）**
7. 点 **1 号省** → 标题显示 `1: 斯德哥尔摩`；**地形行应显示"草原"** ＋ 那张 `combat_terrain_grasslands` 配图（主对话用探针实测：省 1 = grasslands）。
8. **建筑行应出现 4 个图标：市场 / 工场 / 神庙 / 船坞**（存档里省 1 的 `buildings = marketplace workshop temple shipyard`，已实测），每张下面有中文图注；换成一个建筑少于 4 个的省，应能看出"一行 4 个、满了换行"。
9. 点 **4 号省** → 地形应显示"森林"；点 **6 号省** → "农田"（已实测：4=forest、6=farmlands）。
10. 拖动时间轴到很早期再看同一个省 → 发展度/拥有者/宗教随帧变化（**这是我们比 pdx-tools 强的部分**：它是存档当日快照）。
11. 省份历史表里点某条记录的「把地图切到那天」图标 → 时间轴跳到那天，再点一次回到当前帧。
12. 面板里的「转到国家」按钮 → 国家面板打开，返回按钮文案含该省名。

**C. 国家面板波 1（**用 `mp_俄罗斯1574_11_12` 这份存档验收，期望值已实测**）**
13. 打开该存档的查看页 → 点俄国任一省份 → 转到国家；或直接点列强榜里的俄国。
14. **总览**应能看到（与存档标量一致，允许 ±1% 的显示误差）：威望 **84**、稳定 **1**、正统性 **89**、腐败 **1.17**、重商主义 **100**、陆军传统 **65**、海军传统 **30**、通胀 **2.7%**、人力约 **97K**、水手约 **14,887**、城市数 **373**、政体 **俄罗斯君主制（russian_monarchy）**、科技 **12 / 13 / 13**、附属国 **RIG·VOL·SME**、宿敌 **法国·英国·波斯**、正在交战 **波斯·英国**。
15. **专制主义**那一格应显示 `—` 并注明"存档未记录"（本存档确实没有这个键，实测 0 次出现）——这不是 bug。
16. **君主卡**应显示当前君主的姓名、王朝、ADM/DIP/MIL 与年龄；**理念卡**每条理念 7 个 pip，实心＝已解锁。
17. **外交区**里空的关系行（如"分封""普洛尼亚"）**整行不出现**，不是显示空白。
18. **历史** Tab 有时间流，每条带「把地图切到那天」；点开能跳时间轴。
19. 在省份面板点任何一个国旗 → 切到那个国家的国家面板（pdx-tools 的交互：国旗就是入口）。

**D. 阶段闸门（执行者贴结果）**：`pnpm test`、`pnpm verify:player`、`pnpm verify:deploy` 全绿；离线自包含页（`生成查看页.bat` 生成的那份）双击也能用面板。

### 6.6 可直接粘贴的提示词 → 四个新对话

**→ 新对话 S1（数据面）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、第三对话任务书.md（§1.3/1.5/1.9/3.2）、省份国家界面阶段任务书.md（§1、§2、§5、§6.1）——
你是「阶段1·S1：数据面（省份全量＋国家波1）」的执行者。

要点：
- 只改 packages/eu4-parser/src/**、packages/eu4-parser/test/**、apps/site/public/viewer-build.js、viewer-data.js、
  viewer-store.js（只加可选入参）、viewer-page.js（只加加载入参）、scripts/render-timeline.ts、apps/site/test/viewer-*.test.ts；
  别动 viewer.js / viewer.html / style.css / page-theme.js / verify-player-run.ts / assets/** / 交接.md；
  loadStoredSave 与 DATA_PATH 的判据一个字都不许动；
- 根因：document.ts 的 extractProvince() 只认 cores/institutions，其它子块（claims / latent_trade_goods / buildings /
  building_builders / great_projects）全被丢掉；devastation 与 active_trade_company 现在散在 extra 里，要提成一等字段；
- 按阶段任务书 §2 的冻结 schema 产出「省份全量键 ＋ 国家波1键」；两份数据面（render-timeline.ts 与 viewer-build.js）
  逐键一致、不许缺键、countryDetail 的 tag 键要排序；
- 中文名 / 地形表 / 地区表都读 apps/site/public/assets/ui/*.json（另一个对话 S2 在产出；你先按格式写读取逻辑，
  离线面 readFileSync、浏览器面 fetch 后作为入参传入，别自己现算地形）；
- 改完跑 pnpm timeline 重新生成 tmp/timeline/data.json，再跑 pnpm test；给新键补 viewer-build.test.ts 的断言；
- 先跑 tmp/province-block-probe.ts、tmp/panel-shape-probe.ts、tmp/terrain-derive-probe.ts 看现成数据形状，别发明字段名；
- 汇报先给结论（各新键覆盖数、两份数据面一致的测试输出、测试结果），不要叙述调试过程。
```

**→ 新对话 S2（游戏文件表）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、第三对话任务书.md（§1.4/1.5/3.2）、省份国家界面阶段任务书.md（§4、§5、§6.2）——
你是「阶段1·S2：游戏文件表」的执行者。

要点：
- 只新增/修改 scripts/export-ui-tables.ts、apps/site/public/assets/ui/*.json；
  别碰任何 .png（S3 的）、数据面文件、面板文件；
- 产出 6 份 JSON：provinceTerrain.json（算法已跑通，直接复用 tmp/terrain-derive-probe.ts：loadProvincePixels 拿每像素省 id
  ＋ terrain.bmp 的 8bpp 调色板索引 ＋ terrain.txt 的 terrain{} 得类别 ＋ 逐省多数票 ＋ categories.*.terrain_override 优先）、
  area.json（游戏 map/area.txt ＋ 汉化 mod 的地区中文名）、uiNames.json（建筑/奇观/地形/宗教/政体改革中文名；地形中文名游戏里
  没有，按阶段任务书 §6.5 的对照自造）、advisorIds.json、ledgerSlots.json（income 19 / expense 38）、manaSlots.json（46 槽）；
- ledger / mana 槽位枚举是本阶段唯一硬风险：上游在 Rust eu4save crate。本机 web_fetch 被沙箱挡，但 codeload.github.com /
  api.github.com / cdn.jsdelivr.net 可用（例：Invoke-WebRequest
  https://codeload.github.com/rakaly/eu4save/tar.gz/refs/heads/master -OutFile r.tgz）；取不到就用已知数值的存档反推校准；
  两条都不通就如实报「波 3 暂缓」，**不许编造槽位名**；
- 交付 tmp/ui-tables-report.md：每张表的条数、抽查 5 条、与游戏文件对照的证据；
- 汇报先给结论（6 张表条数、槽位枚举是否拿下、风险），不要叙述调试过程。
```

**→ 新对话 S3（素材落位）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、第三对话任务书.md（§1.4/1.5/3.2）、省份国家界面阶段任务书.md（§4、§5、§6.3）——
你是「阶段1·S3：素材落位」的执行者。素材原始件在 tmp/eu4-ui-assets/（只读）。

要点：
- 只新增/修改 scripts/export-ui-assets.ts、apps/site/public/assets/ui/**/*.png、scripts/verify-deploy.ts；
  别碰 assets/ui/*.json（S2 的表）、别碰数据面与面板文件；
- 按阶段任务书 §4 的映射规则落位，**每一类都必须查 .gfx，不能靠猜文件名**：
  建筑 47 key（building_icons.gfx 的 GFX_<key> → texturefile）、奇观 139 key → 134 张（great_project.gfx；17 个文件名≠key，
  含 4 个含大写）、宗教 28 张 ＋ 11 条别名、思潮 8 张按声明序 0–7、地形 17 张（combat_terrain_*；glacier→arctic.png、
  coastline→inland_ocean.png）、政体改革 346 张（common/government_reforms 里显式 icon 字段）、理念/修正/科技/其它图标；
- 文件名必须是小写 ASCII 的游戏 key；不保留游戏原目录结构；客户端不做 name→path 映射；
- verify-deploy.ts 加 assets/ui 的必需目录、子类非空、文件数 < 20000 的检查，报出文件数与体积；
  奇观 9.93 MB 是体积大头，要削（转 WebP / 缩图）先问主对话；
- 汇报先给结论（各子类覆盖数、缺失清单、文件数与体积、verify:deploy 结果），不要叙述调试过程。
```

**→ 新对话 U（面板：外壳 ＋ 交互 ＋ 省份面板完全复刻 ＋ 国家骨架与波 1）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、第三对话任务书.md（§0/1.5/1.6/1.7/1.8/1.9）、省份国家界面阶段任务书.md（§2/§5/§6.4/§6.5）——
你是「阶段1·U：面板外壳·交互·省份面板·国家骨架与波1」的执行者。
目标：**把 pdx-tools 的省份面板与国家面板尽可能完全复刻**（构成照它，代码自己写，全中文）。

要点：
- 只改 apps/site/public/viewer.html、viewer.js、style.css、page-theme.js、scripts/verify-player-run.ts、
  apps/site/test/frontend.test.ts、packages/eu4-parser/test/build-guards.test.ts；数据面与素材是别的对话的，别动；
- 三段推进：①外壳与交互——#hover / #detail / #detailBack / #detailTitle / #detailClose / #detailBody / #detailGotoCountry /
  #mapFull / #pageThemePanelOpacity；交互照第三对话任务书 §1.7 的状态机逐行实现（hover 只对陆地、点海洋直接关闭、
  |dx|+|dy|>15 才算拖拽、返回按钮文案、转到国家、当前帧的拥有者）；全屏照 §1.8（保留 .bar）；
  ②省份面板完全复刻（行表见阶段任务书 §6.4 第二段：12 行 KV ＋ 地形/奇观/思潮/归属地区 增行 ＋ 4 张分组表）；
  ③国家面板骨架（Tab 切换）＋ 波 1 六个 Tab（总览/君主/将领/文化/宗教/历史，构成见第三对话任务书 §1.9）；
- 上游数据没到就先用占位数据，**不要自己造数据面**；空值规则：分组级空 → 整组不渲染，单元格级空 → `—`；
- 铁律：viewer.js 零反引号、page-theme.js 扁平脚本、viewer.html 无 ${} 插值、素材路径站点根绝对；
- **必须产出 `tmp/验收-阶段1.md`**：把阶段任务书 §6.5 的清单落成"双击什么 → 打开什么 → 点哪里 → 应看到什么值"，
  期望值用只读探针在本机存档上算出来（§6.5 里主对话已给的锚点可直接用：省 1 = 草原 ＋ 市场/工场/神庙/船坞、
  省 4 = 森林、省 6 = 农田；俄国 1574 的威望 84 / 正统 89 / 腐败 1.17 / 重商 100 / 陆军传统 65 / 海军传统 30 /
  通胀 2.7% / 城市 373 / 科技 12-13-13 / 附属 RIG·VOL·SME / 宿敌 法国·英国·波斯 / 专制主义显示 —）；
- 验收：pnpm test 与 pnpm verify:player 全绿（条数以实跑为准），并给出一份离线自包含页的目视结果；
- 汇报先给结论（做哪些交互、断言在第几节、验收清单的期望值、目视结果），不要叙述调试过程。
```

### 6.7 后续波次怎么接（同一个对话继续，不另开）

**完整提示词在 §11**（波 2 四段、波 3 三段、阶段 4 一段），照那张表复制即可。三条规矩：
1. **同一个对话继续贴**，不要为新波次新开对话（新开会撞文件所有权）；
2. 每波顺序：先 S2（表/枚举）与 S1（数据键），U 同时按冻结键名写渲染（占位数据不阻塞）；
3. 每波收尾由 **U 汇总**产出 `tmp/验收-阶段N.md`；S1/S2/S3 在回报里给出"可验项 ＋ 用探针算出的期望值"，由 U 一并写进清单。

---

## 6.8 阶段 1.5：阶段 1 验收发现的三个问题（**先修再往下走**）

> 用户 2026-09-19 在阶段 1 验收时报了三个问题：①全屏模式下选中的省份与鼠标有偏移；②地图上鼠标是"抓手"，不舒服，要统一成普通指针；③文化、特质这些没有中文。
> **决定：现在修，不拖到最后。**（①坐标偏移会让后面每一波的目视验收都不可信；③名字是**数据面缺名字**，波 2/波 3 的州名/阶级/特权/顾问/建筑全要名字，现在把"名字从哪来"定死；②顺手一行 CSS。）

### 6.8.1 U：全屏命中坐标 ＋ 光标

**问题 ①（真 bug）**：全屏模式下 hover/点击命中的省份与鼠标位置有偏移。
**根因与修法（照这个改）**：命中必须用**画布自己**的 `getBoundingClientRect()`——它已经包含 CSS transform 的平移与缩放——把客户端坐标直接映射到画布像素：

```
const rect = canvas.getBoundingClientRect();          // 每次事件现取，不许缓存
const x = Math.floor((clientX - rect.left) / rect.width  * canvas.width);
const y = Math.floor((clientY - rect.top)  / rect.height * canvas.height);
```

**不要再叠加或减去 `zx/zy/zScale`、也不要用 `canvas.clientWidth/canvas.width` 换一次比例**——那等于把变换算了两次；平时看不出来，进出全屏时 `clampView()` 改了 `zx/zy`，偏移就显出来了。
另外：进出全屏后要 `clampView(); applyView();`，并清掉 hover 高亮缓存、按新 rect 重算当前 hover。

**问题 ②**：`.view` / `.view canvas` 的 `cursor` 统一成 `default`，删掉 `grab`、`grabbing` 与 `.dragging canvas{cursor:grabbing}` 三条规则；拖拽时也不换指针。`touch-action:none` 保留；面板内按钮/链接保持 `pointer`。

**断言（`scripts/verify-player-run.ts` 新一轮）**：用两个**不同的合成 rect**（普通 ＋ "全屏"尺寸）断言**同一个屏幕点**算出**同一个省 id**；再断言 `.view` 的 cursor 规则里不含 `grab`/`grabbing`。

### 6.8.2 S2：名字表（这才是"没有汉化"的真正原因）

**先回答用户的疑问**：**不需要重新上传汉化目录**。原因是**这些名字从来没被烤进数据面**——地图的文化视图只用颜色、不显示名字，所以一直没暴露。汉化 mod 其实是有的，主对话已实测（显式 UTF-8 读）：

| 词条 | 汉化 mod `2976470733` | 本体 |
|---|---|---|
| 文化 `swedish` | ✅ `text_l_english.yml` | `Swedish` |
| 建筑 `marketplace` | ✅ `text_l_english.yml` | `Market Place` |
| 政体改革 `TSARDOM`（**大写键**） | ✅ `government_names_l_english.yml` | `Tsardom` |
| 地区 `brittany_area` | ✅ `areas_regions_l_english.yml` | `Brittany` |
| 兵种 `muscovite_musketeer` | ✅ `text_l_english.yml` | Offensive Eastern Musketeers |
| 特质 personalities | ⚠️ **键名待查清**（`scholar:` 这种写法在 mod 与本体里都搜不到；`personalityoptions_l_english.yml` 是事件选项，不是君主特质） | — |

**要求**
1. **先探针**：列出既有链能覆盖哪些 key 族（`packages/eu4-parser/src` 里现成的 localisation 加载器 ＋ 汉化 mod 目录 `…\236850\2976470733\localisation` ＋ 本体 `localisation`）；**特质的 key 形态必须查清**（去 `common/ruler_personalities/` 找定义，再回 localisation 找对应键），不要猜。
2. **扩充 `apps/site/public/assets/ui/uiNames.json`**，至少覆盖：`cultures` / `personalities` / `governmentReforms` / `buildings` / `greatProjects` / `terrain` / `religions` / `institutions` / `ideaGroups` / `advisors` / `areas` / `units`。
3. **没有中文的条目自造**，并把**全部自造条目写成 `tmp/自造译名清单.md`**（key ｜ 英文 ｜ 拟译名 ｜ 依据）——**用户要求主观译名全部列出来给他核对**，别默默造。
4. **铁律**：这些 yml 是 **UTF-8（带 BOM）**，读取必须显式 UTF-8；**控制台（cp936）里中文显示成乱码是正常现象，不许据此判断"没有中文"**——上一轮差点因此漏掉。

### 6.8.3 S1：把名字烤进数据面

- 读 S2 的 `uiNames.json`，产出 `DATA.uiNames`（一个大对象，键族同上）或按字典对齐的 `cultureNames` / `personalityNames`；**面板只在运行期读数据面，不许在运行期查文件**（离线页读不到）。
- **宗教名已有 `RELIGION_FALLBACK` 兜底链，不要做第二套**；文化与特质按同样思路补齐。
- 两份数据面（`render-timeline.ts` / `viewer-build.js`）逐键一致，`viewer-build.test.ts` 补断言；`pnpm timeline` 重生成参照。
- 回报里给一张表：面板会显示的每类名字（文化/特质/政体改革/建筑/奇观/地形/思潮/理念/顾问/地区），逐项标注「来自汉化 mod / 自造 / 缺失」。

### 6.8.4 R：重建 10 份存档

- 名字表与坐标修复进代码后**必须重建**才能看见：双击 `修复存档数据.bat`（跑前备份 `.dev-storage/index.json` 与 10 份 `meta.json`；只重建、绝不删除）。
- 顺带重建一份离线自包含页（`生成查看页.bat`）确认离线页也有中文。

### 6.8.5 【你能自己验的测试目标 → 阶段 1.5】

1. **全屏坐标**：进全屏 → 鼠标停在某个省上 → 高亮的就是鼠标底下那个省（放大到 2–4 倍再试，仍然准）；点它 → 面板标题就是那个省；退出全屏再点同一位置，结果一致。
2. **光标**：鼠标在地图上移动是普通箭头（不是小手/抓手）；按住拖动时也是普通箭头；移到按钮上是手型。
3. **汉化**：点任意存档的省份 → 「文化」显示中文（如"瑞典"而不是 `swedish`）；转到国家 → 君主卡里的**特质**是中文；政体、建筑、地形、地区、思潮、理念、顾问都应是中文；**没有中文的条目按 §6.8.2 的清单逐条对照**（核对 `tmp/自造译名清单.md`）。
4. 若某条确实是空的：面板显示**原 key 或 `—`**，并且能在清单里查到"为什么没有"。
5. **旗帜尺寸（省份面板）**：点一个省 → 「拥有者」那面旗与**目录页卡片主角旗一样大**（48×48 方框、4px 圆角、细边框、`object-fit:contain`）；「核心」「宣称」里的每一面旗也是这个大小，多面旗**换行、不缩回去**；殖民地是"宗主旗 ＋ 右半色块"。
6. **旗帜＋国名（国家面板）**：「总览」Tab 最上面能看到 **旗或国家娘（同样 48×48）＋ 国名**，国名字号与卡片上的国名一致。

### 6.8.6 可直接粘贴的提示词 → 阶段 1.5

**→ U 继续（坐标 ＋ 光标 ＋ 旗帜尺寸）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

阶段 1 验收发现的问题，现在修，见 省份国家界面阶段任务书.md §6.8.1 与 §6.8.7：
①全屏模式下选中的省份与鼠标位置有偏移（真 bug）；②地图上的鼠标是"抓手"，用户要求统一成普通指针；
③面板里的旗帜/国家娘太小（用户原话：大小跟目录页卡片主要国家的旗帜一致）；④国家面板「总览」里要加"旗＋国名"。

要点：
- 只改 apps/site/public/viewer.html、viewer.js、style.css、scripts/verify-player-run.ts（别动数据面与素材）；
- 命中改用 canvas.getBoundingClientRect() 直接映射到画布像素：x=(clientX-rect.left)/rect.width*canvas.width，
  y=(clientY-rect.top)/rect.height*canvas.height；**不要再叠加/减去 zx/zy/zScale 或 clientWidth 比例**（那是算了两次变换，
  进出全屏 clampView 改了 zx/zy 之后偏移才显出来）；rect 每次事件现取不许缓存；进出全屏后 clampView()+applyView() 并清 hover 缓存；
- cursor：.view/.view canvas 统一 default，删掉 grab/grabbing 与 .dragging canvas{cursor:grabbing}；touch-action:none 保留；
  面板内按钮保持 pointer；
- 旗帜尺寸照 style.css 第 118–123 行那套（目录页卡片主角旗 = 48×48 方框、4px 圆角、1px var(--border-soft) 边框、
  background var(--panel2)、object-fit:contain；.flagBox/.flagTint 三行是两页共用的冻结结构，殖民地＝宗主旗＋右半色块）：
  省份面板的 拥有者/占领者/核心/宣称 与小按钮旁的旗全部换成这个尺寸（多面旗换行不缩小）；
  国家面板「总览」Tab 最上面加一块 旗(48×48)＋国名；四主题都要好看；不要写死颜色；
- verify-player-run.ts 加断言：两个不同合成 rect（普通/全屏）下同一个屏幕点算出同一个省 id；cursor 规则里不含 grab/grabbing；
  面板旗尺寸＝48×48（或断言与卡片用同一个类）；总览顶部存在"旗＋国名"块；
- 产出 tmp/验收-阶段1.5.md（§6.8.5 的第 1、2、5、6 条写成"怎么点、应看到什么"）；
- 验收：pnpm test 与 pnpm verify:player 全绿（条数以实跑为准）。汇报先给结论。
```

**→ S2 继续（名字表）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

用户反馈"文化和特质没有汉化"。**结论已定：不用重新上传汉化目录**——是这些名字从来没被烤进数据面（地图文化视图只用颜色）。
见 省份国家界面阶段任务书.md §6.8.2；主对话实测汉化 mod 2976470733 已有 文化/建筑/政体改革/地区/兵种 的中文（政体改革键是大写 TSARDOM）。

要点：
- 只新增/修改 scripts/export-ui-tables.ts、apps/site/public/assets/ui/uiNames.json；
- 先探针：列出现有链能覆盖的 key 族；**特质的 key 形态必须查清**（去 common/ruler_personalities/ 找定义再回 localisation 找键），不要猜；
- 扩充 uiNames.json 至少覆盖：cultures / personalities / governmentReforms / buildings / greatProjects / terrain /
  religions / institutions / ideaGroups / advisors / areas / units；
- 没有中文的自造，并把全部自造条目写成 tmp/自造译名清单.md（key｜英文｜拟译名｜依据）——用户要逐条核对；
- 铁律：yml 是 UTF-8（带 BOM），必须显式 UTF-8 读；**控制台 cp936 里中文显示成乱码是正常的，不许据此判断"没有中文"**；
- 汇报先给结论（各键族覆盖数、自造几条、清单路径）。
```

**→ S1 继续（把名字烤进数据面）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

把 S2 的 apps/site/public/assets/ui/uiNames.json 读进构建期并烤进数据面，见 省份国家界面阶段任务书.md §6.8.3。

要点：
- 只改数据面相关文件（packages/eu4-parser/**、viewer-build.js、viewer-data.js、render-timeline.ts、apps/site/test/viewer-*.test.ts）；
- 产出 DATA.uiNames（或按字典对齐的 cultureNames/personalityNames）；面板只在运行期读数据面，**不许在运行期查文件**（离线页读不到）；
- 宗教名已有 RELIGION_FALLBACK 兜底链，**不要做第二套**；文化与特质按同样思路补齐；
- 两份数据面逐键一致 + viewer-build.test.ts 补断言；改完 pnpm timeline 重生成 tmp/timeline/data.json 再 pnpm test；
- 回报给一张表：面板会显示的每类名字（文化/特质/政体改革/建筑/奇观/地形/思潮/理念/顾问/地区），逐项标注
  「来自汉化 mod / 自造 / 缺失」。
```

**→ R（重建，让用户马上看得见）**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

阶段 1.5 的修复（名字表 + 全屏坐标 + 光标）已进代码，要重建 10 份存档才能看见，见 省份国家界面阶段任务书.md §6.8.4。

要点：
- 跑前备份 .dev-storage/index.json 与 10 份 meta.json；只重建、绝不删除；
- 用现成入口：双击 修复存档数据.bat，或 node scripts/rebuild-stored-data.ts --root .dev-storage；
- 重建后抽查两份：打开托管查看页确认（文化中文、特质中文、全屏点击准、光标是箭头）；离线自包含页也重建一份确认；
- 验收：pnpm verify:deploy 全绿；node tmp/data-format-audit.js 仍 10/10 PAYLOAD；汇报先给结论（重建几份、抽查结果）。
```

### 6.8.7 面板里的旗帜尺寸（用户 2026-09-19 追加）

用户原话：**「省份界面的旗帜或国家娘太小了，大小跟目录页卡片主要国家的旗帜大小一致」**、**「在国家面板的总览里面添加显示旗帜或国家娘、国名，大小跟目录页卡片主要国家的旗帜大小一致」**。

**参照物（已实测，照抄这一套）**：`apps/site/public/style.css` 第 118–123 行，目录页卡片主角旗 ＝ **48×48 方框**：

```css
.flagBox{position:relative;display:inline-block;line-height:0}
.flagBox>img{display:block}
.flagTint{position:absolute;left:50%;top:0;width:50%;height:100%;pointer-events:none}
/* 卡片主角旗的尺寸与框（以下四条是要对齐的部分） */
.card .hero .flagBox,.card .hero .heroFallback{width:48px;height:48px;border-radius:4px;
  border:1px solid var(--border-soft);background:var(--panel2);overflow:hidden}
.card .hero .flagBox>img{width:100%;height:100%;object-fit:contain}
```

（`flagBox`/`flagTint` 三行是**两页共用**的冻结结构：殖民地＝宗主旗 ＋ 右半色块，必须原样复用。48×48 本身就是正方形，同时满足早先「图标 1:1」的要求；`object-fit:contain` 保证旗不被拉变形。）

**要求**

1. 在 `viewer.html` 里加面板专用的一组类（例如 `.detailFlagBox` / `.detailFlagBox>img` / `.detailFallback`），尺寸/圆角/边框/底色/`object-fit` **逐条对齐卡片那套**（48×48、`border-radius:4px`、`1px solid var(--border-soft)`、`background:var(--panel2)`、`object-fit:contain`）；**不要为面板另写第二套尺寸**，四套主题都用变量、别写死颜色。
2. **省份面板**里所有国家旗——`拥有者` / `占领者` / `核心` / `宣称` 四处、以及「转到国家」按钮旁——都换成这个尺寸；核心/宣称是多国列表时**允许换行，不许为了塞下而缩回小图标**。
3. **国家面板「总览」Tab 的最上面**加一块：**旗或国家娘（48×48）＋ 国名**（同一行左右排布；国名字号与目录页卡片上的国名一致，长名不要省略到看不清，必要时换行）。
4. 殖民地一律走运行时叠加（宗主旗 ＋ 右半 `flagTint`），与卡片同结构同算法；缺旗仍退化成 **TAG 方块**（同样 48×48）。
5. `verify-player-run.ts` 加断言：面板里的旗读的是同一个尺寸（断言类名/内联宽度为 48）；「总览」顶部存在"旗＋国名"块。
6. 与早先「面板左上角图标 1:1」的要求不冲突（48×48 就是 1:1）；若日后与 §6.8.5 有任何冲突，**以本节为准**。

---

## 7. 阶段 2：国家面板波 2（建筑 / 州 / 阶级 / 顾问）

| 对话 | 任务 |
|---|---|
| S1 | 数据面补波 2 键：`buildingCount`（从各省 buildings 按拥有国聚合，不依赖它的 `num_of_buildings_indexed`）、`states`、`estates`、`crownland`、`advisors`；两份实现逐键一致 |
| S2 | 补 `area.json` 的州名（若阶段 1 未含）、`advisorIds.json` 完善 |
| S3 | 波 2 图标：州议会、议程、顾问头像（有就用，没有就退化成文字） |
| U | 波 2 四个 Tab 渲染（建筑条形图 / 州表 / 阶级卡 / 顾问网格），启用骨架里对应的 Tab |

**【你能自己验的测试目标 → 阶段 2】**（执行者落成 `tmp/验收-阶段2.md`）
1. 俄国国家面板 → **建筑** Tab：条形图里应出现大量"市场"（俄国 1574 有 373 座城市，建筑统计应与该国省份的建筑总数吻合；期望值由执行者探针算出）。
2. **州** Tab：州名是中文（不是 `brittany_area` 这种 key）；首都州带 ★；繁荣度/发展度两列有值。
3. **阶级** Tab：顶部一条"王室领地"；每个阶级一张卡，显示忠诚度%、领地占比%、特权表；**没有阶级的国家显示"无阶级"**（不是空白）。
4. **顾问** Tab：名臣顾问头像＋获得日期；没有名臣的国家只显示那一行文字。
5. 抽一个国家核对：把面板上的建筑总数与用探针算出的存档值对一下（执行者给出命令与结果）。

---

## 8. 阶段 3：国家面板波 3（财政 / 点数 ＋ 思潮徽章）

| 对话 | 任务 |
|---|---|
| S2 | **财政 19/38 槽位枚举** 与 **点数 46 槽位枚举**（唯一硬风险；取上游 `eu4save` 源码或校准存档反推；做不出来就如实报"暂缓"） |
| S1 | 数据面补 `budget`、`manaSpent` 键 |
| U | 财政 Tab（三指标卡＋瀑布图＋明细表＋支出树）、点数 Tab（三条量条＋饼表）、**思潮 Tab 改成"已接纳 N/8"徽章 ＋ 各省进度条**（不做它的开发规划器） |

**【你能自己验的测试目标 → 阶段 3】**
1. 俄国 → **财政** Tab：能看到"上月/年初至今/去年"三个区间切换；瀑布图有正负条；明细表的科目名是中文（如 核心税收/附庸收入/利息支出…）。
2. **点数** Tab：能看到 ADM/DIP/MIL 三条量条与"点数去哪了"的分组明细（理念/科技/发展省份…）。
3. **思潮** Tab：只显示"已接纳 N/8"徽章＋各省进度条（**故意的**：pdx-tools 那个 Tab 是"最省点数接纳思潮"的规划器，不是显示器，我们不做）。
4. 若槽位枚举没拿下：这三个 Tab 显示"**数据待补**"而不是显示错位的科目名（宁可说没有，不许编）。

---

## 9. 阶段 4：重建 10 份存档与离线页 ＋ 总验收

- **R 对话**：逐份重建 10 份存档的 `viewer/data.json` / `raster.png` / `thumb.png` / 离线页 `index.html`；**只重建、绝不删除**；跑前备份 `index.json` 与 10 份 `meta.json`。
  ⚠️ **注意（2026-09-19 实测）**：`scripts/rebuild-stored-data.ts`（就是双击那个 `修复存档数据.bat` 跑的）**只处理"还不是 payload"的存档**——对它来说已经是 `{version,data,panels}` 的会打印 `already payload - skipped` 然后 **跳过**。所以**直接双击它不会刷新数据面已经过时的那 8 份存档**（阶段 1.5 之后，10 份里只有 `b6263f5baf54`、`9809fb920676` 是新数据面）。要刷新必须走两条路之一：
  1. **先给驱动加 `--all` / `--refresh` 开关**（推荐：加完以后双击 .bat 就能全量重建，阶段 4 也用得上）；
  2. 或逐份跑 `node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage`（今天就能用，10 份约 10–15 分钟）。
  3. **等价替代（只刷托管页）**：在**目录页逐份点卡片的「重新解析」**——它在浏览器里用**当前** `viewer-build.js` 重建数据面并写回 `viewer/data.json` / `raster.png` / `thumb.png`，在托管页上与 R 的结果**等价**；代价是**要点 10 次**、每份等十几秒，而且**不刷新离线自包含页**（浏览器路径不写 `viewer/index.html`，所以离线页里仍是旧数据 ＋ 旧 `viewer.js`，在离线页上看不到 1.5 的修复与中文）。
- **总验收（用户）**：`tmp/验收-阶段4.md` 由主对话写：10 份存档逐份打开看一眼（每个面板都能开、四主题正常、离线页双击也能用）；`pnpm test` / `verify:player` / `verify:deploy` 三个闸门；`node tmp/data-format-audit.js` 仍 10/10 PAYLOAD。

---

## 10. 全局纪律

1. **不许碰 `.dev-storage/`**（R 除外，且只重建、绝不删除）；测试用 `tmp/` 或 dev-server 的 `--root` 指临时目录。
2. **不许改 `交接.md` 与别的对话名下的文件**；要改就写进回报。
3. **中文路径/文本**用 `[IO.File]::ReadAllText/WriteAllText` ＋ `UTF8Encoding($false)`，不要 `Get-Content/Set-Content`。
4. `.bat` 如需新增：CRLF、控制台只输出 ASCII、绝不 `chcp`、中文写文件，并加进 `apps/site/test/frontend.test.ts` 的 `launchers` 名单。
5. 测试条数以 `pnpm test` 实跑输出为准；跑测试需要会话是 `danger-full-access`。
6. **`viewer.js` 零反引号**、`page-theme.js` 扁平脚本、`viewer.html` 无 `${}`、素材路径站点根绝对——这四条有静态守卫，别踩。
7. 汇报先给结论（做完什么＋验证结果＋**验收清单期望值**），不要叙述调试过程。

---

## 11. 后续阶段的提示词（波 2 / 波 3 / 阶段 4）

**为什么不能只说"做波 2"**：每一波都是四方分工（S1 数据 / S2 表 / S3 图标 / U 界面），它们不知道彼此的边界、键名、产物与验收条目；光说一句会各自猜。**照下表复制，贴给同一个对话**（不要为新波次新开对话）。

| 波次 | 对话 | 贴哪一段 | 完成标志 |
|---|---|---|---|
| 波 2 | S1 | §11.1 | 数据面出现 `buildingCount/states/estates/crownland/advisors`，两份面一致 |
| 波 2 | S2 | §11.2 | 州名、advisorIds、阶级·特权中文名就位 |
| 波 2 | S3 | §11.3 | 波 2 图标能取到；缺的退化成文字 |
| 波 2 | U | §11.4 | 四个 Tab 能点开看真数据；**汇总出 `tmp/验收-阶段2.md`** |
| 波 3 | S2 | §11.5 | `ledgerSlots.json`(19/38) 与 `manaSlots.json`(46) 就位；拿不到就报"暂缓" |
| 波 3 | S1 | §11.6 | 数据面出现 `budget/manaSpent`，两份面一致 |
| 波 3 | U | §11.7 | 财政/点数 Tab ＋ 思潮 N/8 徽章；**汇总出 `tmp/验收-阶段3.md`** |
| 阶段 4 | R | §11.8 | 10 份重建完成 ＋ 离线页可用；产出 `tmp/验收-阶段4.md` |

### 11.1 → S1（波 2 数据面）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 1 已完成，现在做「波 2：国家面板的数据面」——见 省份国家界面阶段任务书.md §2 与 §7。

要点：
- 文件所有权与波 1 相同：只改 packages/eu4-parser/**、apps/site/public/viewer-build.js、viewer-data.js、
  viewer-store.js（只加可选入参）、viewer-page.js（只加加载入参）、scripts/render-timeline.ts、apps/site/test/viewer-*.test.ts；
  别动 viewer.js / viewer.html / style.css / page-theme.js / verify-player-run.ts / assets/**；
- 产出 §2 里"波 2"那几组键：buildingCount（**从各省 buildings 按拥有国聚合，不要用存档的 num_of_buildings_indexed**）、
  states、estates、crownland、advisors；名字（建筑/州/顾问/阶级/特权）读 S2 的 apps/site/public/assets/ui/uiNames.json 与 area.json；
- 两份数据面逐键一致、不许缺键、countryDetail 的 tag 键排序；viewer-build.test.ts 补断言；pnpm timeline 重生成参照；
- 回报里给"可验项 ＋ 期望值"（例如俄国 1574 的建筑统计前三名、州数、阶级名与忠诚度），U 会汇总进 tmp/验收-阶段2.md；
- 闸门：pnpm test 全绿（条数以实跑为准）。汇报先给结论。
```

### 11.2 → S2（波 2 表）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 2 的"表"部分，见 省份国家界面阶段任务书.md §7。

要点：
- 只改 scripts/export-ui-tables.ts、apps/site/public/assets/ui/*.json；
- 补齐：area.json 的**州名**（若阶段 1 只做了地区名）、advisorIds.json（common/advisors 全量 id → 中文名；
  汉化 mod 有就用，没有就自造并列入 tmp/自造译名清单.md）、州议会/阶级/特权的中文名（来自 localisation，注意键的大小写）；
- 回报里给"可验项 ＋ 期望值"（某国顾问名单、某州的中文名），U 会汇总进 tmp/验收-阶段2.md；
- 闸门：产物能用 node -e "JSON.parse(...)" 读出，条数报出来。汇报先给结论。
```

### 11.3 → S3（波 2 图标）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 2 需要的图标（能取到就取，取不到让 U 退化成文字），见 省份国家界面阶段任务书.md §7。

要点：
- 只改 scripts/export-ui-assets.ts、apps/site/public/assets/ui/**/*.png、scripts/verify-deploy.ts；
- 找：州议会/法院类建筑（可复用波 1 的建筑图标）、议程/阶级图标（all/estates、estate_interface、privileges）、
  顾问头像（all/advisors，没有就用 ideas_EU4 里的通用头像，再没有就退化）；
- 按 §4 的规则落位成小写 key 文件名；每个子类报"有几个、缺几个"；verify:deploy 仍 < 20000 文件；
- 回报给"可验项"（哪几个图标应该在面板里出现），交给 U。汇报先给结论。
```

### 11.4 → U（波 2 四个 Tab）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

现在做「波 2：建筑/州/阶级/顾问 四个 Tab」，见 省份国家界面阶段任务书.md §7 与第三对话任务书.md §1.9。

要点：
- 文件所有权同波 1（apps/site/public/viewer.html、viewer.js、style.css、page-theme.js、scripts/verify-player-run.ts、
  apps/site/test/frontend.test.ts、packages/eu4-parser/test/build-guards.test.ts）；别动数据面与素材；
- 四个 Tab 按 §1.9 的构成渲染：建筑＝条形图（y 建筑名、x 省份数、按名排序）；州＝表（州名/发展度/繁荣度/州议会/
  首都州★；治理成本与集权先写"暂不提供"）；阶级＝王室领地条 ＋ 每阶级一张卡（忠诚度/领地占比/议程/特权表/影响力修正表）；
  顾问＝名臣头像网格（带日期）；
- 空值规则同前：分组级空 → 整组不渲染；单元格级空 → `—`；省份面板的旗仍按 §6.8.7 的 48×48；
- 数据键见 §2 的"波 2"组（S1 在产出；没到先用占位数据，不要自己造数据面）；
- verify-player-run.ts 给四个 Tab 各加一条断言（能切换、关键字段渲染出来）；
- **汇总产出 tmp/验收-阶段2.md**：把 §7 的四条验收 ＋ S1/S2/S3 给的可验项与期望值，写成"双击什么 → 打开什么 →
  点哪里 → 应看到什么值"；
- 闸门：pnpm test 与 pnpm verify:player 全绿。汇报先给结论。
```

### 11.5 → S2（波 3 槽位枚举＝唯一硬风险）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 3 的"表"部分——**这是整个阶段唯一的硬风险**，见 省份国家界面阶段任务书.md §8 与 §6.2 第 5、6 条。

要点：
- 只改 scripts/export-ui-tables.ts、apps/site/public/assets/ui/*.json；
- 目标：ledgerSlots.json（income 19 槽 / expense 38 槽）与 manaSlots.json（adm/dip/mil 各 46 槽）的"槽位 → 科目名"；
  上游在 Rust eu4save crate：本机 web_fetch 被沙箱挡，但 codeload.github.com / api.github.com / cdn.jsdelivr.net 可用
  （例：Invoke-WebRequest https://codeload.github.com/rakaly/eu4save/tar.gz/refs/heads/master -OutFile r.tgz）；
  取不到就用已知数值的存档反推校准（8 国 ledger 实测都是定宽 19/38）；
- **拿不到就如实报"波 3 暂缓"**，不许编造科目名（U 会把这三处显示成"数据待补"）；
- 回报给"可验项 ＋ 期望值"（某国某区间的收入科目名与数值），交给 U 汇总。汇报先给结论。
```

### 11.6 → S1（波 3 数据面）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 3 的数据面，见 省份国家界面阶段任务书.md §2 的"波 3"组与 §8。

要点：
- 文件所有权同前；产出 countryDetail 的 budget（19/38 槽，槽位名读 S2 的 ledgerSlots.json）与 manaSpent（46 槽，
  读 manaSlots.json）；
- **S2 的枚举没到就别猜槽位名**：先按数组位置输出、名字留空，等 S2 到了再补；
- 两份数据面逐键一致 ＋ 断言；pnpm timeline 重生成；
- 回报里给"可验项 ＋ 期望值"，交给 U。汇报先给结论（键是否就位、两份面是否一致）。
```

### 11.7 → U（波 3 财政/点数 ＋ 思潮徽章）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

波 3：财政/点数两个 Tab ＋ 思潮徽章，见 省份国家界面阶段任务书.md §8。

要点：
- 财政 Tab ＝ 三指标卡 ＋ 瀑布图 ＋ 明细表（上月/年初至今/去年/合计 ＋ 占比）＋ 支出树（树图先用简单嵌套列表即可，
  不强求 pdx-tools 那种 treemap）；点数 Tab ＝ 三条量条 ＋ "点数去哪了"分组明细；
- 思潮 Tab **只做"已接纳 N/8"徽章 ＋ 各省进度条**（不做它的开发规划器）；
- 槽位枚举没拿下时，三处显示"数据待补"，**不许显示错位的科目名**；
- verify-player-run.ts 加断言；**汇总产出 tmp/验收-阶段3.md**（§8 的四条 ＋ S1/S2 的可验项）；
- 闸门：pnpm test 与 pnpm verify:player 全绿。汇报先给结论。
```

### 11.8 → R（阶段 4 重建）

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

阶段 4：重建 10 份存档与离线页，见 省份国家界面阶段任务书.md §9。

要点：
- 跑前备份 .dev-storage/index.json 与 10 份 meta.json 到 tmp/taskD-backup/；**只重建、绝不删除**；
- ⚠️ **不要只双击 `修复存档数据.bat` 就以为重建完了**：`scripts/rebuild-stored-data.ts` 对"已经是 payload"的存档会打印
  `already payload - skipped` 并**跳过**——而数据面过时的那些档恰恰都还是合法 payload，所以它一份都不会重建。
  按 §9 走两条路之一：①给驱动加 `--all` / `--refresh` 开关（推荐，加完双击 .bat 即全量重建）；②逐份跑
  `node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage`；
  （③等价替代：在目录页逐份点卡片「重新解析」——托管页结果一样，但不刷新离线自包含页，且要点 10 次）；
- 逐份抽查：托管查看页能开、省份/国家面板都有数据（**文化是中文、建筑/地形/奇观有内容、国家面板有数据**——这三样在旧数据面上是空的，正好当"重建成功"的判据）、离线自包含页双击也能用；
- 产出 tmp/验收-阶段4.md（10 份逐份一行：能否打开、面板是否正常、离线页是否正常）；
- 闸门：pnpm verify:deploy 全绿；node tmp/data-format-audit.js 仍 10/10 PAYLOAD；汇报先给结论（用了哪条路、重建几份、抽查结果）。
```

> 阶段 4 跑完，由**主对话**收尾：目视两种面板与四主题 → `pnpm test` / `verify:player` / `verify:deploy` → 更新 `交接.md`（顺带划掉 §七 那条已过时的"任务 D 遗留"）→ 本任务书归档。

---

## 12. 每阶段的对话顺序（谁先谁后、谁在等谁）

**一句话**：每一波都是 **S2 表 → S1 数据 → U 界面** 这条链；**S2/S3/U 可以同时开工**，S1 的"验证"等 S2 的表，U 的"跑闸门"等 S1。用户只做三件事：贴提示词、读 `tmp/验收-阶段N.md`、照着点验。

### 12.0 通用时序（每一波都照这个走）

| 步 | 谁 | 动作 | 在等谁 | 你此时做什么 |
|---|---|---|---|---|
| 1 | **S2** | 出/更新 `apps/site/public/assets/ui/*.json`（表、枚举、中文名） | — | 贴 §11.2 / §11.5 给 S2 |
| 2 | **S1** | 读 S2 的表 → 烤进数据面 → `pnpm timeline` → `pnpm test` | **S2 的表落地**（代码可以先写） | 贴 §11.1 / §11.6；S2 报"表就位"后**提醒 S1 跑一遍** |
| 3 | **S3** | 图标落位（不挡任何人） | — | 贴 §11.3 |
| 4 | **U** | 按冻结键名写渲染 ＋ 断言（先用占位数据） | —（**跑闸门**要等 S1） | 贴 §11.4 / §11.7 |
| 5 | **U** | 闸门全绿后**汇总** `tmp/验收-阶段N.md` | S1/S2/S3 的回报 | 读清单 → 双击 .bat 点验 → 批注 |
| 6 | **主对话** | 复核闸门、补验收清单、记进度 | U 的汇总 | 有问题把它发给主对话 |

**两条硬规矩（阶段 1.5 就是第 1 条翻的车）**
1. **S2 改过 `assets/ui/*.json` 之后，S1 必须重跑 `pnpm timeline` 再跑 `pnpm test`**：离线参照与浏览器面都读这份表，不重生成就会出现"两份数据面不一致"。（实测：U 在 23:56 跑绿 → S2 在 0:01 更新了 `uiNames.json` 277 条 → 没人重生成 → `viewer-build.test.ts` 三条不一致变红。）
2. **每个对话收工前必须自己跑一次自己的闸门**并把实跑输出贴进回报：S1＝`pnpm test`；U＝`pnpm test` ＋ `pnpm verify:player`；S3＝`pnpm verify:deploy`。

### 12.1 波 2（四个对话）

```
T0 同时贴：S2(§11.2) · S3(§11.3) · U(§11.4) · S1(§11.1，先写读表代码)
T1 S2 报"表就位"  → 叫 S1 跑：pnpm timeline + pnpm test
T2 S1 报"数据面就位" → 叫 U 跑闸门：pnpm test + pnpm verify:player
T3 U 交 tmp/验收-阶段2.md → 你点验（§7 四条 + S1/S2 的可验项）
```

### 12.2 波 3（三个对话，唯一的硬风险在这里）

```
T0 同时贴：S2(§11.5 槽位枚举，先做) · U(§11.7 先搭 Tab 与占位)
T1 S2 报"枚举拿到"或"暂缓" → 交给 S1(§11.6) 烤 budget/manaSpent
T2 S1 报就位 → U 跑闸门 → 交 tmp/验收-阶段3.md → 你点验（§8 四条）
   （若 S2 报"暂缓"：这三处显示"数据待补"，其余照做，不必等）
```

### 12.3 阶段 4（一个对话 + 主对话）

```
T0 先确认三道闸门全绿：pnpm test / pnpm verify:player / pnpm verify:deploy
T1 贴 §11.8 给 R → 备份 index.json 与 10 份 meta.json
   ⚠️ 别只双击 修复存档数据.bat：那是"修复还没修过的档"用的，对已经是 payload 的 8 份会静默跳过；
   要么先加 --all 开关，要么逐份跑 build-viewer（见 §9）
T2 R 交 tmp/验收-阶段4.md（10 份逐份一行）→ 你逐份打开看一眼
   （判据：文化是中文、建筑/地形/奇观有内容、国家面板有数据、离线页能用）
T3 主对话收尾：更新 交接.md（划掉 §七 已过时的"任务 D 遗留"）+ 本任务书归档
```

### 12.4 阶段 1.5 的现状（主对话复核结论）

- **产物齐**：`tmp/验收-阶段1.5.md`（U 写的点验清单）、`tmp/自造译名清单.md`（**277 条自造译名，等你核对**）、`tmp/ui-tables-report.md`、`assets/ui/**`（6 张 JSON ＋ 建筑 47 / 奇观 143 / 地形 17 / 宗教 39 / 思潮 8 / 政体改革 670 / 理念 30 / 修正 923 / 图标 67）、`viewer.js` 的坐标/光标/48×48 与「总览」旗＋国名。
- **闸门（主对话 2026-09-19 独立复跑，全绿）**：`pnpm test` **201/201**；`pnpm verify:player` **面板 40/40 ＋ 播放器运行期全过**（断言 26–40 覆盖：点海洋直接关闭、背回目标重定向、全屏切换、🎨「面板」拉条、`#map`/`#hover` 同一变换、按画布方框命中、缺旗 TAG 方块、进全屏按新方框重画高亮、89 面旗全是 48×48、`.view` 光标是 default、总览顶部「48×48 旗＋国名」）；`pnpm verify:deploy` **站点可发布**（ui 素材 1,944 文件 / 20.7 MB，全部小写游戏 key，9 个族全非空）。
  **但复核开始时 `viewer-build.test.ts` 是红的**（`uiNames / cultureNames / personalityNames` 三条不一致）：原因是 **S2 在 0:01 更新了 `uiNames.json`（277 条自造译名）而没人重跑生成器**——离线参照 `tmp/timeline/data.json` 还停在 23:53。按硬规矩 1 跑一次 `pnpm timeline` 后立刻全绿，**证明是参照过期、不是实现错误**。
- **重建**：10 份存档里**只重建了 2 份**（`b6263f5baf54` = mp_俄罗斯1574、`9809fb920676` = mp_阿尔达比勒1524），其余 8 份要等阶段 4；**想在浏览器里验 1.5，就用这两份**（或双击 `tmp/timeline/index.html`，那份数据面是最新的、但没有旗帜与图标）。
- **数据面变大**：单份 payload 从约 2 MB 涨到 **4.3–5.2 MB**（最大头是 `countryDetail` 2.3 MB，因为 273 个国家的历代君主/将领/历史/州/阶级都烤进去了）；**gzip 后只有 1.0–1.1 MB（22%）**，Cloudflare 走 brotli 更小，所以传输不是问题，但浏览器要多解析约 3 MB JSON。**若你介意"秒开"，可让 S1 在阶段 4 前做一次瘦身**（例如历史事件只保留最近 N 条 / 只给有省份且国力前 N 的国家），这是可选项，不影响本次验收。

### 12.5 启动卡（照这个贴，别想别的）

**现在（波 2 开机）——五个对话，同时贴**

| 对话 | 贴本任务书的这一段 | 备注 |
|---|---|---|
| **R**（新开一个） | **§11.8** | 现在就能开，独立于波 2；**必须先给驱动加 `--all`**，否则它跳过全部 10 份 |
| **S2** | §11.2 | 与 S1/S3/U 同时贴 |
| **S3** | §11.3 | 同时 |
| **U** | §11.4 | 同时（先用占位数据） |
| **S1** | §11.1 | 同时（先写读表代码） |

**等待关系——只有两条硬等待（其余全并行）**
1. **S2 报"表就位"** → 你去 S1 那里说一句：**「S2 的表好了，跑 `pnpm timeline` ＋ `pnpm test`」**。
2. **S1 报"数据面就位"** → 你去 U 那里说一句：**「数据面好了，跑闸门并交 `tmp/验收-阶段2.md`」**。

**你每一波只做四件事**
1. 贴提示词（能同时的一起贴）；
2. 收到"表就位／数据面就位"就转告下一棒（上面两条）；
3. 读 `tmp/验收-阶段N.md`，双击 .bat 点验；
4. 有问题把清单发给主对话，没问题就说"过"。

**波 3 开机**：S2（§11.5）＋ U（§11.7）同时贴 → S2 报枚举结果（拿到/暂缓）→ S1（§11.6）→ U 跑闸门交 `tmp/验收-阶段3.md` → 你点验。
**阶段 4**：R（§11.8，此时 `--all` 已就绪，双击 `修复存档数据.bat` 即可）→ 你逐份打开看（判据：文化中文、建筑/地形/奇观有内容、国家面板有数据、离线页能用）→ 主对话收尾。
