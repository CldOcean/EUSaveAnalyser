# EUSaveAnalyser · 免费建站手册（Cloudflare Pages + R2）

> 目标：把查看站挂到公网，**全程在免费额度内**，不用买服务器、不用命令行。
> 现在就能发——建站闸门已跑通（2026-09-19 主对话实跑 `pnpm verify:deploy`）：
> **可部署 4,672 文件 / 71.7 MB**（上限 20,000 文件、单文件 25 MiB），最大单文件 2.24 MB。
> 本机专用、**绝不提交**的目录：`apps/site/public/assets/flags/mods/`（14,550 文件 / 55 MB）、根 `.dev-storage/`（251 MB）、`.dsh-meow/`（记忆库）——`.gitignore` 已挡好。

---

## 0. 开始前（我已替你确认）

- [x] 建站闸门全绿：文件数、单文件体积、入口文件、素材族、旗帜、背景图都通过。
- [x] `.gitignore` 补上了两个**本机专用**目录：根 `.dev-storage/` 与 `.dsh-meow/`（原来只写了不存在的 `apps/site/.dev-storage/`，不补会平白提交 251 MB）。
- [x] `存档示例/mp_俄罗斯1574_11_12.eu4`（8.3 MB）**要提交**——十几个测试与脚本都读它，别排除。
- [ ] （可选清理）根目录那个 0 字节怪文件 `console.log(JSON.stringify(d.saves` 是误操作产物，删掉更干净；不删也不影响 Pages（它不在 `public/` 里）。

---

## 1. 把代码推到 GitHub（仓库已在本机建好，只差远程 + 推送）

**现状（2026-09-19 实查）**：本机仓库已经 `git init` 并提交好——`main` 分支 **2 个提交**、**4,776 个跟踪文件**（`apps` 4,698 个全在里面，`apps/site/functions` 也在）、工作区干净；**只差"远程仓库地址"和 push**。git 已装：`C:\Program Files\Git\cmd\git.exe`。三条路任选：

**① 双击根目录 `上传到GitHub.bat`（最省事）**
它会 `git add -A` → 有改动才 commit → 没配远程就问你要仓库地址并 `git remote add` → `git push -u origin main`。首次会弹 GitHub 登录窗，点浏览器授权一下即可。

**② 命令行**（**重开**一个 PowerShell，否则认不到刚装的 git）
```powershell
cd /d F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser
git remote add origin https://github.com/<用户名>/<仓库名>.git
git push -u origin main
```
若仍提示找不到 `git`，用全路径：`& "C:\Program Files\Git\cmd\git.exe" remote add origin ...`（其余命令同理）。

**③ GitHub Desktop**（不想碰命令就用它）
`File → Add local repository…` → 选项目根 → 顶部 **Publish repository** → 勾 **Keep this code private** → Publish。

**push 被拒绝怎么办**（报 `rejected ... remote contains work that you do not have`，＝你之前用网页手动传过文件）：
- 那个仓库是刚建的空壳、只有手传的几个文件夹 → `git push -u --force origin main`（本地这份是全量）；
- 想保留远程历史 → 先 `git pull --rebase origin main` 再 push；提示冲突就停下发我。

**推完核对（1 分钟）**：GitHub 仓库首页应显示 **约 4,776 个文件**，能看到 `apps/site/public/…` 与 **`apps/site/functions/…`**（API 全靠它）；**不该出现** `apps/site/public/assets/flags/mods/`、`tmp/`、`.dev-storage/`、`.dsh-meow/`。

> 之后每次改完代码，只要双击 `上传到GitHub.bat`（或 `Commit` → `Push`），Cloudflare 会自动重新部署。

## 2. 在 Cloudflare 建 R2 桶（存存档）

1. 注册/登录 Cloudflare（免费）→ 左侧菜单 **R2** → 首次进入点 **Enable/开通**（免费额度内不收费，但通常要**绑一张卡**）。
2. **Create bucket** → 名称必须填 **`eu4-saves`**（与 `apps/site/wrangler.toml` 一致）→ 位置默认 → 创建。

## 3. 建 Pages 项目（把网站挂起来）

> ⚠️ **千万不要建在 "Workers" 上**（2026-09-19 实际踩到）：新版 Cloudflare 把 Workers 和 Pages 放在同一个 "Create application" 页面、顶部有 **Workers / Pages 两个标签**。如果选成 **Workers**（Import a repository），它的 **Deploy command 默认是 `npx wrangler deploy`**，会在构建时报
> `✘ [ERROR] Missing entry-point to Worker script or to assets directory` —— 因为本项目的 `apps/site/wrangler.toml` 只写 Pages 的设置（Root/Build output）与 R2 绑定，没有 `main`/`[assets]`。
> **本项目必须用 Pages**：后端是 **Pages Functions**（`apps/site/functions/api/saves.ts` 按路径自动变成 `/api/saves` 路由），这是 Pages 独有能力；Worker 那条路即使把静态资源配好，上传/列表接口也不会生效。建错的 Worker 留着不管、或删掉都行。
> 另一个坑：若界面里出现 **Deploy command** 字段（Pages 新版也可能有），**清空它**。

Cloudflare → **Workers & Pages → Create application → 选顶部的 `Pages` 标签 → Connect to Git** → 授权 Cloudflare 访问你的 GitHub → 选中 `EUSaveAnalyser` → 构建设置**照抄**：

| 设置项 | 填什么 |
|---|---|
| Project name | `eu4-save-archive`（或你喜欢的名字，决定网址） |
| Production branch | `main` |
| Framework preset | **None** |
| Build command | **留空**（`public/` 本身就是产物，不需要构建） |
| Build output directory | **`public`** |
| Root directory（在 Advanced/高级里展开才有） | **`apps/site`** |

点 **Save and Deploy** → 等 1 分钟左右 → 给你一个 **`https://<项目名>.pages.dev`** 地址。

## 4. 绑 R2 + 设置上传口令（关键的一步）

进入刚建的 Pages 项目：

1. **Settings → Functions → R2 bucket bindings** → **Add binding**：
   - Variable name：**`SAVES_BUCKET`**
   - R2 bucket：**`eu4-saves`**
   - Environment：先加 **Production**（建议把 **Preview** 也加一份，否则预览环境报错）。
2. **Settings → Environment variables** → **Add**：
   - Name：**`UPLOAD_TOKEN`**
   - Value：**一串 20 位以上的随机字符**（大小写字母＋数字，别用生日/常用密码）——这就是你以后在目录页填的**上传口令**。
   - 点 **Encrypt/加密**（作为 Secret 保存）。
3. 回到 **Deployments** → 右上角 **Retry deployment / 重新部署**。
   ⚠️ **绑定和环境变量改动必须重新部署才生效**（这一步最容易漏，漏了的表现是上传报 401 或页面说没有存储）。

## 5. 上线自检（5 分钟，逐条打勾）

1. 打开 `https://<你的项目>.pages.dev/` → **目录页出现**（初始是空列表）。
2. 在目录页填**上传口令**（＝第 4 步的 `UPLOAD_TOKEN`）→ 拖入一份 `.eu4` → 卡片出现、缩略图出现。
   - 首次会现场解析一次（几十秒），之后就是"秒开"。
3. Cloudflare → **R2 → eu4-saves**，应能看到：
   `index.json`、`saves/<id>/original.eu4`、`saves/<id>/meta.json`、
   `saves/<id>/viewer/data.json`、`saves/<id>/viewer/raster.png`、`saves/<id>/viewer/thumb.png`。
4. 点开查看页：九视图、时间线播放、缩放拖动、点省份/国家弹面板、底部三张表 + 两张排行榜、点表头排序。
5. **手机**打开同一个地址（第四轮 C3 已适配）：页面不再缩小、单指拖动、双指捏合缩放、面板能滚。
6. （可选）删掉刚上传的测试存档，确认 R2 里的文件也被清掉。

## 6. 自定义域名（可选）

Pages 项目 → **Custom domains → Set up a domain** → 填你的域名。
域名 DNS 已托管在 Cloudflare 的话是**一键完成**（自动配证书）；不在的话按提示改一次 DNS 记录。免费版自带 HTTPS。

## 7. 以后怎么更新

1. 本地改代码 → 跑 `pnpm test`（＋动了 `viewer.js` 就跑 `pnpm verify:player`）。
2. GitHub Desktop：`Commit to main` → **`Push origin`**。
3. Cloudflare 自动重新部署（约 1 分钟）。
4. **永远不要**用 `wrangler pages deploy apps/site/public`：那会把本机 14,550 个 mods 旗帜一起上传、直接超 20,000 文件上限（这条是 `wrangler.toml` 注释里踩过的坑，所以必须走 git）。

## 8. 免费额度与上限

| 项 | 免费版 | 你的现状 |
|---|---|---|
| Pages 文件数 | 20,000 / 次部署 | 4,672（23%） |
| Pages 单文件 | 25 MiB | 最大 2.24 MB |
| Pages 构建次数 | 500 / 月 | 只有 push 才触发 |
| Pages 流量/请求 | 不限 | 个人站随便看 |
| Functions 请求 | 100,000 / 天 | 列表/上传/取存档都走它 |
| R2 存储 | 10 GB·月 | 单份存档约 2.7 MB，能放几千份 |
| R2 操作 | 100 万 Class A / 1000 万 Class B 每月 | 够用 |
| R2 出流量 | **免费** | 这是选 R2 而不是 S3 的原因 |

（以 Cloudflare 官网现价为准。）

## 9. 五个最常见的坑

1. **绑定/环境变量改完没重新部署** → 上传 401、或页面说没有存储。
2. **忘了在目录页填上传口令** → 报「上传未授权：请在设置里填入上传口令」。
3. **Root directory 没填 `apps/site`** → 部署"成功"但首页 404。
4. **Handled 成 `wrangler pages deploy`** → 撞 20,000 文件上限（必须走 git）。
5. **单份存档超过 25 MiB** → 被 Pages/R2 限制挡住（正常 `.eu4` 都在这之下）。

## 10. 卡住了就把这几条发我

- Pages 项目 → **Deployments** 里最后一次构建的**状态与日志尾部**；
- 浏览器控制台（F12 → Console）的红色报错原文；
- Cloudflare → **Settings → Functions** 里绑定名是不是 `SAVES_BUCKET`、**Environment variables** 里有没有 `UPLOAD_TOKEN`；
- 你看到的现象（哪一步、点了什么、期望看到什么、实际看到什么）。
