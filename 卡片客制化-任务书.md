# EUSaveAnalyser · 卡片客制化任务书（现行版）

> 更新：2026-09-19。**本文件只写"还没做的事"**——已完成的功能、bug 修复与历史决策看 `交接.md`。
> 第 1–6 轮的旧任务书（含每轮根因分析、被否掉的方案）已归档到 `docs/卡片客制化-任务书-历史（第1-6轮）.md`。
> 项目根：`F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser`。每个执行者开工前先读 `交接.md` + `欧陆风云4-数据位置.md` + 本文件对应章节。

---

## 0. 你现在要做什么（操作清单）

| 批次 | 交给谁 | 任务 | 提示词在 | 完成标志 |
|---|---|---|---|---|
| **现在只剩这一个** | **新对话 P** | 任务 D：修 `viewer/data.json` 的两种格式打架，并把 6 份退化的存档恢复成"秒开" | §2.4 末尾 | 10 份 `data.json` 都是 `{version,data,panels}` + `pnpm test`/`verify:deploy` 全绿 |

> **任务 A（构建侧：两份 `hashColor` 加 fmix32 + 删旧殖民合成旗）、任务 B（前端：殖民地旗运行时叠加）、任务 C（重建 10 份存档与离线页）都已在 2026-09-19 落地**，验收也过了（`pnpm test` 193 全过、`pnpm verify:player` 全绿、`pnpm verify:deploy` 报站点可发布）。它们留下了一个新问题，就是下面的任务 D——本节表里不再列它们，规格留在 §2.1–§2.3 作参考。
> 执行者开工前先读 `交接.md` + `欧陆风云4-数据位置.md` + 本文件对应章节；做完把结论告诉第三个主对话（本对话，负责联调、全量验证与更新 `交接.md`），执行者不要自己改 `交接.md`、也不要改本任务书。

---

## 1. 背景与冻结的契约（所有任务共同遵守）

### 1.1 项目一句话

把历年《欧陆风云 4》存档做成个人纪念网站：上传 `.eu4` → 浏览器里生成月度版图时间线 → 九视图逐帧回放 + 兴衰曲线 + 三张表。部署目标是 **GitHub + Cloudflare 免费版**（Pages 静态 + Functions + R2）；Worker 无文件系统、10ms CPU，而 gamestate 解压 57MB → **解析与生成只能在浏览器或本地做**。发布暂缓，现在是完善功能阶段。

### 1.2 已经落地的（本轮不要重做）

目录页卡片：标题点击改名、`#flagSet` 原版/国家娘切换、结档缩略图、主角旗帜分页（每页 3 个）、排序含结档/封档日期、导航栏亚克力。
查看页：标题行最右 `📝 修改存档信息`、H1 可改名且显示 `custom.title || 国名`、副标题＝`版本 · 结档日期 · 封档日期 · 模组`、主角国家 TAG 弹列强榜前 15 下拉、标题行主角旗帜排（每页 5 个）、导航栏亚克力。
数据层：`SaveRecord.custom` + `GET/PATCH /api/saves/:id`；`viewer/thumb.png` 缩略图管线；两份 localisation 镜像的宗教名兜底（`RELIGION_FALLBACK`）。

### 1.3 冻结的数据契约（已实现，改动需谨慎）

| 项 | 约定 |
|---|---|
| 字段 | `custom = { title?, endDate?, sealedAt?, mods?, protagonists?: [{tag, player?}] }`；没有该键＝`{}` |
| 写入 | `PATCH /api/saves/:id` 深合并；`""`/全空格/`[]` 删除该字段；`custom: null` 清空整块；脏类型视为"没提供" |
| 清洗 | `tag`：转大写→剔除非 `[A-Z0-9]`→取前 4 位→长度 <2 丢弃；同 tag 去重取首条；最多 20 条。`title` 80 / `endDate` 20 / `sealedAt` 20 / `mods` 200 字符截断 |
| 读取 | `GET /api/saves/:id`（免令牌，找不到 404） |
| 封档日期 | 显示一律 `custom.sealedAt || uploadedAt 前 10 位`；四处口径一致（卡片行 / 面板 / 副标题 / 排序）；标签统一「封档日期」 |
| 权限 | 写请求头带 `localStorage['catalogue.token']` 作为 `x-upload-token`；401 提示去目录页填口令 |

### 1.4 冻结的颜色算法（两份实现必须逐字一致）

`hashColor(tag)` 用来给"存档里没有颜色的国家"取地图颜色，也用来给殖民地旗的右半色块取色：

```js
function hashColor(tag) {
  let h = 0;
  for (const ch of tag) h = (h * 31 + ch.charCodeAt(0)) >>> 0;   // 多项式哈希
  h ^= h >>> 16; h = Math.imul(h, 0x85ebca6b);                   // murmur3 fmix32：
  h ^= h >>> 13; h = Math.imul(h, 0xc2b2ae35);                   // 取色相前必须先位混合
  h ^= h >>> 16;
  return hslToRgb((h >>> 0) % 360, 0.6, 0.45);                   // HSL 参数保持不变
}
```

- 两份定义在：`scripts/lib/map-assets.ts`（离线面）与 `apps/site/public/viewer-build.js`（浏览器数据面）。**只改一份会让 `viewer-build.test.ts`（两数据面逐键比对）挂。**
- 现状是坏的：只写 `h % 360` 时，多项式哈希的低位随字符近似线性变化 → 实测 `C00`–`C09` 色相 43–52、`C10`–`C17` 74–81、`D00`–`D05` 284–289，殖民地旗右半全是黄（`#B8912E`…`#B8A52E`）。

---

## 2. 任务（A、B、C 已完成，只剩 §2.4 的任务 D）

> §2.1–§2.3 是已完成三批的原始规格，留作参考；真正要派的是 **§2.4 任务 D**。

### 2.4 任务 D（新对话 P）：修 `viewer/data.json` 的格式打架 + 恢复 6 份存档的"秒开"

**现象（2026-09-19 第三个主对话复核，与第二个主对话实测一致）**：`node tmp/data-format-audit.js` 实跑 —— 10 份里有 **6 份 OFFLINE ✗**（`fb0e9dce2a8e`、`3d2c314baae7`、`0febd901efcb`、`41512f20a3db`、`7efd60657781`、`c5464cb5f049`），`viewer/data.json` 顶层是**离线原始数据面** `{w,h,scale,start,end,…}`；另外 4 份是**浏览器入库格式** `{version:2, data, panels}`（`8fc9283423d0`、`990e9f3381e8`、`9809fb920676`、`b6263f5baf54`——这 4 份正是用户手点托管查看页时被浏览器自愈回写的，mtime 比其余 6 份晚 8 分钟）。

**根因**：`apps/site/public/viewer-store.js` 的 `loadStoredSave()` 只认后者（它要求 `stored.data.tags`），而 `apps/site/src/build-viewer.ts` 是把离线生成器 `scripts/render-timeline.ts` 的整份输出照搬进 `saves/<id>/viewer/` 的——离线生成器写出的 `data.json` 是它自己的格式。**两个生产者在同一条路径上互相覆盖。**

**后果**：那 6 份打开**托管**查看页时不再"秒开"——`loadStoredSave()` 返回 undefined → 回退到"现场重建"（约 10 秒，浏览器有游戏目录缓存时会重新入库、自愈）；离线自包含页不受影响。**另有第二个症状**：目录页卡片要靠 `viewer/data.json` 里的 `data.colonialParent` 画殖民地旗（`app.js` 的 `colonialParents` 走同一个 `loadStoredSave()`），这 6 份拿不到 → 卡片的殖民地旗也一起退化。

**冻结的契约（验收就查这个）**

- 存档目录里的 `saves/<id>/viewer/data.json` **永远**是 `{version:2, data, panels}`，且 `data.tags` 必须存在（`loadStoredSave()` 的判据，不许动它）。
- `data` 与 `apps/site/public/viewer-build.js` 的 `buildViewerData().data` **逐键相同**（现有测试 `viewer-build.test.ts` 已经证明浏览器面 ≡ `tmp/timeline/data.json`，所以"服务端改成产出浏览器 payload"不会改变任何一个数据键）。
- `panels` 形状＝浏览器面那份：`{ leaderHtml, cityHtml, institutionHtml, header, leaders }`。
  - `header` 至少要有 `title` / `start` / `end`（`viewer.html` 的 `data-fact` 靠它填字；`viewer.js` 对 data-fact 是按名字通用填充，另外点名用 `end` 与 `title`），并**建议**补齐 `frames / provinces / events / width / height / firstMonth / lastIndex / seaHex / unownedHex`，与 `viewer-build.js` 同形（这几个现在是留给以后"这份文件里有什么"那行字的）。
  - `leaders` 是 `[{tag, name, …}]`（查看页"主角国家"下拉用它，缺了会退化成读页面上的列强榜，不算错但别丢）。
- 原始离线面（`{w,h,scale,…}`）**不再发布进存档目录**：一个目录里只有一种 `data.json`、一个名一个格式，这正是这次 bug 的教训；原始面留在 `tmp/timeline/` 当构建中间物。若执行者坚持另发一份，必须换名（如 `viewer/offline-data.json`）并在回报里说明理由。

**推荐的实现（省事、且不引入第二份实现）**

- 只改 `apps/site/src/build-viewer.ts`：render-timeline 跑完后，**在 Node 里直接调用站点自己服务的那几个模块**（`apps/site/public/` 的 `game-data.js`、`game-localisation.js`、`game-tables.js`、`viewer-build.js`、`eu4-parser.js`、`parser.js`）调 `buildViewerData(...)` 拿到 `{data, panels}`；**逐键比对**它与 render-timeline 刚写出的 `data.json`，**不一致就报错退出、不覆盖**；一致才把 `{version:2, data, panels}` 写成/上传成 `viewer/data.json`，并跳过原始 `data.json` 的发布。
- 现成样板：`tmp/task10-wrap.mjs`（2026-09-18 任务 10 已经走过这条路：同一个数据面在 Node 里再建一次、逐键比对、只在一致时落盘）。抄它，别重写。
- 好处：`scripts/render-timeline.ts` **一个字都不用改** → `pnpm timeline` 与以 `tmp/timeline/data.json` 为基准的那 5 个测试全部不受影响；面板 HTML 也不会有"两份实现"的漂移风险（面板直接来自 `viewer-build.js`）。
- 代价：每份重建多一次解析/构建（约 3~5 秒）。重建本来就是稀有操作，可以接受。
- 备选（允许，但必须补断言）：让 `render-timeline.ts` 额外输出 `payload.json`（`data` 用它已经写好的那份；`panels` 用它的 `leaderTable` / `cityTable` / `institutionOrigins` + `leaderRows.slice(0,15)` 组装），由 `build-viewer.ts` 发布成 `viewer/data.json`。走这条就**必须**在 `apps/site/test/viewer-build.test.ts` 里把"离线组装的 panels ≡ 浏览器 `built.panels`"补齐断言（现有断言只覆盖三张表的 HTML 字符串）。
- **不要**让 `loadStoredSave()` 去兼容离线格式——`panels` 缺失会让三张表空着。

**恢复那 6 份（双击一次入口）**

1. 动手前先把 `.dev-storage/index.json` 与 10 份 `meta.json` 备份到 `tmp/taskD-backup/`。
2. 生产者修好后重跑那 6 份（逐份，首次已实测每份 5~10 秒）：
   `node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage`
3. 给用户一个**双击一次**的入口：根目录新增 `修复存档数据.bat`，它调用一个 node 驱动（例如新增 `scripts/rebuild-stored-data.ts`）：读 `.dev-storage/index.json`，只处理 `viewer/data.json` 还不是 payload 的存档，逐个走上面那条命令，中文详情写进 `tmp\修复存档数据.txt`。
   - `.bat` 必须：**CRLF** 换行、控制台只输出 ASCII、绝不 `chcp`、中文只写文件；并把 `修复存档数据.bat` 加进 `apps/site/test/frontend.test.ts` 第 109 行的 `launchers` 列表（那里是这批启动器的静态闸门）。
4. 跑完比对备份：`custom`（当前只有 `b6263f5baf54` 有）与 10 份的 `viewerData: true` 一个不少，`info` 六个键不退化。
5. 不要要求用户手点浏览器 6 次；如果你认为只能那样，先在回报里说明理由再停下。

**验收**

- `node tmp/data-format-audit.js` → 10/10 `PAYLOAD ✓`。
- `pnpm test`、`pnpm verify:deploy` 全绿；`pnpm verify:player` 跑一遍确认不受影响。测试条数以实跑输出为准，不要引用文档里的数字。
- 抽查一份（例如 `990e9f3381e8`）：取 `/saves/<id>/viewer/data.json` 确认 `version === 2 && data.tags && panels.leaderHtml`；托管查看页（`/viewer.html?id=990e9f3381e8`）秒开、三张表不空。
- 回报里写清：改了哪个生产者、6 份是怎么恢复的、**以后重建还会不会踩同一个坑**。

**文件所有权（只有你能改）**：`apps/site/src/build-viewer.ts`；新增的 `scripts/rebuild-stored-data.ts` 与根目录 `修复存档数据.bat`；`apps/site/test/frontend.test.ts`（只加 launcher 名单）；`apps/site/test/viewer-build.test.ts`（只在走备选方案时加断言）；`scripts/render-timeline.ts`（只在走备选方案时改"额外输出 payload"这一小块）。
**不许改**：`apps/site/public/viewer-store.js` 的读取格式与 `loadStoredSave()` 判据、`apps/site/public/**` 的其它文件、`交接.md`、本任务书。

**可直接粘贴的提示词 → 新对话 P**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 卡片客制化-任务书.md 的「2.4 节」——
你是「任务 D：修 viewer/data.json 的格式打架」的执行者，根因、冻结契约、推荐做法都写在任务书里，
照着做，不用再猜。可以先跑 `node tmp/data-format-audit.js` 看改前的基线（现在是 6 份 OFFLINE ✗）。

要点：
- 只改 apps/site/src/build-viewer.ts（推荐），加上新增的 scripts/rebuild-stored-data.ts 与根目录
  修复存档数据.bat；别动 apps/site/public/viewer-store.js 的读取格式与 loadStoredSave() 判据、
  别动 apps/site/public/ 其它文件、别动 交接.md 与本任务书；
- 目标一：让 build-viewer 在 Node 里调站点自己的模块（game-data.js / game-localisation.js /
  game-tables.js / viewer-build.js / eu4-parser.js / parser.js）调 buildViewerData()，逐键比对
  render-timeline 写出的 data.json，一致才把 {version:2, data, panels} 发布成 viewer/data.json，
  不一致就报错退出、不覆盖；原始离线面不再发进存档目录。现成样板是 tmp/task10-wrap.mjs，抄它；
- 目标二：把 6 份退化的存档（fb0e9dce2a8e、3d2c314baae7、0febd901efcb、41512f20a3db、7efd60657781、
  c5464cb5f049）恢复成 payload，入口做成双击一次的 修复存档数据.bat（CRLF／控制台纯 ASCII／绝不 chcp／
  中文写文件／并加进 frontend.test.ts 的 launchers 名单）；动手前先备份 index.json 与 10 份 meta.json 到
  tmp/taskD-backup/；别让用户手点浏览器 6 次，若只能那样先停下来说明理由；
- 验收：node tmp/data-format-audit.js 出 10/10 PAYLOAD；抽查一份取 /saves/<id>/viewer/data.json 确认
  version===2 && data.tags && panels.leaderHtml；meta/index 的 custom 与 viewerData 不丢；
  pnpm test 与 pnpm verify:deploy 全绿、pnpm verify:player 跑一遍不受影响（测试条数以实跑输出为准）；
- 汇报先给结论（改了什么、10 份现在什么格式、怎么双击恢复的、以后重建还会不会踩同一个坑），
  不要叙述调试过程。
```

### 2.1 任务 A（新对话 N）：构建侧收尾

**目标**：把上面那段 fmix32 位混合落进两份 `hashColor`；清掉已被淘汰的"构建期殖民合成旗"。

**文件所有权（只有你能改）**：`scripts/lib/map-assets.ts`、`apps/site/public/viewer-build.js`、`scripts/export-colonial-flags.ts`、`scripts/verify-flags.ts`、`apps/site/public/assets/flags/colonial/`、`apps/site/public/assets/flags/colonial-modded/`。
**不许改**：`app.js`、`viewer.js`、`viewer.html`、`style.css`（任务 B 的地盘）、`render-timeline.ts`、`render-map.ts`、`apps/site/src/**`、`交接.md`、`.dev-storage/**`。

**要求**

1. 两份 `hashColor` 按 §1.4 改成"先位混合再取模"，**逐字一致**；`hslToRgb(h%360, 0.6, 0.45)` 的参数不变（否则地图配色会整体变样）。
2. 删除 `assets/flags/colonial/` 与 `colonial-modded/` 里的旧合成旗（各 11 个文件）——新机制不再需要它们，留着会让人误以为还有一条"缺旗"链路。
3. `scripts/export-colonial-flags.ts`：顶部加一段说明"**已被运行时叠加取代**（前端取宗主旗 + 右半色块），保留仅供以后想导出静态图时使用"。脚本逻辑可以不动。
4. `scripts/verify-flags.ts`：去掉依赖 `colonial/` 目录的断言；并把"缺旗检查"从 `pnpm verify:deploy` 链路里摘掉（新机制不会缺旗，真正可能缺的是宗主旗本身，那由既有检查覆盖）。
5. **不要**动 `render-timeline.ts` / `render-map.ts` 的生成逻辑——它们 import 同一份 `map-assets.ts`，会跟着自动生效。
6. 测试条数不要写死：判断"只增不减"一律以 `pnpm test` 实跑输出为准。

**验收**

- 贴出**改前/改后色相表**（可参考 `tmp/hue-spread.js` 的打印方式：列出 `C00`–`C17`、`D00`–`D05` 的色相与 hex）。改后应散布在整个色环（10–353 这种量级），不再是 43–52 一条线。
- `assets/flags/colonial*` 已清空/删除；仓库里不再有指向这两个目录取旗的代码。
- `pnpm test`、`pnpm verify:deploy` 全绿。

**可直接粘贴的提示词 → 新对话 N**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 卡片客制化-任务书.md 的「1.4 节 + 2.1 节」——
你是「任务 A：构建侧收尾」的执行者。

要点：
- 只改 scripts/lib/map-assets.ts、apps/site/public/viewer-build.js、scripts/export-colonial-flags.ts、
  scripts/verify-flags.ts 与 assets/flags/colonial* 两个目录；别动 app.js、viewer.js、viewer.html、style.css、
  render-timeline.ts、render-map.ts、apps/site/src/**、交接.md、.dev-storage；
- 两份 hashColor 按任务书 1.4 节的代码改成"先 fmix32 位混合再取模"，两份必须逐字一致（只改一份会让
  viewer-build.test.ts 的两数据面逐键比对挂掉）；HSL 参数 0.6/0.45 不变；
- 删掉 assets/flags/colonial 与 colonial-modded 里的旧合成旗；给 export-colonial-flags.ts 加"已被运行时叠加取代"的说明；
  verify-flags.ts 去掉依赖 colonial 目录的断言，并把缺旗检查从 verify:deploy 摘掉；
- 验收要贴改前/改后的色相表（C00–C17、D00–D05），改后应散布开、不再全是黄；
- 测试条数以 pnpm test 实跑输出为准；做完跑 pnpm test 与 pnpm verify:deploy 全绿并贴结果；
- 汇报先给结论，不要叙述调试过程。
```

### 2.2 任务 B（新对话 O）：前端运行时叠加殖民地旗

**目标**：殖民地的旗改成**纯 DOM 叠加**——底图是宗主国的旗（原版或国家娘），右半边蒙一个该殖民地自己的颜色块。不生成图片、不用 canvas、不依赖服务器。

**背景（为什么这样做）**：`C##`/`D##` 是动态殖民 tag，游戏本体没有它们的旗美术。老方案是构建期合成 PNG，但新存档/新殖民地永远要重跑脚本 + 重新部署，而且同一个 tag 在不同战役里宗主不同（实测 34 个 tag 如此）。前端叠加的宗主旗是**按当前存档现取**的，天然正确，而且立刻生效。

**文件所有权（只有你能改）**：`apps/site/public/app.js`、`apps/site/public/viewer.js`、`apps/site/public/viewer.html`、`apps/site/public/style.css`、`scripts/verify-player-run.ts`、`apps/site/test/frontend.test.ts`。
**不许改**：`scripts/**`（任务 A 在做）、`apps/site/src/**`、`交接.md`、`.dev-storage/**`。

**冻结的实现（照这个写）**

1. 结构：
   ```html
   <span class="flagBox"><img class="flag" data-tag="C11" src="…宗主旗…"><i class="flagTint" style="background:#b82e4c"></i></span>
   ```
   ```css
   .flagBox{position:relative;display:inline-block;line-height:0}
   .flagBox>img{display:block}
   .flagTint{position:absolute;left:50%;top:0;width:50%;height:100%;pointer-events:none}
   ```
2. 解析顺序：`DATA.colonialParent[tag]` 有值 → `img.src = FLAG_PREFIX[flagSet] + <宗主>.png`，并挂上色块；没有宗主 → 照旧 `<TAG>.png`；**宗主旗也 404 → 沿用现在的行为**（隐藏 / TAG 方块，别抛错）。
3. 颜色：客户端自己算，**必须与构建侧同一算法**（§1.4 的位混合 + 同样的 HSL 参数）。同一 tag 恒定；**不随「配色」模式变化**（旗子不该跟着地图配色模式变色）。
4. 四个渲染点（都要改到）：①`viewer.js` 的 `applyFlagSet()`（覆盖所有 `img.flag`，列强榜表格在这里）；②`onFlagMissing()`（原来的"再试一次合成旗"改成"换成宗主旗 + 挂色块"）；③`paintHeroStrip()`（查看页标题行主角旗帜排）；④`app.js` 的卡片主角旗帜（`protagonistsMarkup` 与它自己的 404 重试链）。
5. **幂等**：`applyFlagSet()` 会被「旗帜」按钮反复调用——不要重复插入 `.flagTint`；切 base/modded 时只换底图，色块不变。
6. **离线自包含页同样要工作**（不依赖服务器；`file://` 也能跑）；全程禁用 `canvas` / `toDataURL`。
7. **铁律**：`viewer.js` 里**不许出现反引号**（它被内联进 HTML 模板字符串，历史踩过 3 次；`packages/eu4-parser/test/build-guards.test.ts` 静态把关）。

**现成对照物**：`tmp/colonial-preview.html`（直接双击可开）。它用的就是上面这套结构，并列出清 1683 档 44 个殖民地的新旧颜色。**照抄它的结构与 CSS**；同一 tag 算出来的颜色必须与它一致。

**验收**

- `pnpm test`、`pnpm verify:player` 全绿。
- `verify-player-run.ts` 新增断言：殖民地渲染出 `.flagBox` + `.flagTint`、色块颜色由 `flagFill(tag)` 决定且 ≠ 宗主旗色；点「旗帜」切换后底图 `src` 变、`.flagTint` 的 `background` 不变；非殖民 tag 不产生色块。
- `frontend.test.ts` 断言目录页卡片里的殖民地旗帜也是同一结构。
- 本地打开 `http://127.0.0.1:8788/viewer.html?id=990e9f3381e8`：列强榜与标题行旗帜排里的殖民地旗显示为"宗主旗 + 右半色块"；目录页卡片同理。

**可直接粘贴的提示词 → 新对话 O**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 卡片客制化-任务书.md 的「2.2 节」——
你是「任务 B：前端运行时叠加殖民地旗」的执行者，冻结的做法已经写在任务书里。

要点：
- 只改 apps/site/public/app.js、viewer.js、viewer.html、style.css、scripts/verify-player-run.ts、
  apps/site/test/frontend.test.ts；别动 scripts/**（构建侧另一个对话在做）、apps/site/src/**、交接.md、.dev-storage；
- 殖民地旗 = 宗主的旗（跟随「旗帜：原版/国家娘」按钮）+ 右半 50% 色块；结构与 CSS 严格照任务书 2.2 节，
  现成对照物是 tmp/colonial-preview.html（可直接双击看，颜色必须与它一致）；
- 宗主从 DATA.colonialParent[tag] 现取；宗主旗也 404 时沿用现在的行为（隐藏 / TAG 方块）；
  不生成图片、不用 canvas；applyFlagSet 会被反复调用，务必幂等（别重复插色块）；
- 四个渲染点：applyFlagSet()、onFlagMissing()、paintHeroStrip()、app.js 的卡片旗帜；
- viewer.js 里绝对不许出现反引号（会被 HTML 模板字符串截断，已有 3 次前科 + build-guards 守卫）；
- 测试条数以 pnpm test 实跑输出为准；做完跑 pnpm test 与 pnpm verify:player 全绿并贴结果；
- 汇报先给结论，不要叙述调试过程。
```

### 2.3 任务 C（新对话 M）：重建 10 份存档与离线页

**目标**：把任务 A 的颜色函数与任务 B 的 `viewer.js`（含叠加逻辑）**烤进**每份存档的产物——`viewer/data.json`、`viewer/raster.png`、`viewer/thumb.png`、离线自包含页 `viewer/index.html`。不做这一步，页面上看到的还是旧代码生成的东西。

**文件所有权**：不改任何源代码；只写 `.dev-storage/saves/<id>/viewer/**`（这是**唯一**允许写用户数据的任务，且只重建、不删除）。

**要求**

1. 逐份重建（id 列表以 `.dev-storage/index.json` 为准，写这条时是 10 份）：
   ```
   node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage
   ```
   每份 8~16 秒，逐份跑；失败就记下来继续，别硬撑。
2. 重建后抽查（写进回报）：
   - 每份 `viewer/thumb.png` 存在；
   - 离线页 `viewer/index.html` 里能搜到新逻辑（例如 `flagBox`），说明它内联的是新 `viewer.js`；
   - 宗教名正常（例：1683 档与 1566 档的表里是中文，不是 `tengri_pagan_reformed` / `nahuatl` 这种原始键）；
   - 地图颜色抽查：随便挑一个"存档里没有 `map_color` 的国家"，确认颜色与 `tmp/hue-spread.js` 算出来的新值一致（说明 A 的颜色函数真的生效了）。
3. **只重建，绝不删除**用户数据。

**验收**：10/10 重建成功；上面四项抽查通过；回报里给出每份的耗时与结论。

**可直接粘贴的提示词 → 新对话 M**

```
继续 EUSaveAnalyser 项目（F:\WorkPlace\.ProjectsDateless\EUSaveAnalyser）。

先读 交接.md、欧陆风云4-数据位置.md 与 卡片客制化-任务书.md 的「2.3 节」——
你是「任务 C：重建 10 份存档与离线页」的执行者。任务 A（颜色函数）与任务 B（前端叠加旗）都已经完成，
现在要把它们烤进已存存档的产物里。

要点：
- 用 node apps/site/src/build-viewer.ts <id> --save .dev-storage/saves/<id>/original.eu4 --root .dev-storage
  逐份重建 .dev-storage/index.json 里列出的每一份存档（只重建，绝不删除用户数据）；
- 重建后抽查并写进回报：thumb.png 是否都在、离线页里能否搜到 flagBox（说明内联了新 viewer.js）、
  宗教名是否中文、随便挑一个没有 map_color 的国家核对颜色是否等于 tmp/hue-spread.js 的新值；
- 不改任何源代码，别动 交接.md、别动 app.js/viewer.js/style.css；
- 汇报先给结论（重建了几份、四项抽查结果），不要叙述调试过程。
```

---

## 3. 全局纪律（每个任务都适用）

1. **不许碰 `.dev-storage/`**（任务 C 与任务 D 除外，且都只重建、绝不删除；任务 D 动手前先备份 `index.json` 与全部 `meta.json` 到 `tmp/taskD-backup/`）；测试一律用 `tmp/` 或 dev-server 的 `--root` 指到临时目录。
2. **不许改 `交接.md`**，也不许改别的任务名下的文件；需要改就写进回报，由主对话协调。
3. **`.bat` 约定**（如果确实需要做）：CRLF 换行、控制台只输出 ASCII、绝不 `chcp`、中文详情写文件。
4. **中文路径/文本**用 `[IO.File]::ReadAllText/WriteAllText` + `UTF8Encoding($false)`，不要用 `Get-Content/Set-Content`。
5. **测试条数不要写死**：以 `pnpm test` 实跑输出为准（文档里的任何数字都可能过时）。跑测试需要会话是 `danger-full-access`；遇到 `spawn EPERM` 让用户放宽沙箱，别改代码绕过。
6. **禁止反引号**：`viewer.js` 与 `render-timeline.ts` 的模板内出现反引号会截断 HTML 模板（已有 3 次前科 + 静态守卫）。
7. **汇报口径**：先给结论（做完什么 + 验证结果），再列改了哪些文件、怎么验证的、还有什么没做；不要叙述调试过程。

## 4. 收尾（第三个主对话负责，不用派活）

A、B、C 都报完成后，主对话做：端到端目视（目录页卡片 / 查看页标题行旗帜排 / 列强榜殖民地旗 / 封档日期 / 宗教名）→ `pnpm test` + `pnpm verify:player` + `pnpm verify:deploy` → 更新 `交接.md`（把本轮结果写进去、清理已完成条目）。
任务 D 由**新对话 P** 执行，P 报完成后由第三个主对话收尾：`node tmp/data-format-audit.js` 复核 10/10 PAYLOAD → 在托管查看页目视抽查一份（秒开、三张表不空）→ 备份比对 `index.json` / `meta.json` 无退化 → `pnpm test` + `pnpm verify:player` + `pnpm verify:deploy` → 把 `交接.md` 第七节那条"唯一遗留"划掉（`卡片客制化-任务书.md` 若再无待办，也一并说明）。
